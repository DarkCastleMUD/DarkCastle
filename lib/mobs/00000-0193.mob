#1
chain~
Chain~
Chain is here, looking for ideas to steal.
~
Kevin looks like he's between the ages of 22-24.  He is picking his nose.
Every few seconds he types "score" then he jots down some notes.  He
appears to be reading as many help files as he can find.  He also seems
interested in finding a copy of the DC code, and is keeping an eye out for
any Imps that might be nearby.
~
134217731 0 -1
35914280 0 0 -1
0 16 105
1 -1 4000d1+0 0d0+33
0 0
8 8 2 3669751 128 0
C 1
>bribe_prog 50000~
if ispc($n)
  if class($n) == 3
    say You know, I've got the handiest skill to teach you.
    say I've used it to stalk, erm, keep an eye on plenty of
    say people during my day.
    MPTEACHSKILL $n 313
    say Have fun!
    break
  endif
  say Look, I can't help you, sorry.
endif
~
>speech_prog teach~
say Give me 50,000 gold!
~
|
S
#2
Bugmob~
Bugmob~
A bug in the zonefile stands here, waiting to be reported.
~
~
0 0 -1
2097152 0 0 -1
0 0 110
20 10 100d1+0 1d1+0
0 0
8 8 0 0 0 0
>arand_prog 50~
tell Apocalypse There's a bug here!
tell Urizen There's a bug here!
tell Julian There's a bug here!
~
|
S
#3
merchant plat~
the roving platinum merchant~
A roving platinum merchant wanders here, hawking her goods.
~
   The woman before you is obviously a traveling merchant.  She is dressed in
thick and dirty furs, and wears black traveling boots made of thick hide.  She
carries bags full of her goods; the insides glimmer and sparkle magically with
her expensive wares.
~
264004 0 -1
35913896 0 0 -1
-1000 1 54
-130 -70 19378d1+0 1d1+250
100000 2000000
8 8 2 0 0 0
C 1
S
#4
familiar gremlin~
a vicious gremlin~
A small but vicious little gremlin scampers about its master's feet.
~
This small rapacious being skitters about its master's feet hungrily
looking in every direction for some mischief to cause.  While under the
control of its powerful master however, the gremlins knowledge and love
of dismantling magical and mechanical devices is put to better use.  From
time to time the gremlin quickly jumps atop his master's powerful golem
and enacts minor repairs to the assorted combat chips and scraps upon its form.
~
134480131 0 -1
35651616 16 0 -1
0 11 18
-21 0 250d1+0 3d4+34
0 0
8 8 0 0 520 0
T 14 14 9 16 13 0
>give_prog water skin waterskin buffalo~
MPECHO The vicious gremlin hops about and cackles gleefully.
drink skin
drink skin
emote burps loudly.
MPECHO The despicable little gremlin looks about and begins to pee on your leg!
~
>rand_prog 1~
MPECHO The gremlin gnashes its teeth dangerously as it searches for water after midnight.
~
>fight_prog 10~
MPECHOAT $n A vicious gremlin quickly scampers forward and sinks its poisonous claws into your flesh!
MPECHOAROUND $n A vicious gremlin quickly scampers forward and sinks its poisonous claws into $n!
MPDAMAGE $n 2d25 poison
~
|
S
#5
familiar imp~
a clever imp~
A devious black imp peers about nervously awaiting a command from its master.
~
The small imp is black and hairless.  It's body is covered in a shiny
black leather that glistens like the skin of a snake.  Small fangs
protrude from it's mouth like that of a bat.
~
134217987 0 -1
35651592 0 0 -1
0 0 18
-21 0 200d1+0 1d4+33
0 100
8 8 0 6293500 1048576 24
T 18 18 13 16 13 0
>rand_prog 1~
MPECHO A small black imp mutters a few quick words and shimmers briefly before fading from view.
MPBESTOW $i INVISIBLE 8
~
>give_prog gem diamond flawed~
if class($n) == 1
MPECHO The small black imp critically examines the diamond you give it.
say Hrm, well its not perfect, but it still looks reasonably tasty.
MPECHO Pops the diamond in his mouth, chews, and swallows.
say Not bad, I guess, perhaps deserving of at least a little help.
c 'cure serious' $n
MPJUNK diamond
else
say Thanks for the diamond, chump!
MPECHO The small imp quickly pops the diamond in its mouth.
mmm
MPJUNK diamond
break
endif
endif
~
>rand_prog 1~
MPECHO A small imp looks about mischievously, then quickly scuttles behind its master's shoulder.
~
>rand_prog 1~
MPECHO The small imp peeks in your pockets attempting to find its favorite tasty treat, diamonds.
~
>give_prog gem diamond shimmering~
if class($n) == 1
MPECHO The small black imp rubs his hands together gleefully.
say Many thanks! Perhaps this will help protect you from abyssal denizens like myself.
MPECHO Pops the diamond in his mouth, chews, and swallows.
c 'sanctuary' $n
MPJUNK diamond
else
say Thanks for the diamond, chump!
MPECHO The small imp quickly pops the diamond in its mouth.
mmm
MPJUNK diamond
break
endif
endif
~
|
S
#6
familiar chipmunk~
a small chipmunk~
A small chipmunk skitters back and forth happily.
~
The little chipmunk is small and brown with a little stripe down its
back of white fur.  It appears quite content with life and simply
waits for an opportunity to help its master.
~
134480131 0 -1
36175904 0 0 -1
0 21 18
-21 0 275d1+0 3d5+34
0 250
8 8 1 0 128 256
T 18 11 10 18 16 0
>fight_prog 8~
MPECHO The chipmunk skitters quickly under the feet of its opponent upsetting their balance!
trip
~
>greet_prog 10~
if class($n) == 11
or class($n) == 9
  MPECHOAROUND $n The small chipmunk looks about furtively and begins vigorously humping $n's leg.
  MPECHOAT $n The small chipmunk looks about furtively and begins vigorously humping your leg.
 break
endif
~
|
S
#7
familiar owl~
a wise owl~
An owl is here, surveying his surroundings.
~
Among the wisest and most intelligent of the avian nobility, owls
have been said to possess powers allowing their masters to view
the world through their far-seeing eyes.  Though loyal and trust-
worthy servants, the wise owls do have a penchant for tasty mice
and are known to be very prideful.
Should a druid master wish its owl to perform its tasks unfailingly
they would do well to carry an ample supply of such mice.
~
134217731 0 -1
304349216 0 0 -1
0 15 10
-22 0 250d1+0 2d7+34
0 0
8 8 2 0 1024 2097152
T 8 16 18 15 13 0
>fight_prog 20~
MPECHOAT $n A shrewd owl swoops in from above, and gives you a nasty peck on the face!
MPECHOAROUND $n A shrewd owl swoops in from above, and pecks $n on the face!
MPDAMAGE $n 2d25 undefined
~
|
S
#8
empty mob~
$B$4-= MOB SLOT FOR CREATE GOLEM - DO NOT TOUCH =-$R~
blank mob for create golem - do not touch
~
~
0 0 -1
2097152 0 0 -1
0 0 0
20 0 1d1+0 1d1+0
0 0
8 8 0 0 0 0
>speech_prog p yorick~
emote pulls a skull from his back pocket.
emote clears his throat.
say Alas, poor Yorick! I knew him, Horatio: a fellow
say of infinite jest, of most excellent fancy: he hath
say borne me on his back a thousand times and now, how
say abhorred in my imagination it is! my gorge rims at
say it. Here hung those lips that I have kissed I know
say not how oft. Where be your gibes now? your
say gambols? your songs? your flashes of merriment,
say that were wont to set the table on a roar? Not one
say now, to mock your own grinning? quite chap-fallen?
say Now get you to my lady's chamber, and tell her, let
say her paint an inch thick, to this favour she must
say come; make her laugh at that. Prithee, Horatio, tell
say me one thing.
smug
say Snooguns.
~
|
S
#9
spanish inquisition~
the spanish inquisition~
The Spanish Inquisition is here, asking you if you know any Canadians.
~
~
2 0 -1
2097152 0 0 -1
0 0 0
20 10 0d1+0 1d1+0
0 0
8 8 0 0 0 0
>catch_prog 1~
say Oh.. and I bet you didn't expect us.
wink
~
>rand_prog 20~
if ispc($r)
  say Hey.. Psst. $r! Know any Canadians?
  say I've been charged with bringing them in for making that show about the hallucinating mountie.
  say Nasty business, ya'know?
  mpthrow 9 1 2
else
  say Anyone know any Canadians?
  say I've been charged with bringing them in for making that show about the hallucinating mountie.
  say Nasty business, ya'know?
  mpthrow 9 1 2
endif
~
|
S
#10
jay~
Jay~
Jay is here, selling drugs to the kids and cursing incessantly.
~
C~
2361094 0 -1
1614097096 0 0 -1
0 1 69
-80 -200 6900d1+0 400d400+400
35000 500000
8 8 1 8388607 0 0
T 25 25 25 25 25 0
>speech_prog p yorick~
emote pulls a skull from his back pocket.
emote clears his throat.
say Alas, poor Yorick! I knew him, Horatio: a fellow
say of infinite jest, of most excellent fancy: he hath
say borne me on his back a thousand times and now, how
say abhorred in my imagination it is! my gorge rims at
say it. Here hung those lips that I have kissed I know
say not how oft. Where be your gibes now? your
say gambols? your songs? your flashes of merriment,
say that were wont to set the table on a roar? Not one
say now, to mock your own grinning? quite chap-fallen?
say Now get you to my lady's chamber, and tell her, let
say her paint an inch thick, to this favour she must
say come; make her laugh at that. Prithee, Horatio, tell
say me one thing.
smug
say Snooguns.
~
|
S
#11
Sheldon, The Brave and Intrepid Adventurer~
Sheldon Mob For Posting - Don't Delete~
This mob has keywords set specifically for posting on mortal boards.
~
Empty description.
~
526082 0 -1
2097192 0 0 -1
0 1 50
19 10 100d1+0 1d1+1
0 0
8 8 0 0 0 0
T 11 20 11 11 11 0
S
#12
$0.$R~
$B$4-=MOB PROC MOB - DO NOT EDIT=-$R~
$0.$R
~
~
0 0 -1
2359304 0 0 -1
0 0 0
20 10 0d1+0 1d1+0
0 0
8 8 0 0 0 0
S
#24
their own stupidity~
stupidity~
You see something here, and you know you're going to die.
~
You see something here and you have the feeling you're about to die.
~
919328 4 -1
35916416 4096 32 -1
0 31 108
-80 -150 64000d1+0 150d50+100
0 0
8 8 1 263432 0 1048576
C 4
S
#25
lenny corgi~
Lenny the Corgi~
Lenny the hypervigilant Corgi sits here watching your every move.
~
   Lenny appears to be a medium-sized Pembroke Welsh Corgi.  Although he is
descended from royal lineage, Lenny is mostly only concerned with the locations
of his squeaky toys, prompt meals and guarding the members of his household
from such serious threats as mylar balloons, his own shadow and wherever "that
mooing noise" is coming from.  He regards you with bright-eyed intensity that
could easily be mistaken for insanity.
~
140511170 2 -1
1074004480 134217728 0 -1
0 21 105
20 10 35000d1+0 1d10+1
0 0
6 6 1 3934204 128 0
T 10 28 15 28 18 0
>speech_prog test~
MPOLOAD 30019 random
give durendal ozymandias
~
>rand_prog 1~
MPECHO Lenny's body goes rigid and his ears perk up.
MPPAUSE 2
MPECHO Lenny barks furiously at something outside that you can neither see nor hear.
MPPAUSE 2
MPECHO Lenny stares intently out the window.
~
>speech_prog p Do my taxes~
MPPAUSE 2
MPECHO Lenny barks loudly.
MPPAUSE 2
MPECHO Lenny clears his throat.
say Sir, I am a dog.  I lack the proper forms to attempt such a feat.  Besides, I cannot hold a pencil.
MPPAUSE 2
MPECHO Lenny cocks his head slightly and drools on the floor.
~
>speech_prog p do you want dog ice cream~
MPPAUSE 2
MPECHO Lenny freezes and stares at you.
MPPAUSE 1
MPECHO Lenny barks loudly.
MPPAUSE 1
MPECHO Lenny barks loudly.
MPPAUSE 1
MPECHO Lenny barks loudly.
MPPAUSE 1
MPECHO Lenny barks loudly.
MPPAUSE 1
MPECHO Lenny's non-stop barking is beginning to get annoying.  Perhaps you shouldn't talk about dog ice cream.
~
>speech_prog p do you want the ball~
MPPAUSE 2
MPECHO Lenny's body goes rigid.  He stares at you intently.
MPPAUSE 1
MPECHO Lenny issues a sharp bark and cocks his head to the side, listening...
~
>speech_prog p fetch~
stand
MPPAUSE 2
MPECHO Lenny takes off like a madman, crashing into a wall before stopping in front of The Ball(tm).
MPPAUSE 2
get ball
wear ball face
MPPAUSE 2
MPECHO Lenny races back at top speed.
MPPAUSE 2
sit
remove ball
give ball $n
MPECHOAT $n Lenny tilts his head and looks at you expectantly.
~
|
S
#71
"Terrible" Terry Tate~
Terry Tate, Office Linebacker~
"Terrible" Terry Tate: Office Linebacker is here looking to tackle you.
~
Huge muscles bulging, alert eyes and quick reflexes.  Clearly this is not
a person to mess with.  Unfortunately he wants to mess with you.
~
919328 0 -1
1923621608 0 0 -1
-1000 29 100
-80 -100 30053d1+0 10d10+100
0 0
8 8 0 528 1048576 8
C 7
>fight_prog 15~
say YOU MUST BE OUT OF YOUR MIND SON!
say YOU CAN'T JUST CUT THE CHEESE WHEREVER YOU PLEASE!
MPECHOAT $n "Terrible" Terry Tate: Office Linebacker throws his entire weight at you!
bash $n
say That's just NASTAY son!
~
>fight_prog 15~
say YOU CAN'T JUST WALK AWAY FROM A K22 PAPER JAM!
say YOU MUST BE OUT OF YOUR MIND SON!
MPECHOAT $n "Terrible" Terry Tate: Office Linebacker tries to take your legs out from underneath you!
trip $n
~
>attack_prog 100~
say YOU KNOW YOU HAVE TO USE A COVER SHEET ON YOUR TPS FORMS DOUG!
MPECHOAT $n "Terrible" Terry Tate: Office Linebacker tackles you to the ground!
MPDAMAGE $n 5d10 undefined
~
|
S
#73
orro quest guide~
Orro, the Quest Guide~
Orro, the Quest Guide is here, pleased to show you quest items.
~
~
165634 4 -1
536871040 167772160 0 -1
0 0 105
-5 10 10000d1+0 25d25+25
0 5
8 8 0 0 0 0
S
#88
fire elemental conjured~
a blazing fire elemental~
A controlled $B$4fire$3 elemental burns brightly here.$R
~
~
134267666 0 -1
50397256 16777216 0 -1
0 27 49
-25 -22 750d1+0 8d4+30
0 0
8 8 0 16 4194828 1040
>speech_prog p hit me~
say hitting you
mpdamage $n 1d10 fire
say hit you
~
|
S
#89
ice elemental conjured~
a freezing ice elemental~
A controlled $B$7ice$3 elemental chills the air here.$R
~
~
134267666 0 -1
50333768 16777216 0 -1
0 27 49
-25 -22 1250d1+0 8d4+30
0 0
8 8 0 4194816 28 4195840
>speech_prog p hit me~
say hitting you
mpdamage $n 1d10 fire
say hit you
~
|
S
#90
energy elemental lightning conjured~
a crackling lightning elemental~
A controlled $B$5lightning$3 elemental crackles loudly here.$R
~
~
134267666 0 -1
855638088 16777216 0 -1
0 27 49
-25 -22 1000d1+0 8d4+30
0 0
8 8 0 32 2097164 1056
>speech_prog p hit me~
say hitting you
mpdamage $n 1d10 fire
say hit you
~
|
S
#91
earth elemental conjured~
a stone-faced earth elemental~
A controlled $B$0earth$3 elemental shakes the ground here.$R
~
~
134267666 0 -1
50331720 16777248 0 -1
0 27 49
-25 -22 1500d1+0 8d4+30
0 0
8 8 0 0 1048588 525314
>speech_prog p hit me~
say hitting you
mpdamage $n 1d10 fire
say hit you
~
|
S
#92
spirit panther~
the spirit of a panther~
The spirit of a panther hovers above the ground here.
~
~
0 0 -1
0 0 0 -1
0 0 0
20 10 0d1+0 1d1+0
0 0
8 8 0 0 0 0
>speech_prog p hit me~
say hitting you
mpdamage $n 1d10 fire
say hit you
~
|
S
#93
snake spirit~
the spirit of a snake~
An ethereal snake hovers slightly above the ground here.
~
~
0 0 -1
0 0 0 -1
0 0 0
20 10 0d1+0 1d1+0
0 0
8 8 0 0 0 0
>speech_prog p hit me~
say hitting you
mpdamage $n 1d10 fire
say hit you
~
|
S
#94
spirit scorpion~
the spirit of a scorpion~
The spirit of a scorpion hovers slightly above the ground here.
~
~
0 0 -1
0 0 0 -1
0 0 0
20 10 0d1+0 1d1+0
0 0
8 8 0 0 0 0
>speech_prog p hit me~
say hitting you
mpdamage $n 1d10 fire
say hit you
~
|
S
#95
penguin spirit~
the spirit of a penguin~
The spirit of a penguin hovers slightly above the ground here.
~
~
0 0 -1
0 0 0 -1
0 0 0
20 10 0d1+0 1d1+0
0 0
8 8 0 0 0 0
>speech_prog p hit me~
say hitting you
mpdamage $n 1d10 fire
say hit you
~
|
S
#96
spirit eagle~
the spirit of an eagle~
The spirit of an eagle soars far above the ground here.
~
~
0 0 -1
0 0 0 -1
0 0 0
20 10 0d1+0 1d1+0
0 0
8 8 0 0 0 0
>speech_prog p hit me~
say hitting you
mpdamage $n 1d10 fire
say hit you
~
|
S
#97
spirit Pirahna~
the spirit of a Pirahna~
The spirit of a Pirahna swims through the water here.
~
~
0 0 -1
0 0 0 -1
0 0 0
20 10 0d1+0 1d1+0
0 0
8 8 0 0 0 0
>speech_prog p hit me~
say hitting you
mpdamage $n 1d10 fire
say hit you
~
|
S
#98
spirit goat~
the spirit of a goat~
The spirit of a goat is here, hovering slightly above the ground.
~
~
0 0 -1
0 0 0 -1
0 0 0
20 10 0d1+0 1d1+0
0 0
8 8 0 0 0 0
>speech_prog p hit me~
say hitting you
mpdamage $n 1d10 fire
say hit you
~
|
S
#99
spirit dog~
the spirit of a dog~
The spirit of a dog is here, hovering slightly above the ground.
~
~
0 0 -1
0 0 0 -1
0 0 0
20 10 0d1+0 1d1+0
0 0
8 8 0 0 0 0
>speech_prog p hit me~
say hitting you
mpdamage $n 1d10 fire
say hit you
~
|
S
#100
guard spam~
the Spam Guard~
The Spam Guard checks you over to see if you have spammed recently.
~
The guard just chuckles and says, "You spam -- you die!"
~
558855 0 -1
2097192 0 0 -1
0 1 109
-20 -10 33d1+0 0d0+5
10000 1
8 8 1 0 0 0
C 6
>speech_prog bleh~
mpsetmath room_13.description {this is
a new desc
yay
}
~
>all_greet_prog 100~
mpsetmath room_13.description {testing
blahblah}
~
|
S
#101
skillset mob~
PROC MOB MAGE LEVEL 1-20~
This is a Skillset Mob for the Mage Class, levels 1-20.
~
~
0 0 -1
35651624 0 0 -1
0 0 0
20 10 1d1+0 1d1+0
0 0
8 8 0 0 0 0
>load_prog 100~
cast 'shield' $i
cast 'strength' $i
~
>arand_prog 1~
if isspelled($i) shield
  else
    cast 'shield' $i
  break
endif
if isspelled($i) strength
  else
    cast 'strength' $i
  break
endif
~
>fight_prog 7~
cast 'burning hands'
~
>fight_prog 7~
say Missiles! Find your mark!
cast 'magic missile'
~
|
S
#102
skillset mob~
PROC MOB MAGE LEVEL 21-35~
This is a Skillset Mob for the Mage Class, levels 21-35.
~
~
0 0 -1
35651624 0 0 -1
0 0 0
20 10 1d1+0 1d1+0
0 0
8 8 0 0 0 0
>load_prog 100~
cast 'shield' $i
cast 'strength' $i
cast 'armor' $i
cast 'resist fire' $i
~
>arand_prog 1~
if isspelled($i) shield
  else
    cast 'shield' $i
  break
endif
if isspelled($i) strength
  else
    cast 'strength' $i
  break
endif
if isspelled($i) armor
  else
    cast 'armor' $i
  break
endif
if isspelled($i) resist_fire
  else
    cast 'resist fire' $i
  break
endif
~
>fight_prog 10~
cast 'fireball'
~
>fight_prog 10~
MPECHOAT $n $I closes $l eyes and begins to concentrate on your demise.
MPECHOAROUND $n $I closes $l eyes and begins to concentrate on $n's demise.
cast 'lightning bolt'
~
>fight_prog 4~
say Missiles! Find your mark!
cast 'magic missile'
~
|
S
#103
skillset mob~
PROC MOB MAGE LEVEL 36-50~
This is a Skillset Mob for the Mage Class, levels 36-50.
~
~
0 0 -1
35913768 0 0 -1
0 0 0
20 10 1d1+0 1d1+0
0 0
8 8 0 0 0 0
>load_prog 100~
cast 'clarity'
cast 'armor'
cast 'infravision'
if rand(10)
  cast 'fly'
endif
cast 'strength'
cast 'shield'
cast 'resist magic'
cast 'haste'
cast 'true sight'
cast 'resist fire'
if $i.level > 38
  cast 'stoneskin'
endif
if $i.level > 46
  if rand(5)
    cast 'fireshield'
  endif
endif
~
>arand_prog 1~
if isspelled($i) armor
  else
    cast 'armor'
  break
endif
if isspelled($i) strength
  else
    cast 'strength'
  break
endif
if isspelled($i) shield
  else
    cast 'shield'
  break
endif
if isspelled($i) haste
  else
    cast 'haste'
  break
endif
if isspelled($i) resist_fire
  else
    cast 'resist fire'
  break
endif
if $i.level > 38
  if isspelled($i) stoneskin
    else
      cast 'stoneskin'
    break
  endif
endif
if $i.level > 46
  if isspelled($i) fireshield
    else
      if rand(75)
        cast 'fireshield'
        break
      endif
    endif
endif
if isspelled($i) clarity
  else
    cast 'clarity'
  break
endif
if isspelled($i) true_sight
  else
    cast 'true sight'
  break
endif
if isspelled($i) resist_magic
  else
    cast 'resist magic'
  break
endif
~
>fight_prog 25~
if isspelled($n) sanctuary
    cast 'dispel magic'
else
  if isspelled($n) fireshield
    cast 'dispel magic'
  else
    if isspelled($n) stoneshield
      cast 'dispel magic'
    else
      if isspelled($n) lightning_shield
        cast 'dispel magic'
      else
        if isspelled($n) acid_shield
        cast 'dispel magic'
        endif
      endif
    endif
  endif
endif
~
>fight_prog 20~
if $i.level < 40
  if rand(20)
    cast 'magic missile'
    break
  endif
  if rand(25)
    cast 'burning hands'
    break
  endif
  if rand(33)
    cast 'lightning bolt'
    break
  endif
  if rand(50)
    cast 'chill touch'
    break
  endif
  cast 'fireball'
  break
endif
if $i.level < 45
  if rand1k(166)
    cast 'magic missile'
    break
  endif
  if rand1k(200)
    cast 'burning hands'
    break
  endif
  if rand1k(250)
    cast 'lightning bolt'
    break
  endif
  if rand1k(333)
    cast 'chill touch'
    break
  endif
  if rand1k(500)
    cast 'fireball'
    break
  endif
  cast 'meteor swarm'
  break
endif
if rand(50)
  if rand(25)
    cast 'magic missile'
    break
   endif
  if rand(33)
    cast 'burning hands'
    break
  endif
  if rand(50)
    cast 'lightning bolt'
    break
  endif
    cast 'chill touch'
    break
else
  if rand(33)
    cast 'fireball'
    break
  endif
  if rand(50)
    cast 'meteor swarm'
    break
  endif
  cast 'hellstream'
  break
endif
~
|
S
#104
skillset mob~
PROC MOB MAGE LEVEL 51+~
This is a Skillset Mob for the Mage Class, level 51+.
~
~
0 0 -1
35913768 0 0 -1
0 0 0
20 10 1d1+0 1d1+0
0 0
8 8 0 0 0 0
>act_prog p utters the words, 'dispel magic'~
if isfight($i)
if ispc($n)
   if $i.level > 89
      if rand(15)
         laugh $n
         if rand(33)
            cast 'dispel magic' $n
            else
            if rand(50)
               cast 'hellstream' $n
               else
               cast 'meteor swarm' $n
               endif
            endif
         endif
      endif
   if $i.level > 74
      if $i.level < 90
         if rand(10)
            laugh $n
            if rand(33)
               cast 'dispel magic' $n
               else
               if rand(50)
                  cast 'hellstream' $n
                  else
                  cast 'meteor swarm' $n
                  endif
               endif
            endif
         endif
      endif
   if $i.level > 50
      if $i.level < 75
         if rand(5)
            laugh $n
            if rand(33)
               cast 'dispel magic' $n
               else
               if rand(50)
                  cast 'hellstream' $n
                  else
                  cast 'meteor swarm' $n
                  endif
               endif
            endif
         endif
      endif
   endif
endif
~
>act_prog p utters the words, 'zawsufuq hajqp'~
if isfight($i)
if ispc($n)
   if $i.level > 89
      if rand(15)
         say Great, all I need is a little goth wannabe running around playing as a vampire.
         cast 'hellstream' $n
         endif
      endif
   if $i.level > 74
      if $i.level < 90
         if rand(10)
            say Great, all I need is a little goth wannabe running around playing as a vampire.
            if rand(50)
               cast 'hellstream' $n
               else
               cast 'meteor swarm' $n
               endif
            endif
         endif
      endif
   if $i.level > 50
      if $i.level < 75
         if rand(5)
            say Great, all I need is a little goth wannabe running around playing as a vampire.
            if rand(50)
               cast 'hellstream' $n
               else
               cast 'meteor swarm' $n
               endif
            endif
         endif
      endif
   endif
endif
~
>act_prog p p utters the words, 'aque bragh'~
if isfight($i)
if ispc($n)
   if $i.level > 89
      if rand(15)
         say Just because your eye liner is running, doesn't mean you have to take it out on me.
         cast 'hellstream' $n
         endif
      endif
   if $i.level > 74
      if $i.level < 90
         if rand(10)
            say Just because your eye liner is running, doesn't mean you have to take it out on me.
            cast 'hellstream' $n
            endif
         endif
      endif
   if $i.level > 50
      if $i.level < 75
         if rand(5)
            say Just because your eye liner is running, doesn't mean you have to take it out on me.
            if rand(50)
               cast 'hellstream' $n
               else
               cast 'meteor swarm' $n
               endif
            endif
         endif
      endif
   endif
endif
~
>act_prog p slams a bolt of focused ki energy into the flow of magic around you!~
if isfight($i)
if ispc($n)
   if $i.level > 89
      if rand(15)
         say That wasnt very feng shui of you.
         cast 'hellstream' $n
         endif
      endif
   if $i.level > 74
      if $i.level < 90
         if rand(10)
            say That wasnt very feng shui of you.
            if rand(75)
               cast 'hellstream' $n
               else
               cast 'meteor swarm' $n
               endif
            endif
         endif
      endif
   if $i.level > 50
      if $i.level < 75
         if rand(5)
            say That wasnt very feng shui of you.
            if rand(25)
               cast 'hellstream' $n
               else
               cast 'meteor swarm' $n
               endif
            endif
         endif
      endif
   endif
endif
~
>act_prog p focuses and punches you square in the chest~
if isfight($i)
if ispc($n)
   if $i.level > 89
      if rand(15)
         say Was that supposed to be a love tap?
         cast 'hellstream' $n
         endif
      endif
   if $i.level > 74
      if $i.level < 90
         if rand(10)
            say Was that supposed to be a love tap?
            cast 'hellstream' $n
            endif
         endif
      endif
   if $i.level > 50
      if $i.level < 75
         if rand(5)
            say Was that supposed to be a love tap?
            if rand(50)
               cast 'hellstream' $n
               else
               cast 'meteor swarm' $n
               endif
            endif
         endif
      endif
   endif
endif
~
>act_prog p sings a witty little limerick to you!~
if isfight($i)
if ispc($n)
   if $i.level > 89
      if rand(15)
         say What's with all the American Idol rejects coming out of the woodwork?
         cast 'hellstream' $n
         endif
      endif
   if $i.level > 74
      if $i.level < 90
         if rand(10)
            say What's with all the American Idol rejects coming out of the woodwork?
            if rand(50)
               cast 'hellstream' $n
               else
               cast 'meteor swarm' $n
               endif
            endif
         endif
      endif
   if $i.level > 50
     if $i.level < 75
         if rand(5)
            say What's with all the American Idol rejects coming out of the woodwork?
            if rand(25)
               cast 'hellstream' $n
               else
               cast 'meteor swarm' $n
               endif
            endif
         endif
      endif
   endif
endif
~
>act_prog p lashes out quickly and cripples you with a painful blow!~
if isfight($i)
if ispc($n)
   if $i.level > 89
      if rand(15)
         say I will not be the gimp!
         cast 'hellstream' $n
         endif
      endif
   if $i.level > 74
      if $i.level < 90
         if rand(10)
            say I will not be the gimp!
            cast 'hellstream' $n
            endif
         endif
      endif
   if $i.level >  50
      if $i.level < 75
         if rand(15)
            say I will not be the gimp!
            if rand(50)
               cast 'hellstream' $n
               else
               cast 'meteor swarm' $n
               endif
            endif
         endif
      endif
   endif
endif
~
>act_prog p with lethal accuracy and strength.~
if isfight($i)
if ispc($n)
   if $i.level > 89
      if rand(15)
         say I'll show you lethal accuracy...
         if rand(50)
            cast 'paralyze' $n
            else
            cast 'hellstream' $n
            endif
         endif
      endif
   if $i.level > 74
      if $i.level < 90
         if rand(10)
            say I'll show you lethal accuracy...
            if rand(50)
               cast 'paralyze' $n
               else
               cast 'hellstream' $n
               endif
            endif
         endif
      endif
   if $i.level > 50
      if $i.level < 75
         if rand(5)
            say I'll show you lethal accuracy...
            if rand(33)
               cast 'paralyze' $n
               else
               cast 'hellstream' $n
               endif
            endif
         endif
      endif
   endif
endif
~
>act_prog p circles around~
if isfight($i)
if ispc($n)
   if $i.level > 89
      if rand(15)
         say Striking me from behind!
         if rand(50)
            say Perhaps you should flee like the coward you are!
            cast 'fear' $n
            else
            cast 'hellstream' $n
            endif
         endif
      endif
   if $i.level > 74
      if $i.level < 90
         if rand(10)
            say Striking me from behind!
            if rand(50)
               say Perhaps you should flee like the coward you are!
               cast 'fear' $n
               else
               cast 'hellstream' $n
               endif
            endif
         endif
      endif
   if $i.level > 50
      if $i.level < 75
         if rand(5)
            say Striking me from behind!
            if rand(33)
               say Perhaps you should flee like the coward you are!
               cast 'fear' $n
               else
               if rand(50)
                  cast 'hellstream' $n
                  else
                  cast 'meteor swarm' $n
                  endif
               endif
            endif
         endif
      endif
   endif
endif
~
>act_prog p utters the words, 'unmanifgz'~
if isfight($i)
if ispc($n)
   if $i.level > 89
      if rand(15)
         curse $n
         cast 'hellstream' $n
         endif
      endif
   if $i.level > 74
      if $i.level < 90
         if rand(10)
            curse $n
            if rand(50)
               cast 'hellstream' $n
               else
               cast 'meteor swarm' $n
               endif
            endif
         endif
      endif
   if $i.level > 50
      if $i.level < 75
         if rand(5)
            curse $n
            if rand(50)
               cast 'hellstream' $n
               else
               cast 'meteor swarm' $n
               endif
            endif
         endif
      endif
   endif
endif
~
>act_prog p utters the words, 'ubralicra'~
if isfight($i)
if ispc($n)
   if $i.level > 89
      if rand(15)
         say Oh, like I need to see you to kick your arse!
         if rand(50)
            if cansee($n)
                cast 'hellstream' $n
                else
                cast 'solar gate' $n
                endif
            endif
         endif
      endif
   if $i.level > 74
      if $i.level < 90
         if rand(10)
            say Oh, like I need to see you to kick your arse!
            if rand(33)
               if cansee($n)
                  cast 'hellstream' $n
                  else
                  if rand(50)
                      cast 'solar gate' $n
                      else
                      cast 'firestorm' $n
                      endif
                   endif
               endif
            endif
         endif
      endif
   if $i.level > 50
      if $i.level < 75
         if rand(5)
            say Oh, like I need to see you to kick your arse!
            if rand(33)
               if cansee($n)
                   cast 'meteor swarm' $n
                   else
                  if rand(50)
                     cast 'hellstream' $n
                     else
                     cast 'firestorm' $n
                     endif
                  endif
               endif
            endif
         endif
      endif
   endif
endif
~
>act_prog p utters the words, 'qahijf gsfal'~
if isfight($i)
if ispc($n)
   if $i.level > 89
      if rand(15)
         say Is that supposed to amuse me? I'm a master mage, not a 5 year old, you douche bag.
         cast 'hellstream' $n
         endif
      endif
   if $i.level > 74
      if $i.level < 90
         if rand(10)
            say Is that supposed to amuse me? I'm a master mage, not a 5 year old, you douche bag.
            if rand(50)
               cast 'hellstream' $n
               else
               cast 'meteor swarm' $n
               endif
            endif
         endif
      endif
   if $i.level > 50
      if $i.level < 75
         if rand(5)
            say Is that supposed to amuse me? I'm a master mage, not a 5 year old, you douche bag.
            if rand(33)
               cast 'hellstream' $n
               else
               cast 'meteor swarm' $n
               endif
            endif
         endif
      endif
   endif
endif
~
>act_prog p utters the words, 'qarr solilla'~
if isfight($i)
if ispc($n)
   if $i.level > 89
      if rand(15)
         say I've gotten a bigger jolt sticking a 9-volt to my tongue.
         cast 'hellstream' $n
         endif
      endif
   if $i.level > 74
      if $i.level < 90
         if rand(10)
            say I've gotten a bigger jolt sticking a 9-volt to my tongue.
            if rand(50)
               cast 'hellstream' $n
               else
               cast 'meteor swarm' $n
               endif
            endif
         endif
      endif
   if $i.level > 50
      if $i.level < 75
         if rand(5)
            say I've gotten a larger jolt sticking a 9-volt to my tongue.
            if rand(33)
               cast 'hellstream' $n
               else
               cast 'meteor swarm' $n
               endif
            endif
         endif
      endif
   endif
endif
~
>act_prog p utters the words, 'gji brul'~
if isfight($i)
if ispc($n)
   if $i.level > 89
      if rand(15)
         say I've had sunburns that hurt more than that.
         cast 'hellstream' $n
         endif
      endif
   if $i.level > 74
      if $i.level < 90
         if rand(10)
            say I've had sunburns that hurt more than that.
            if rand(50)
               cast 'hellstream' $n
               else
               cast 'meteor swarm' $n
               endif
            endif
         endif
      endif
   if $i.level > 50
      if $i.level < 75
         if rand(5)
            say I've had sunburns that hurt more than that.
            if rand(33)
               cast 'hellstream' $n
               else
               cast 'meteor swarm' $n
               endif
            endif
         endif
      endif
   endif
endif
~
>act_prog p utters the words, 'efaxi'~
if isfight($i)
if ispc($n)
   if $i.level  > 89
      if rand(15)
         say Oh come on!  These robes are dry clean only!
         cast 'hellstream' $n
         endif
      endif
   if $i.level > 74
      if $i.level < 90
         if rand(10)
            say Oh come on!  These robes are dry clean only!
            if rand(50)
               cast 'hellstream' $n
               else
               cast 'meteor swarm' $n
               endif
            endif
         endif
      endif
   if $i.level > 50
      if $i.level < 75
         if rand(5)
            say Oh come on!  These robes are dry clean only!
            if rand(33)
               cast 'hellstream' $n
               else
               cast 'meteor swarm' $n
               endif
            endif
         endif
      endif
   endif
endif
~
>act_prog p deathstroke~
if isfight($i)
if ispc($n)
   if $i.level > 89
      if rand(20)
         say Leave my kidneys alone!
         cast 'hellstream' $n
         endif
      endif
   if $i.level > 74
      if $i.level < 90
         if rand(15)
            say Leave my kidneys alone!
            if rand(50)
               cast 'hellstream' $n
               else
               cast 'meteor swarm' $n
               endif
            endif
         endif
      endif
   if $i.level > 50
      if $i.level < 75
         if rand(10)
            say Leave my kidneys alone!
            if rand(50)
               cast 'hellstream' $n
               else
               cast 'meteor swarm' $n
               endif
            endif
         endif
      endif
   endif
endif
~
>act_prog p attempts to hit you in your solar plexus!~
if isfight($i)
if ispc($n)
   if $i.level > 89
      if rand(15)
         say Rat Bastard!
         cast 'hellstream' $n
         endif
      endif
   if $i.level > 74
      if $i.level < 90
         if rand(10)
            say Rat Bastard!
            if rand(50)
               cast 'meteor swarm' $n
               else
               cast 'hellstream' $n
               endif
            endif
         endif
      endif
   if $i.level > 50
      if $i.level < 75
         if rand(5)
            say Rat Bastard!
            if rand(25)
               cast 'hellstream' $n
               else
               cast 'meteor swarm' $n
               endif
            endif
         endif
      endif
   endif
endif
~
>act_prog p delivers a HARD BLOW into your solar plexus! You are STUNNED!~
if isfight($i)
if ispc($n)
   if $i.level > 89
      if rand(15)
         say Rat Bastard!
         cast 'paralyze' $n
         endif
      endif
   if $i.level > 74
      if $i.level < 90
         if rand(10)
            say Rat Bastard!
            if rand(50)
               cast 'paralyze' $n
               else
               cast 'hellstream' $n
               endif
            endif
         endif
      endif
   if $i.level > 50
      if $i.level < 75
         if rand(5)
            say Rat Bastard!
            if rand(25)
               cast 'paralyze' $n
               else
               cast 'hellstream' $n
               endif
            endif
         endif
      endif
   endif
endif
~
>act_prog p lunges forward in an attempt to smash into you but falls flat on~
if isfight($i)
if ispc($n)
   if $i.level > 89
      if rand(15)
         say Maybe you should stick to being a dumb meat shield.  Oh wait...
         laugh $n
         if rand(25)
            cast 'paralyze' $n
            else
            cast 'hellstream' $n
            endif
         endif
      endif
   if $i.level > 74
      if $i.level < 90
         if rand(10)
            say Maybe you should stick to being a dumb meat shield.  Oh wait...
            laugh $n
            if rand(25)
               cast 'paralyze' $n
               else
               cast 'hellstream' $n
               endif
            endif
         endif
      endif
   if $i.level > 50
      if $i.level < 75
         if rand(5)
            say Maybe you should stick to being a dumb meat shield.  Oh wait...
            laugh $n
            if rand(25)
               cast 'paralyze' $n
               else
               cast 'hellstream' $n
               endif
            endif
         endif
      endif
   endif
endif
~
>act_prog p tries to headbutt you, but fails miserably.~
if isfight($i)
if ispc($n)
   if $i.level > 89
      if rand(15)
         say Tsk, all brawn and no brains.
         if rand(75)
            cast 'hellstream' $n
            else
            cast 'meteor swarm' $n
            endif
         endif
      endif
   if $i.level > 74
      if $i.level < 90
         if rand(10)
            say Tsk, all brawn and no brains.
            if rand(50)
               cast 'hellstream' $n
               else
               cast 'meteor swarm' $n
               endif
            endif
         endif
      endif
   if $i.level > 50
      if $i.level < 75
         if rand(5)
            say Tsk, all brawn and no brains.
            if rand(25)
               cast 'hellstream' $n
               else
               cast 'meteor swarm' $n
               endif
            endif
         endif
      endif
   endif
endif
~
>act_prog p You are SHOCKED~
if isfight($i)
if ispc($n)
   if $i.level > 89
      if rand(15)
         say OUCH, damnit!  I'm pretty sure I heard your peanut brain rattling around with that headbutt.
         if rand(25)
            cast 'meteor swarm' $n
            else
            cast 'hellstream' $n
            endif
         endif
      endif
   if $i.level > 74
      if $i.level < 90
         if rand(10)
            say OUCH, damnit!  I'm pretty sure I heard your peanut brain rattling around with that headbutt.
            if rand(50)
               cast 'meteor swarm' $n
               else
               cast 'hellstream' $n
               endif
            endif
         endif
      endif
   if $i.level > 50
       if $i.level < 75
         if rand(5)
            say OUCH, damnit!  I'm pretty sure I heard your peanut brain rattling around with that headbutt.
            if rand(75)
               cast 'meteor swarm' $n
               else
               cast 'hellstream' $n
               endif
            endif
         endif
      endif
   endif
endif
~
>act_prog p starts swinging~
if isfight($i)
if ispc($n)
   if $i.level > 89
      if rand(15)
         say Tsk, like a bull in a china shop.
         cast 'hellstream' $n
         endif
      endif
   if $i.level > 74
      if $i.level < 90
         if rand(10)
            say Tsk, like a bull in a china shop.
            cast 'hellstream' $n
            endif
         endif
      endif
   if $i.level > 50
      if $i.level < 75
         if rand(5)
            say Tsk, like a bull in a china shop.
            cast 'hellstream' $n
            endif
         endif
      endif
   endif
endif
~
>act_prog p starts FOAMING at the mouth, and goes BERSERK on you!~
if isfight($i)
if ispc($n)
   if $i.level > 89
      if hitprcnt($i) < 30
         say RABIES!
         if rand(20)
            MPTELEPORT $i area
            else
            cast 'hellstream' $n
            endif
         endif
      endif
   if $i.level > 74
      if $i.level < 90
         if hitprcnt($i) < 25
            say RABIES!
            if rand(15)
               MPTELEPORT $i area
               else
               cast 'hellstream' $n
               endif
            endif
         endif
      endif
   if $i.level > 50
      if $i.level < 75
         if hitprcnt($i) < 20
            say RABIES!
            if rand(10)
               MPTELEPORT $i area
               else
               cast 'hellstream' $n
               endif
            endif
         endif
      endif
   endif
endif
~
>act_prog p starts advancing toward you, but trips over~
if isfight($i)
if ispc($n)
   if $i.level > 89
      if rand(15)
         say Never miss an opportunity to kick a dirty ape when it's down.
         cast 'hellstream' $n
         endif
      endif
   if $i.level > 74
      if $i.level < 90
         if rand(10)
            say Never miss an opportunity to kick a dirty ape when it's down.
            if rand(25)
               cast 'meteor swarm' $n
               else
               cast 'hellstream' $n
               endif
            endif
         endif
      endif
   if $i.level > 50
      if $i.level < 75
         if rand(5)
            say Never miss an opportunity to kick a dirty ape when it's down.
            if rand(50)
               cast 'hellstream' $n
               else
               cast 'meteor swarm' $n
               endif
            endif
         endif
      endif
   endif
endif
~
>act_prog p advances confidently towards you, and flies into a rage!~
if isfight($i)
if ispc($n)
   if $i.level > 89
      if rand(15)
         say Damn, dirty ape.
         cast 'hellstream' $n
         endif
      endif
   if $i.level > 74
      if $i.level < 90
         if rand(10)
            if rand(25)
               cast 'meteor swarm' $n
               else
               cast 'hellstream' $n
               endif
            endif
         endif
      endif
   if $i.level > 50
      if $i.level < 75
         if rand(5)
            if rand(50)
               cast 'hellstream' $n
               else
               cast 'meteor swarm' $n
               endif
            endif
         endif
      endif
   endif
endif
~
>act_prog p utters the words, 'saugai'~
if isfight($i)
if ispc($n)
   if $i.level > 89
      if rand(15)
         say Damn, dirty hippy!
         if rand(50)
            cast 'hellstream' $n
            else
            cast 'meteor swarm' $n
            endif
         endif
      endif
  if $i.level > 74
      if $i.level < 90
         if rand(10)
            say Damn, dirty hippy!
            if rand(50)
               cast 'hellstream' $n
               else
               cast 'meteor swarm' $n
               endif
            endif
         endif
      endif
 if $i.level > 50
      if $i.level < 75
         if rand(5)
            say Damn, dirty hippy!
            if rand(50)
               cast 'hellstream' $n
               else
               cast 'meteor swarm' $n
               endif
            endif
         endif
      endif
   endif
endif
~
>act_prog p utters the words, 'qfzzsuio oculoahp'~
if isfight($i)
if ispc($n)
   if $i.level > 89
      if rand(15)
         say Who needs RAID when you've got hellstream?
         cast 'hellstream' $n
         endif
      endif
   if $i.level > 74
      if $i.level < 90
         if rand(10)
            say Who needs RAID when you've got hellstream?
            cast 'hellstream' $n
            endif
         endif
      endif
   if $i.level > 50
      if $i.level < 75
         if rand(5)
            say Who needs RAID when you've got hellstream?
            cast 'hellstream' $n
            endif
         endif
      endif
   endif
endif
~
>act_prog p disarms you and sends your weapon flying!~
if isfight($i)
if ispc($n)
   if $i.level > 89
      if rand(15)
         say Oh, like I can't whip your ass with my bare hands.
         cast hellstream $n
         endif
      endif
   if $i.level > 74
      if $i.level < 90
         if rand(10)
            say Oh, like I can't whip your ass with my bare hands.
            cast 'hellstream' $n
            endif
         endif
      endif
   if $i.level > 50
      if $i.level < 75
         if rand(5)
            say Oh, like I can't whip your ass with my bare hands.
            cast 'hellstream' $n
            endif
         endif
      endif
   endif
endif
~
>act_prog p You dodge a bash from~
if isfight($i)
if ispc($n)
   if $i.level > 89
      if rand(20)
         say Never miss an opportunity to kick someone when they're down!
         cast 'hellstream' $n
         endif
      endif
   if $i.level > 74
      if $i.level < 90
         if rand(15)
            say Never miss an opportunity to kick someone when theyr'e down!
            cast 'hellstream' $n
            endif
        endif
      endif
   if $i.level > 50
      if $i.level < 75
         if rand(10)
            say Never miss an opportunity to kick someone when they're down!
            cast 'hellstream' $n
            endif
         endif
      endif
   endif
endif
~
>act_prog p sends you sprawling.~
if isfight($i)
if ispc($n)
   if $i.level > 89
      if rand(15)
         say You dare sully my greatness?!
         cast 'hellstream' $n
         endif
      endif
   if $i.level > 74
      if $i.level < 90
         if rand(10)
            say You dare sully my greatness?!
            if rand(75)
               cast 'hellstream' $n
               else
               cast 'meteor swarm' $n
               endif
            endif
         endif
      endif
   if $i.level > 50
      if $i.level < 75
         if rand(5)
            say You dare sully my greatness?!
            if rand(50)
               cast 'hellstream' $n
               else
               cast 'meteor swarm' $n
               endif
            endif
         endif
      endif
   endif
endif
~
>act_prog p utters the words, 'euzuiz yjfl'~
if isfight($i)
if ispc($n)
   if $i.level > 89
      if rand(15)
         snarl $n
         cast 'hellstream' $n
         endif
      endif
   if $i.level > 74
      if $i.level < 90
         if rand(10)
            snarl $n
            cast 'hellstream' $n
            endif
         endif
      endif
   if $i.level > 50
      if $i.level < 75
         if rand(5)
            snarl $n
            if rand(50)
               cast 'hellstream' $n
               else
               cast 'meteor swarm' $n
               endif
            endif
         endif
      endif
   endif
endif
~
>act_prog p utters the words, 'solar gate'~
if isfight($i)
if ispc($n)
   if $i.level > 89
      if rand(15)
         say Do you really think you can outshine my power!?
         if rand(50)
            cast 'solar gate'
            else
            cast 'hellstream' $n
            endif
         endif
      endif
   if $i.level > 74
      if $i.level < 90
         if rand(10)
            say Do you really think you can outshine my power!?
            if rand(50)
               cast 'solar gate'
               else
               cast 'hellstream' $n
               endif
            endif
         endif
      endif
   if $i.level > 50
      if $i.level < 75
         if rand(5)
            say Do you really think you can outshine my power!?
            if rand(50)
               cast 'solar gate'
               else
               cast 'hellstream' $n
               endif
            endif
         endif
      endif
   endif
endif
~
>act_prog p utters the words, 'meteor swarm'~
if isfight($i)
if ispc($n)
   if $i.level > 89
      if rand(15)
         say Like a few little pebbles are going to hurt me!
         if rand(50)
            cast 'hellstream' $n
            else
            cast 'meteor swarm' $n
            endif
         endif
      endif
   if $i.level > 74
      if $i.level < 90
         if rand(10)
            say Like a few little pebbles are going to hurt me!
            if rand(33)
               cast 'hellstream' $n
               else
               if rand(50)
                  cast 'meteor swarm' $n
                  else
                  cast 'fireball' $n
                  endif
               endif
            endif
         endif
      endif
   if $i.level > 50
      if $i.level < 75
         if rand(5)
            say Like a few little pebbles are going to hurt me!
            if rand(20)
               cast 'hellstream' $n
               else
               if rand(50)
                  cast 'meteor swarm' $n
                  else
                  cast 'fireball' $n
                  endif
               endif
            endif
         endif
      endif
   endif
endif
~
>act_prog p utters the words, 'firestorm'~
if isfight($i)
if rand($n)
   if $i.level > 89
      if rand(15)
         say Your fire does not burn bright enough.  Let me turn up the heat!
         if rand(50)
            cast 'hellstream' $n
            else
            cast 'firestorm'
            endif
         endif
      endif
   if $i.level > 74
      if $i.level < 90
         if rand(10)
            say Your fire does not burn bright enough.  Let me turn up the heat!
            if rand(33)
               cast 'hellstream' $n
               else
               if rand(50)
                  cast 'firestorm'
                  else
                  cast 'fireball' $n
                  endif
               endif
            endif
         endif
      endif
   if $i.level > 50
      if $i.level < 75
         if rand(5)
            say Your fire does not burn bright enough.  Let me turn up the heat!
            if rand(15)
               cast 'hellstream' $n
               else
               if rand(50)
                  cast 'firestorm'
                  else
                  cast 'fireball' $n
                  endif
               endif
            endif
         endif
      endif
   endif
endif
~
>act_prog p utters the words, 'paralyze'~
if isfight($i)
if ispc($n)
   if $i.level > 89
      if rand(15)
         say You rat bastard!
         if rand(5)
            cast 'paralyze' $n
            else
            cast 'hellstream' $n
            endif
         endif
      endif
   if $i.level > 74
      if $i.level < 90
         if rand(10)
            say You rat bastard!
            if rand(5)
               cast 'paralyze' $n
               else
               cast 'hellstream' $n
               endif
            endif
         endif
      endif
   if $i.level > 50
      if $i.level < 75
         if rand(5)
            say You rat bastard!
            if rand(5)
               cast 'paralyze' $n
               else
               cast 'hellstream' $n
               endif
            endif
         endif
     endif
   endif
endif
~
>act_prog p utters the words, 'hellstream'~
if isfight($i)
if ispc($n)
   if $i.level > 89
      if rand(15)
         say Your flames dont burn hot enough.  Let me show you how it's done!
         cast 'hellstream' $n
         endif
      endif
   if $i.level > 74
      if $i.level < 90
         if rand(10)
            say Your flames dont burn hot enough.  Let me show you how it's done!
            if rand(33)
               cast 'firebal'l $n
               else
               cast 'hellstream' $n
               endif
            endif
         endif
      endif
   if $i.level > 50
      if $i.level < 75
         if rand(5)
            say Your flames dont burn hot enough.  Let me show you how it's done!
            if rand(50)
               cast 'fireball' $n
               else
               cast 'hellstream' $n
               endif
            endif
         endif
      endif
   endif
endif
~
>act_prog p utters the words, 'eugszr~
if isfight($i)
if ispc($n)
   if $i.level > 89
      if rand(15)
         laugh $n
         if rand(33)
            cast 'dispel magic' $n
            else
            if rand(50)
               cast 'hellstream' $n
               else
               cast 'meteor swarm' $n
               endif
            endif
         endif
      endif
   if $i.level > 74
      if $i.level < 90
         if rand(10)
            laugh $n
            if rand(33)
               cast 'dispel magic' $n
               else
               if rand(50)
                  cast 'hellstream' $n
                  else
                  cast 'meteor swarm' $n
                  endif
               endif
            endif
         endif
      endif
   if $i.level > 50
      if $i.level < 75
         if rand(5)
            laugh $n
            if rand(33)
               cast 'dispel magic' $n
               else
               if rand(50)
                  cast 'hellstream' $n
                  else
                  cast 'meteor swarm' $n
                  endif
               endif
            endif
         endif
      endif
   endif
endif
~
>act_prog p utters the words, 'saxf pandoaw'~
if isfight($i)
if ispc($n)
   if $i.level > 89
      if rand(15)
         say Let me show you some real pain!
         cast 'hellstream' $n
         endif
      endif
   if $i.level > 74
      if $i.level < 90
         if rand(10)
            say Let me show you some real pain!
            if rand(50)
               cast 'hellstream' $n
               else
               cast 'meteor swarm' $n
               endif
            endif
         endif
      endif
   if $i.level > 50
      if $i.level < 75
         if rand(5)
            say Let me show you some real pain!
            if rand(20)
               cast 'hellstream' $n
               else
               if rand(50)
                  cast 'meteor swarm' $n
                  else
                  cast 'fireball' $n
                  endif
               endif
            endif
         endif
      endif
   endif
endif
~
>act_prog p utters the words, 'yrawzghfutz'~
if isfight($i)
if ispc($n)
   if $i.level > 89
      if rand(15)
         say That's not a fire.  This is a fire!
         cast 'hellstream' $n
         endif
      endif
   if $i.level > 74
      if $i.level > 90
         if rand(10)
            say That's not a fire.  This is a fire!
            if rand(50)
               cast 'hellstream' $n
               else
               cast 'fireball' $n
               endif
            endif
          endif
       endif
    if $i.level > 50
       if $i.level > 75
       if rand(5)
          say That's not a fire.  This is a fire!
          if rand(33)
             cast 'hellstream' $n
             else
             cast 'fireball' $n
             endif
          endif
       endif
    endif
endif
endif
~
>act_prog p utters the words, 'pzar gsfal'~
if isfight($i)
if ispc($n)
   if $i.level > 89
      if rand(15)
         say Pray to your gods some more!
         if rand(25)
            cast 'hellstream' $n
            else
            if rand(33)
               cast 'meteor swarm' $n
               else
               if rand(50)
                  cast 'solar gate'
                  else
                  cast 'firestorm'
                  endif
               endif
            endif
         endif
      endif
   if $i.level > 74
      if $i.level < 90
         if rand(10)
            say Pray to your gods some more!
            if rand(25)
               cast 'hellstream' $n
               else
               if rand(33)
                  cast 'meteor swarm' $n
                  else
                  if rand(50)
                     cast 'solar gate'
                     else
                     cast 'firestorm'
                     endif
                  endif
               endif
            endif
         endif
      endif
   if $i.level > 50
      if $i.level < 75
         if rand(5)
            say Pray to your gods some more!
            if rand(25)
               cast 'hellstream' $n
               else
               if rand(33)
                  cast 'meteor swarm' $n
                  else
                  if rand(50)
                     cast 'solar gate'
                     else
                     cast 'firestorm'
                     endif
                  endif
               endif
            endif
         endif
      endif
   endif
endif
~
>fight_prog 25~
if isspelled($n) sanctuary
  cast 'dispel magic'
else
  if isspelled($n) fireshield
    cast 'dispel magic'
  else
    if isspelled($n) stoneshield
      cast 'dispel magic'
    else
      if isspelled($n) lightning_shield
        cast 'dispel magic'
      else
        if isspelled($n) acid_shield
          cast 'dispel magic'
        endif
      endif
    endif
  endif
endif
~
>fight_prog 15~
if numpcs() > 4
    if ispc($r)
        if $i.level > 89
             if rand(50)
                if rand(75)
                    cast 'hellstream' $r
                    else
                    cast 'meteor swarm' $r
                    endif
                else
                if rand(75)
                    cast 'solar gate'
                    else
                    cast 'firestorm'
                    endif
                endif
            endif
        if $i.level > 74
            if $i.level < 90
                if rand(25)
                   cast 'hellstream' $r
                   else
                   if rand(50)
                       cast 'firestorm'
                       else
                       cast 'solar gate'
                       endif
                   endif
               endif
            endif
        if $i.level > 50
            if $i.level < 74
                if rand(75)
                   if rand(30)
                       cast 'hellstream' $r
                       else
                       cast 'meteor swarm' $r
                       endif
                   else
                   if rand(25)
                       cast 'solar gate'
                       else
                       cast 'firestorm'
                      endif
                   endif
               endif
           endif
        endif
    else
    if $i.level > 89
        if rand(75)
            cast 'hellstream' $n
            else
            cast 'meteor swarm' $n
            endif
        endif
    if $i.level > 74
        if $i.level < 90
           if rand(50)
               cast 'hellstream' $n
               else
               cast 'meteor swarm' $n
               endif
           endif
        endif
    if $i.level > 50
        if $i.level < 75
           if rand(25)
               cast 'hellstream' $n
               else
               cast 'meteor swarm' $n
               endif
           endif
       endif
    endif
~
>fight_prog 10~
if numpcs() > 4
    if ispc($r)
        if $i.level > 89
            if rand(20)
                if rand(50)
                    cast 'firestorm'
                    else
                    cast 'solar gate'
                    endif
                else
                if rand(20)
                    cast 'magic missile' $r
                    else
                    if rand(25)
                        cast 'burning hands' $r
                        else
                        if rand(33)
                            cast 'lightning bolt' $r
                            else
                            if rand(50)
                                cast 'chill touch' $r
                                else
                                cast 'fireball' $r
                                endif
                            endif
                        endif
                    endif
                endif
            endif
        if $i.level > 74
            if $i.level < 90
                if rand(15)
                    if rand(50)
                        cast 'firestorm'
                        else
                        cast 'solar gate'
                        endif
                    else
                    if rand(20)
                        cast 'magic missile' $r
                        else
                        if rand(25)
                            cast 'burning hands' $r
                            else
                            if rand(33)
                                cast 'lightning bolt' $r
                                else
                                if rand(50)
                                    cast 'chill touch' $r
                                    else
                                    cast 'fireball' $r
                                    endif
                                endif
                            endif
                        endif
                    endif
                endif
            endif
        if $i.level > 50
            if $i.level < 75
                if rand(10)
                    if rand(50)
                        cast 'firestorm'
                        else
                        cast 'solar gate'
                        endif
                    if rand(20)
                        cast 'magic missile' $r
                        else
                        if rand(25)
                            cast 'burning hands' $r
                            else
                            if rand(33)
                                cast 'lightning bolt' $r
                                else
                                if rand(50)
                                    cast 'chill touch' $r
                                    else
                                    cast 'fireball' $r
                                    endif
                                endif
                            endif
                        endif
                    endif
                endif
            endif
        endif
        else
        if rand(20)
            cast 'magic missile' $n
            else
            if rand(33)
                cast 'burning hands' $n
                else
                if rand(50)
                    cast 'chill touch' $n
                    else
                    cast 'fireball' $n
                    endif
                endif
            endif
    endif
~
>fight_prog 3~
if numpcs() > 4
    if ispc($r)
        if $i.level > 89
            if rand(50)
                if rand(75)
                    cast 'solar gate'
                    else
                    cast 'fire storm'
                    endif
                else
                if rand(50)
                    cast 'fear' $r
                    else
                    cast 'paralyze' $r
                    endif
                endif
            endif
        if $i.level > 74
            if $i.level < 90
                if rand(50)
                    if rand(50)
                        cast 'solar gate'
                        else
                        cast 'firestorm'
                        endif
                    else
                    if rand(50)
                        cast 'paralyze' $r
                        else
                        cast 'fear' $r
                        endif
                    endif
                endif
            endif
        if $i.level > 50
            if $i.level < 75
                if rand(50)
                    if rand(25)
                        cast 'solar gate'
                        else
                        cast 'firestorm'
                        endif
                    else
                    if rand(50)
                        cast 'fear' $r
                        else
                        cast 'paralyze' $r
                        endif
                    endif
                endif
            endif
        endif
    else
    if $i.level > 89
        if rand(50)
            cast 'paralyze' $n
            else
            cast 'fear' $n
            endif
        endif
    if $i.level > 74
        if $i.level < 90
            if rand(50)
                if rand(50)
                    cast 'paralyze' $n
                    else
                    cast 'fear' $n
                    endif
                endif
            endif
        endif
    if $i.level > 50
        if $i.level < 75
            if rand(25)
                if rand(50)
                    cast 'paralyze' $n
                    else
                    cast 'fear' $n
                    endif
                endif
           endif
        endif
    endif
~
>load_prog 100~
cast 'clarity'
cast 'armor'
cast 'infravision'
if $i.level  < 75
   if $i.level > 51
       if rand(10)
           cast 'fly'
           endif
      endif
   endif
if $i.level > 74
    if rand(20)
      cast 'fly'
      endif
   endif
cast 'strength'
cast 'shield'
cast 'resist magic'
cast 'haste'
cast 'true sight'
cast 'resist fire'
cast 'stoneskin'
if $i.level < 60
   if $i.level > 51
       if rand(3)
           cast 'fireshield'
           endif
      endif
   endif
if $i.level < 70
    if $i.level > 59
        if rand(6)
            cast 'fireshield'
            endif
      endif
   endif
if $i.level < 80
    if $i.level > 69
        if rand(9)
           cast 'fireshield'
           endif
      endif
   endif
if $i.level < 90
    if $i.level > 79
        if rand(12)
            cast 'fireshield'
           endif
      endif
   endif
if $i.level < 100
    if $i.level > 89
        if rand(15)
           cast 'fireshield'
           endif
      endif
   endif
if $i.level > 99
   if rand(21)
      cast 'fireshield'
      endif
   endif
~
>arand_prog 1~
if isspelled($i) armor
   else
   cast 'armor'
   endif
if isspelled($i) strength
   else
   cast 'strength'
   endif
if isspelled($i) shield
   else
   cast 'shield'
   endif
if isspelled($i) haste
   else
   cast 'haste'
   endif
if isspelled($i) resist_fire
   else
   cast 'resist fire'
   endif
if isspelled($i) stoneskin
   else
   cast 'stoneskin'
   endif
if isspelled($i) fireshield
   else
   if rand(75)
      cast 'fireshield'
      endif
   endif
if isspelled($i) clarity
   else
   cast 'clarity'
   endif
if isspelled($i) true_sight
   else
   cast 'true sight'
   endif
if isspelled($i) resist_magic
   else
   cast 'resist magic'
   endif
~
|
S
#105
skillset mob~
PROC MOB CLERIC LEVEL 1-20~
This is a Skillset Mob for the Cleric Class, levels 1-20.
~
~
256 0 -1
2097192 0 0 -1
0 0 0
20 10 0d1+0 1d1+0
0 0
8 8 0 0 0 0
>load_prog 100~
cast 'armor' $i
cast 'bless' $i
cast 'sanctuary' $i
~
>fight_prog 15~
cast 'cause critic' $n
~
>fight_prog 5~
cast 'cause serious' $n
~
>hitprcnt_prog 50~
if rand(25)
  cast 'cure serious' $i
endif
~
>arand_prog 1~
if isspelled($i) armor
  else
    cast 'armor' $i
  break
endif
if isspelled($i) bless
  else
    cast 'bless' $i
  break
endif
if isspelled($i) sanctuary
  else
    cast 'sanctuary' $i
  break
endif
~
|
S
#106
skillset mob~
PROC MOB CLERIC LEVEL 21-35~
This is a Skillset Mob for the Mage Class, levels 21-35.
~
~
256 0 -1
2097192 0 0 -1
0 0 0
20 10 0d1+0 1d1+0
0 0
8 8 0 0 0 0
>load_prog 100~
cast 'armor' $i
cast 'bless' $i
cast 'sanctuary' $i
cast 'resist cold' $i
~
>fight_prog 10~
cast 'cause critic' $n
~
>fight_prog 80~
say In the name of the Creator, you must be destroyed!
cast 'harm' $n
~
>hitprcnt_prog 50~
if rand(25)
  cast 'cure critic' $i
endif
~
>arand_prog 1~
if isspelled($i) armor
  else
    cast 'armor' $i
  break
endif
if isspelled($i) bless
  else
    cast 'bless' $i
  break
endif
if isspelled($i) sanctuary
  else
    cast 'sanctuary' $i
  break
endif
if isspelled($i) resist_cold
  else
    cast 'resist cold' $i
  break
endif
~
|
S
#107
skillset mob~
PROC MOB CLERIC LEVEL 36-50~
This is a Skillset Mob for the Cleric Class, levels 36-50.
~
~
256 0 -1
2359336 0 0 -1
0 0 0
20 10 1d1+0 1d1+0
0 0
8 8 0 0 0 0
>rand_prog 100~
if isfight($r)
  if isnpc($r)
    cast 'full heal' $r
  endif
endif
~
>load_prog 100~
cast 'armor' $i
cast 'bless' $i
cast 'sanctuary' $i
cast 'resist cold' $i
cast 'resist energy' $i
~
>fight_prog 10~
if tempvar($i[israiny]) == true
  cast 'call lightning'
  break
else
  if tempvar($i[issunny]) == true
    cast 'power harm'
    break
  endif
endif
~
>act_prog cloudless~
MPSETTEMP $i issunny true
MPSETTEMP $i israiny false
~
>act_prog cloudy~
MPSETTEMP $i issunny true
MPSETTEMP $i israiny false
~
>act_prog flashes~
MPSETTEMP $i israiny true
MPSETTEMP $i issunny false
~
>act_prog rainy~
MPSETTEMP $i israiny true
MPSETTEMP $i issunny false
~
>fight_prog 50~
weather
~
>fight_prog 15~
MPECHOAT $n $I calls upon the wrath of $l god to strike you down.
MPECHOAROUND $n $I calls upon the wrath of $l god to strike down $n.
cast 'power harm'
~
>fight_prog 10~
say In the name of the Creator, you must be destroyed!
cast 'harm'
~
>fight_prog 25~
if isspelled($n) sanctuary
    cast 'dispel magic'
    else
     if isspelled($n) fireshield
         cast 'dispel magic'
         else
         if isspelled($n) stoneshield
            cast 'dispel magic'
            else
            if isspelled($n) lightning_shield
               cast 'dispel magic'
               else
               if isspelled($n) acid_shield
                   cast 'dispel magic'
                   endif
               endif
           endif
       endif
   endif
~
>hitprcnt_prog 50~
if rand(25)
  cast 'heal' $i
endif
~
>arand_prog 1~
if isspelled($i) armor
  else
    cast 'armor' $i
  break
endif
if isspelled($i) bless
  else
    cast 'bless' $i
  break
endif
if isspelled($i) sanctuary
  else
    cast 'sanctuary' $i
  break
endif
if isspelled($i) resist_cold
  else
    cast 'resist cold' $i
  break
endif
if isspelled($i) resist_energy
  else
    cast 'resist energy' $i
  break
endif
~
|
S
#108
skillset mob~
PROC MOB CLERIC LEVEL 51+~
This is a Skillset Mob for the Cleric Class, levels 51+.
~
~
256 0 -1
2359336 0 0 -1
0 0 0
20 10 1d1+0 1d1+0
0 0
8 8 0 0 0 0
>damage_prog 350~
if $i.level > 89
   if rand(30)
      say Oh Holy Lord, protect thy servant!
      cast 'full heal'
   endif
endif
if $i.level > 74
   if $i.level < 90
      if rand(25)
         say Oh Holy Lord, protect thy servant!
         cast 'full heal'
      endif
   endif
endif
if $i.level > 50
   if $i.level < 75
      if rand(20)
         say Oh Holy Lord, protect thy servant!
         cast 'full heal'
      endif
   endif
endif
~
>hitprcnt_prog 50~
if $i.level > 89
   if rand(30)
      cast 'full heal'
   endif
endif
if $i.level > 74
   if $i.level < 90
      if rand(25)
         cast 'full heal'
      endif
   endif
endif
if $i.level > 50
   if $i.level < 75
      if rand(20)
         cast 'full heal'
      endif
   endif
endif
~
>fight_prog 50~
weather
~
>fight_prog 25~
if isspelled($n) sanctuary
    cast 'dispel magic'
else
  if isspelled($n) fireshield
    cast 'dispel magic'
else
  if isspelled($n) stoneshield
    cast 'dispel magic'
else
  if isspelled($n) lightning_shield
    cast 'dispel magic'
else
  if isspelled($n) acid_shield
    cast 'dispel magic'
endif
endif
endif
endif
endif
~
>fight_prog 10~
if rand(50)
   cast 'power harm' $n
   else
   cast 'flamestrike' $n
endif
~
>fight_prog 10~
if $i.alignment < 350
   if $n.alignment > 349
      cast 'dispel good' $n
   endif
endif
if $i.alignment > -350
   if $n.alignment < -351
      cast 'dispel evil' $n
   endif
endif
if $n.alignment > -351
   if $n.alignment < 350
      if rand(50)
         cast 'power harm' $n
         else
         cast 'flamestrike' $n
      endif
   endif
endif
~
>rand_prog 5~
if isfight($r)
    if isnpc($r)
        if isfollow($r)
            break
            else
            cast 'full heal' $r
            endif
         endif
   endif
~
>load_prog 100~
cast 'armor'
cast 'bless'
cast 'detect invisibility'
cast 'sense life'
cast 'sanctuary'
cast 'iridescent aura'
if isgood($i)
  mpat 22 say Being Evil, I best protect from the GOOD!
  cast 'protection from evil'
endif
if isevil($i)
  mpat 22 say Being Good, I best protect from the EVIL!
  cast 'protection from good'
endif
if isneutral($i)
  mpat 22 say I am neutral, no spell to cast!
endif
cast 'true sight'
cast 'resist magic'
cast 'resist energy'
cast 'divine intervention'
~
>fight_prog 10~
if tempvar($i[israiny]) == true
   if rand(33)
       cast 'call lightning' $n
       else
       if rand(50)
           cast 'power harm' $n
           else
           cast 'flamestrike' $n
           endif
       endif
   endif
if tempvar($i[issunny]) == true
    if rand(50)
        cast 'power harm' $n
        else
        cast 'flamestrike' $n
        endif
    endif
~
>act_prog cloudless~
MPSETTEMP $i issunny true
MPSETTEMP $i israiny false
~
>act_prog cloudy~
MPSETTEMP $i issunny true
MPSETTEMP $i israiny false
~
>act_prog flashes~
MPSETTEMP $i israiny true
MPSETTEMP $i issunny false
~
>act_prog rainy~
MPSETTEMP $i israiny true
MPSETTEMP $i issunny false
~
>hitprcnt_prog 25~
if isfight($i)
if $i.level > 89
   if rand(15)
      flee
      if isspelled($i) sanctuary
         cast 'full heal'
         else
         cast 'sanctuary'
         cast 'full heal'
         endif
     endif
   endif
if $i.level > 74
   if $i.level < 90
      if rand(10)
         flee
         if isspelled($i) sanctuary
            cast 'full heal'
            else
            cast 'sanctuary'
            cast 'full heal'
            endif
         endif
      endif
   endif
if $i.level > 50
   if $i.level < 75
      if rand(5)
         flee
         if isspelled($i) sanctuary
            cast 'full heal'
            else
            cast 'sanctuary'
            cast 'full heal'
            endif
         endif
      endif
   endif
endif
~
>arand_prog 1~
if isspelled($i) armor
  else
    cast 'armor'
  break
endif
if isspelled($i) bless
  else
    cast 'bless'
  break
endif
if isspelled($i) sanctuary
  else
    cast 'sanctuary'
  break
endif
if isspelled($i) iridescent_aura
  else
    cast 'iridescent aura'
  break
endif
if isspelled($i) resist_magic
  else
    cast 'resist magic'
  break
endif
if isspelled($i) resist_energy
  else
    cast 'resist energy'
  break
endif
if isspelled($i) detect_invisibility
  else
    cast 'detect invisibility'
  break
endif
if isspelled($i) sense_life
  else
    cast 'sense life'
  break
endif
if isspelled($i) true_sight
  else
    cast 'true sight'
  break
endif
if isspelled($i) divine_intervention
  else
    cast 'divine intervention'
  break
endif
~
|
S
#109
skillset mob~
PROC MOB WARRIOR LEVEL 1-20~
This is a Skillset Mob for the Warrior Class, levels 1-20.
~
~
525056 0 -1
2097152 0 0 -1
0 0 0
20 10 0d1+0 1d1+0
0 0
8 8 0 0 0 0
>fight_prog 10~
bash
~
>fight_prog 10~
kick
~
|
S
#110
skillset mob~
PROC MOB WARRIOR LEVEL 21-35~
This is a Skillset Mob for the Warrior Class, levels 21-35.
~
~
525056 0 -1
2097152 0 0 -1
0 0 0
20 10 1d1+0 1d1+0
0 0
8 8 0 0 0 0
>fight_prog 10~
disarm
~
>fight_prog 10~
bash
~
>fight_prog 10~
kick
~
|
S
#111
skillset mob~
PROC MOB WARRIOR LEVEL 36-50~
This is a Skillset Mob for the Warrior Class, levels 36-50.
~
~
526080 0 -1
2097152 0 0 -1
0 0 0
20 10 1d1+0 1d1+0
0 0
8 8 0 0 0 0
>fight_prog 15~
if level($i) > 45
  if level($i) < 51
    MPECHOAT $n You scream in pain from $I's deathstroke to your kidneys!   ARRRGGGGHHHHH!
    MPECHOAROUND $n $I deathstrokes $n in $s kidneys!  $n cries out in pain!
    MPDAMAGE $n 10d20 undefined
    break
  endif
endif
~
>fight_prog 15~
if level($i) > 39
  if level($i) < 46
    MPECHOAT $n You scream in pain from $I's deathstroke to your kidneys!   ARRRGGGGHHHHH!
    MPECHOAROUND $n $I deathstrokes $n in $s kidneys!  $n cries out in pain!
    MPDAMAGE $n 10d10 undefined
    break
  endif
endif
~
>fight_prog 10~
disarm
~
>fight_prog 10~
bash
~
>fight_prog 5~
kick
~
|
S
#112
skillset mob~
PROC MOB WARRIOR LEVEL 51+~
This is a Skillset Mob for the Warrior Class, levesl 51+.
~
~
526080 0 -1
2097152 0 0 -1
0 0 0
20 10 1d1+0 1d1+0
0 0
8 8 0 0 0 0
>act_prog p utters the words, 'zawsufuq hajqp'~
if isfight($i)
   if ispc($n)
      if $i.level > 89
         if rand(15)
             say Oh, as if being the chosen class isn't enough, but a being weeny bloodsucker to boot!
             say That just takes the cake.
             if rand(50)
                 headbutt $n
                 else
                 redirect $n
                 endif
            endif
         endif
     if $i.level > 74
        if $i.level < 90
            if rand(10)
               say Oh, as if being the chosen class isn't enough, but a being weeny bloodsucker to boot!
               say That just takes the cake.
               if rand(50)
                   headbutt $n
                   else
                   redirect $n
                   endif
               endif
            endif
         endif
    if $i.level > 50
        if $i.level < 75
            if rand(5)
               say Oh, as if being the chosen class isn't enough, but a being weeny bloodsucker to boot!
               say That just takes the cake.
               if rand(50)
                   headbutt $n
                   else
                   redirect $n
                   endif
               endif
            endif
         endif
      endif
   endif
~
>act_prog p utters the words, 'aque bragh'~
if isfight($i)
   if ispc($n)
      if $i.level > 89
         if rand(15)
            say My armor!
            if rand(50)
               headbutt $n
               else
               redirect $n
               endif
            endif
         endif
     if $i.level > 74
        if $i.level < 90
            if rand(10)
               say My armor!
               if rand(50)
                  headbutt $n
                  else
                  redirect $n
                  endif
               endif
            endif
         endif
    if $i.level > 50
        if $i.level < 75
            if rand(5)
               say My armor!
               if rand(50)
                  headbutt $n
                  else
                  redirect $n
                  endif
               endif
            endif
         endif
      endif
   endif
~
>act_prog p vibrations make you suffer~
if ispc($n)
   if isfight($i)
       if $i.level > 89
           if rand(15)
              clap
              say Just like a Magic Fingers bed!
              hitall
              endif
           endif
      if $i.level > 74
          if $i.level < 90
              if rand(10)
                 clap
                 say Just like a Magic Fingers bed!
                 hitall
                 endif
               endif
          endif
      if $i.level > 50
          if $i.level < 75
              if rand(5)
                 clap
                 say Just like a Magic Fingers bed!
                 hitall
                 endif
              endif
          endif
      endif
  endif
~
>act_prog p slams a bolt of focused ki energy into the flow of magic around you!~
if isfight($i)
   if ispc($n)
      if $i.level > 89
         if rand(15)
            say Have at thee, cur!
            redirect $n
            endif
         endif
     if $i.level > 74
        if $i.level < 90
            if rand(10)
               say Have at thee, cur!
               redirect $n
               endif
            endif
         endif
    if $i.level > 50
        if $i.level < 75
            if rand(5)
               say Have at thee, cur!
               redirect $n
               endif
            endif
         endif
      endif
   endif
~
>act_prog p blasts you across the room, causing you to fall~
if ispc($n)
   if isfight($i)
       if $i.level > 89
           if rand(15)
              roar
              hitall
              endif
           endif
      if $i.level > 74
          if $i.level < 90
              if rand(10)
                 roar
                 hitall
                 endif
               endif
          endif
      if $i.level > 50
          if $i.level < 75
              if rand(5)
                 roar
                 hitall
                 endif
              endif
          endif
      endif
  endif
~
>act_prog p focuses and punches you square in the chest~
if isfight($i)
   if ispc($n)
      if $i.level > 89
         if rand(15)
            say Yeeeouch!
            redirect $n
            endif
         endif
     if $i.level > 74
        if $i.level < 90
            if rand(10)
               say Yeeeouch!
               redirect $n
               endif
            endif
         endif
    if $i.level > 50
        if $i.level < 75
            if rand(5)
               say Yeeeouch!
               redirect $n
               endif
            endif
         endif
      endif
   endif
~
>act_prog p sings a witty little limerick to you!~
if isfight($i)
   if ispc($n)
      if $i.level > 89
         if rand(15)
            say What's with all the American Idol rejects coming out of the woodwork?
            redirect $n
            endif
         endif
     if $i.level > 74
        if $i.level < 90
            if rand(10)
               say What's with all the American Idol rejects coming out of the woodwork?
               redirect $n
               endif
            endif
         endif
    if $i.level > 50
        if $i.level < 75
            if rand(5)
               say What's with all the American Idol rejects coming out of the woodwork?
               redirect $n
               endif
            endif
         endif
      endif
   endif
~
>act_prog p lashes out quickly and cripples you with a painful blow!~
if isfight($i)
   if ispc($n)
      if $i.level > 89
         if rand(15)
            say Two can play that game!
            if rand(50)
               headbutt $n
               else
               stun $n
               endif
            endif
         endif
     if $i.level > 74
        if $i.level < 90
            if rand(10)
               say Two can play that game!
               if rand(50)
                  headbutt $n
                  else
                  stun $n
                  endif
               endif
            endif
         endif
    if $i.level > 50
        if $i.level < 75
            if rand(5)
               say Two can play that game!
               if rand(50)
                  headbutt $n
                  else
                  stun $n
                  endif
               endif
            endif
         endif
      endif
   endif
~
>act_prog p with lethal accuracy and strength.~
if isfight($i)
   if ispc($n)
      if $i.level > 89
         if rand(15)
            grunt
            if rand(50)
               headbutt $n
               else
               stun $n
               endif
            endif
         endif
     if $i.level > 74
        if $i.level < 90
            if rand(10)
               grunt
               if rand(50)
                  headbutt $n
                  else
                  stun $n
                  endif
               endif
            endif
         endif
    if $i.level > 50
        if $i.level < 75
            if rand(5)
               grunt
               if rand(50)
                  headbutt $n
                  else
                  stun $n
                  endif
               endif
            endif
         endif
      endif
   endif
~
>act_prog p circles around~
if isfight($i)
   if ispc($n)
      if $i.level > 89
         if rand(15)
            say Quit hiding behind your betters and fight like a true warrior!
            redirect $n
            endif
         endif
     if $i.level > 74
        if $i.level < 90
            if rand(10)
               say Quit hiding behind your betters and fight like a true warrior!
               redirect $n
               endif
            endif
         endif
    if $i.level > 50
        if $i.level < 75
            if rand(5)
               say Quit hiding behind your betters and fight like a true warrior!
               redirect $n
               endif
            endif
         endif
      endif
   endif
~
>act_prog p utters the words, 'unmanifgz'~
if isfight($i)
   if ispc($n)
      if $i.level > 89
         if rand(15)
            snarl $n
            redirect $n
            endif
         endif
     if $i.level > 74
        if $i.level < 90
            if rand(10)
               snarl $n
               redirect $n
               endif
            endif
         endif
    if $i.level > 50
        if $i.level < 75
            if rand(5)
               snarl $n
               redirect $n
               endif
            endif
         endif
      endif
   endif
~
>act_prog p utters the words, 'ubralicra'~
 if ispc($n)
   if isfight($i)
       if $i.level > 89
           if rand(15)
              say Oh, like I need to see you to kick your arse!
              hitall
              endif
           endif
      if $i.level > 74
          if $i.level < 90
              if rand(10)
                 say Oh, like I need to see you to kick your arse!
                 hitall
                 endif
               endif
          endif
      if $i.level > 50
          if $i.level < 75
              if rand(5)
                 say Oh, like I need to see you to kick your arse!
                 hitall
                 endif
              endif
          endif
      endif
  endif
~
>act_prog p utters the words, 'qahijf gsfal'~
if isfight($i)
   if ispc($n)
      if $i.level > 89
         if rand(15)
            clap $n
            if rand(50)
               kick $n
               else
               headbutt $n
               endif
            endif
         endif
     if $i.level > 74
        if $i.level < 90
            if rand(10)
               clap $n
               if rand(50)
                  kick $n
                  else
                  headbutt $n
                  endif
               endif
            endif
         endif
    if $i.level > 50
        if $i.level < 75
            if rand(5)
               clap $n
               if rand(50)
                  kick $n
                  else
                  headbutt $n
                  endif
               endif
            endif
         endif
      endif
   endif
~
>act_prog p utters the words, 'qarr solilla'~
 if isfight($i)
   if ispc($n)
      if $i.level > 89
         if rand(15)
            say I've gotten a larger jolt sticking a 9-volt to my tongue.
            redirect $n
            endif
         endif
     if $i.level > 74
        if $i.level < 90
            if rand(10)
               say I've gotten a larger jolt sticking a 9-volt to my tongue.
               redirect $n
               endif
            endif
         endif
    if $i.level > 50
        if $i.level < 75
            if rand(5)
               say I've gotten a larger jolt sticking a 9-volt to my tongue.
               redirect $n
               endif
            endif
         endif
      endif
   endif
~
>act_prog p utters the words, 'gji brul'~
  if isfight($i)
   if ispc($n)
      if $i.level > 89
         if rand(15)
            say I've had sunburns that hurt more than that.
            redirect $n
            endif
         endif
     if $i.level > 74
        if $i.level < 90
            if rand(10)
               say I've had sunburns that hurt more than that.
               redirect $n
               endif
            endif
         endif
    if $i.level > 50
        if $i.level < 75
            if rand(5)
               say I've had sunburns that hurt more than that.
               redirect $n
               endif
            endif
         endif
      endif
   endif
~
>act_prog p utters the words, 'efaxi'~
if isfight($i)
   if ispc($n)
      if $i.level > 89
         if rand(15)
            say You'll find my armor doesn't rust easily.
            redirect $n
            endif
         endif
     if $i.level > 74
        if $i.level < 90
            if rand(10)
               say You'll find my armor doesn't rust easily.
               redirect $n
               endif
            endif
         endif
    if $i.level > 50
        if $i.level < 75
            if rand(5)
               say You'll find my armor doesn't rust easily.
               redirect $n
               endif
            endif
         endif
      endif
   endif
~
>act_prog p deathstroke and falls to the ground.~
if isfight($i)
   if ispc($n)
      if $i.level > 89
         if rand(15)
            say You need more practice.
            MPECHOAT $n You scream in pain from $I's deathstroke to your kidneys!  ARRRGGGGHHHHH!
            MPECHOAROUND $n $I deathstrokes $n in $s kidneys!  $n cries out in pain!
MPDAMAGE $n 1d1% undefined
            MPDAMAGE $n 10d50 undefined
            endif
         endif
      if $i.level > 74
         if $i.level < 90
            if rand(10)
               say You need more practice.
               MPECHOAT $n You scream in pain from $I's deathstroke to your kidneys!  ARRRGGGGHHHHH!
               MPECHOAROUND $n $I deathstrokes $n in $s kidneys!  $n cries out in pain!
MPDAMAGE $n 1d1% undefined
               MPDAMAGE $n 10d40 undefined
               endif
            endif
         endif
    if $i.level > 50
       if $i.level < 75
          if rand(5)
             say You need more practice.
             MPECHOAT $n You scream in pain from $I's deathstroke to your kidneys!  ARRRGGGGHHHHH!
              MPECHOAROUND $n $I deathstrokes $n in $s kidneys!  $n cries out in pain!
              MPDAMAGE $n 10d30 undefined
              endif
            endif
          endif
       endif
   endif
~
>act_prog p deathstroke to your kidneys!   AARRRGGGGHHHH!~
if isfight($i)
   if ispc($n)
      if $i.level > 89
         if rand(15)
            say Leave my kidneys alone!
            bash $n
            endif
         endif
     if $i.level > 74
        if $i.level < 90
            if rand(10)
               say Leave my kidneys alone!
               bash $n
               endif
            endif
         endif
    if $i.level > 50
        if $i.level < 75
            if rand(5)
               say Leave my kidneys alone!
               bash $n
               endif
            endif
         endif
      endif
   endif
~
>act_prog p attempts to hit you in your solar plexus!~
if isfight($i)
   if ispc($n)
      if $i.level > 89
         if rand(15)
            say You cheeky little monkey!
            stun $n
            endif
         endif
     if $i.level > 74
        if $i.level < 90
            if rand(10)
               say You cheeky little monkey!
               stun $n
               endif
            endif
         endif
    if $i.level > 50
        if $i.level < 75
            if rand(5)
               say You cheeky little monkey!
               stun $n
               endif
            endif
         endif
      endif
   endif
~
>act_prog p lunges forward in an attempt to smash into you but falls flat on~
if isfight($i)
   if ispc($n)
      if $i.level > 89
         if rand(15)
            laugh $n
            MPECHOAT $n You scream in pain from $I's deathstroke to your kidneys!  ARRRGGGGHHHHH!
            MPECHOAROUND $n $I deathstrokes $n in $s kidneys!  $n cries out in pain!
MPDAMAGE $n 1d1% undefined
            MPDAMAGE $n 10d50 undefined
            endif
         endif
      if $i.level > 74
         if $i.level < 90
            if rand(10)
               laugh $n
               MPECHOAT $n You scream in pain from $I's deathstroke to your kidneys!  ARRRGGGGHHHHH!
               MPECHOAROUND $n $I deathstrokes $n in $s kidneys!  $n cries out in pain!
MPDAMAGE $n 1d1% undefined
               MPDAMAGE $n 10d40 undefined
               endif
            endif
         endif
    if $i.level > 50
       if $i.level < 75
          if rand(5)
             laugh $n
             MPECHOAT $n You scream in pain from $I's deathstroke to your kidneys!  ARRRGGGGHHHHH!
              MPECHOAROUND $n $I deathstrokes $n in $s kidneys!  $n cries out in pain!
              MPDAMAGE $n 10d30 undefined
              endif
            endif
          endif
       endif
   endif
~
>act_prog p tries to headbutt you, but fails miserably.~
if isfight($i)
   if ispc($n)
      if $i.level > 89
         if rand(15)
            snarl $n
            kick $n
            endif
         endif
     if $i.level > 74
        if $i.level < 90
            if rand(10)
               snarl $n
               kick $n
               endif
            endif
         endif
    if $i.level > 50
        if $i.level < 75
            if rand(5)
               snarl $n
               kick $n
               endif
            endif
         endif
      endif
   endif
~
>act_prog p but trips~
if isfight($i)
   if ispc($n)
      if $i.level > 89
         if rand(15)
            say You need more practice.
            MPECHOAT $n You scream in pain from $I's deathstroke to your kidneys!  ARRRGGGGHHHHH!
            MPECHOAROUND $n $I deathstrokes $n in $s kidneys!  $n cries out in pain!
MPDAMAGE $n 1d1% undefined
            MPDAMAGE $n 10d50 undefined
            endif
         endif
      if $i.level > 74
         if $i.level < 90
            if rand(10)
               say You need more practice.
               MPECHOAT $n You scream in pain from $I's deathstroke to your kidneys!  ARRRGGGGHHHHH!
               MPECHOAROUND $n $I deathstrokes $n in $s kidneys!  $n cries out in pain!
MPDAMAGE $n 1d1% undefined
               MPDAMAGE $n 10d40 undefined
               endif
            endif
         endif
    if $i.level > 50
       if $i.level < 75
          if rand(5)
             say You need more practice.
             MPECHOAT $n You scream in pain from $I's deathstroke to your kidneys!  ARRRGGGGHHHHH!
              MPECHOAROUND $n $I deathstrokes $n in $s kidneys!  $n cries out in pain!
              MPDAMAGE $n 10d30 undefined
              endif
            endif
          endif
       endif
   endif
~
>act_prog p starts swinging like a MADMAN!~
if ispc($n)
   if isfight($i)
       if $i.level > 89
           if rand(15)
              say Amateur.  Let me show you how it's done.
              hitall
              endif
           endif
      if $i.level > 74
          if $i.level < 90
              if rand(10)
                 say Amateur.  Let me show you how it's done.
                 hitall
                 endif
               endif
          endif
      if $i.level > 50
          if $i.level < 75
              if rand(5)
                 say Amateur.  Let me show you how it's done.
                 hitall
                 endif
              endif
          endif
      endif
  endif
~
>act_prog p advances confidently towards you, and flies into a rage!~
if isfight($i)
   if ispc($n)
      if $i.level > 89
         if rand(15)
            say Take a hike, loser.
            stun $n
            endif
         endif
     if $i.level > 74
        if $i.level < 90
            if rand(10)
               say Take a hike, loser.
               stun $n
               endif
            endif
         endif
    if $i.level > 50
        if $i.level < 75
            if rand(5)
               say Take a hike, loser.
               stun $n
               endif
            endif
         endif
      endif
   endif
~
>act_prog p utters the words, 'saugai'~
if isfight($i)
   if ispc($n)
      if $i.level > 89
         if rand(15)
            snarl $n
            headbutt $n
            endif
         endif
     if $i.level > 74
        if $i.level < 90
            if rand(10)
               snarl $n
               headbutt $n
               endif
            endif
         endif
    if $i.level > 50
        if $i.level < 75
            if rand(5)
               snarl $n
               headbutt $n
               endif
            endif
         endif
      endif
   endif
~
>act_prog p utters the words, 'qfzzsuio oculoahp'~
if isfight($i)
   if ispc($n)
      if $i.level > 89
         if rand(15)
            say Bugs!? I Hate bugs!
            kick $n
            endif
         endif
     if $i.level > 74
        if $i.level < 90
            if rand(10)
               say Bugs!? I Hate bugs!
               kick $n
               endif
            endif
         endif
    if $i.level > 50
        if $i.level < 75
            if rand(5)
               say Bugs!? I Hate bugs!
               kick $n
               endif
            endif
         endif
      endif
   endif
~
>act_prog p attempts to disarm you!~
if isfight($i)
   if ispc($n)
      if $i.level > 89
         if rand(15)
            laugh $n
            headbutt $n
            endif
         endif
     if $i.level > 74
        if $i.level < 90
            if rand(10)
               laugh $n
               headbutt $n
               endif
            endif
         endif
    if $i.level > 50
        if $i.level < 75
            if rand(5)
               laugh $n
               headbutt $n
               endif
            endif
         endif
      endif
   endif
~
>act_prog p disarms you and sends your weapon flying!~
if ispc($n)
   if isfight($i)
       if $i.level > 89
           if rand(15)
              say Oh, like I can't whip your ass with my bare hands.
              hitall
              endif
           endif
      if $i.level > 74
          if $i.level < 90
              if rand(10)
                 say Oh, like I can't whip your ass with my bare hands.
                 hitall
                 endif
               endif
          endif
      if $i.level > 50
          if $i.level < 75
              if rand(5)
                 say Oh, like I can't whip your ass with my bare hands.
                 hitall
                 endif
              endif
          endif
      endif
  endif
~
>act_prog p You dodge a bash from~
if isfight($i)
   if ispc($n)
      if $i.level > 89
         if rand(15)
            say You need more practice.
            MPECHOAT $n You scream in pain from $I's deathstroke to your kidneys!  ARRRGGGGHHHHH!
            MPECHOAROUND $n $I deathstrokes $n in $s kidneys!  $n cries out in pain!
MPDAMAGE $n 1d1% undefined
            MPDAMAGE $n 10d50 undefined
            endif
         endif
      if $i.level > 74
         if $i.level < 90
            if rand(10)
               say You need more practice.
               MPECHOAT $n You scream in pain from $I's deathstroke to your kidneys!  ARRRGGGGHHHHH!
               MPECHOAROUND $n $I deathstrokes $n in $s kidneys!  $n cries out in pain!
MPDAMAGE $n 1d1% undefined
               MPDAMAGE $n 10d40 undefined
               endif
            endif
         endif
    if $i.level > 50
       if $i.level < 75
          if rand(5)
             say You need more practice.
             MPECHOAT $n You scream in pain from $I's deathstroke to your kidneys!  ARRRGGGGHHHHH!
              MPECHOAROUND $n $I deathstrokes $n in $s kidneys!  $n cries out in pain!
              MPDAMAGE $n 10d30 undefined
              endif
            endif
          endif
       endif
   endif
~
>act_prog p utters the words, 'euzuiz yjfl'~
if isfight($i)
   if ispc($n)
      if $i.level > 89
         if rand(15)
            laugh $n
            headbutt $n
            endif
         endif
     if $i.level > 74
        if $i.level < 90
            if rand(10)
               laugh $n
               headbutt $n
               endif
            endif
         endif
    if $i.level > 50
        if $i.level < 75
            if rand(5)
               laugh $n
               headbutt $n
               endif
            endif
         endif
      endif
   endif
~
>act_prog p utters the words, 'garandoa oahz'~
if ispc($n)
   if isfight($i)
       if $i.level > 89
           if rand(15)
              roar
              hitall
              endif
           endif
      if $i.level > 74
          if $i.level < 90
              if rand(10)
                 roar
                 hitall
                 endif
               endif
          endif
      if $i.level > 50
          if $i.level < 75
              if rand(5)
                 roar
                 hitall
                 endif
              endif
          endif
      endif
  endif
~
>act_prog p utters the words, 'wzhzaf gxandoaw'~
if isfight($i)
   if ispc($n)
      if $i.level > 89
         if rand(9)
            say Like a few little pebbles are going to hurt me!
            redirect $n
            endif
         endif
      if $i.level > 74
         if $i.level < 90
            if rand(6)
               say Like a few little pebbles are going to hurt me!
               redirect $n
               endif
            endif
         endif
     if $i.level > 50
        if $i.level < 75
           if rand(3)
              say Like a few little pebbles are going to hurt me!
              redirect $n
              endif
           endif
        endif
      endif
   endif
~
>act_prog p utters the words, 'yufzghafw'~
if ispc($n)
   if isfight($i)
       if $i.level > 89
           if rand(12)
              roar
              hitall
              endif
           endif
      if $i.level > 74
          if $i.level < 90
              if rand(8)
                 roar
                 hitall
                 endif
               endif
          endif
      if $i.level > 50
          if $i.level < 75
              if rand(4)
                 roar
                 hitall
                 endif
              endif
          endif
      endif
  endif
~
>act_prog p utters the words, 'sandoaarlkz'~
if isfight($i)
   if ispc($n)
      if $i.level > 89
         if rand(15)
            froth $n
            redirect $n
            endif
         endif
     if $i.level > 74
        if $i.level < 90
            if rand(10)
               froth $n
               redirect $n
               endif
            endif
         endif
    if $i.level > 50
        if $i.level < 75
            if rand(5)
               froth $n
               redirect $n
               endif
            endif
         endif
      endif
   endif
~
>act_prog p utters the words, 'pzrrghxanduaw'~
if isfight($i)
   if ispc($n)
      if $i.level > 89
         if rand(15)
            snort $n
            stun $n
            endif
         endif
     if $i.level > 74
        if $i.level < 90
           if rand(10)
              snort $n
              stun $n
              endif
            endif
         endif
    if $i.level > 50
        if $i.level < 75
           if rand(5)
              snort $n
              stun $n
              endif
            endif
         endif
      endif
   endif
~
>act_prog p utters the words, 'eugszr~
if isfight($i)
   if ispc($n)
      if $i.level > 89
         if rand(15)
            froth $n
            redirect $n
            endif
         endif
     if $i.level > 74
        if $i.level < 90
            if rand(10)
               froth $n
               redirect $n
               endif
            endif
         endif
    if $i.level > 50
        if $i.level < 75
            if rand(5)
               froth $n
               redirect $n
               endif
            endif
         endif
      endif
   endif
~
>act_prog p utters the words, 'saxzf pandoaw'~
if isfight($i)
    if ispc($n)
       if $i.level > 89
           if rand(15)
               say Let me show you some real pain!
               MPECHOAT $n You scream in pain from $I's deathstroke to your kidneys!  ARRRGGGGHHHHH!
               MPECHOAROUND $n $I deathstrokes $n in $s kidneys!  $n cries out in pain!
MPDAMAGE $n 1d1% undefined
               MPDAMAGE $n 10d50 undefined
               endif
           endif
       if $i.level > 74
           if $i.level < 90
              if rand(10)
                 say Let me show you some real pain!
                 MPECHOAT $n You scream in pain from $I's deathstroke to your kidneys!  ARRRGGGGHHHHH!
                 MPECHOAROUND $n $I deathstrokes $n in $s kidneys!  $n cries out in pain!
MPDAMAGE $n 1d1% undefined
                 MPDAMAGE $n 10d40 undefined
                 endif
              endif
           endif
       if $i.level > 50
           if $i.level < 75
               if rand(5)
                   say Let me show you some real pain!
                  MPECHOAT $n You scream in pain from $I's deathstroke to your kidneys!  ARRRGGGGHHHHH!
                  MPECHOAROUND $n $I deathstrokes $n in $s kidneys!  $n cries out in pain!
                  MPDAMAGE $n 10d30 undefined
                  endif
               endif
          endif
       endif
   endif
~
>act_prog p utters the words, 'yrawzghfutz'~
if isfight($i)
   if ispc($n)
      if $i.level > 89
         if rand(15)
            grunt $n
            bash $n
            endif
         endif
      if $i.level > 74
         if $i.level < 90
             if rand(10)
                grunt $n
                bash $n
                endif
             endif
          endif
      if $i.level > 50
         if $i.level < 75
            if rand(5)
               grunt $n
               bash $n
               endif
            endif
         endif
      endif
   endif
~
>act_prog p utters the words, 'pzar gsfal'~
if ispc($n)
   if isfight($i)
       if $i.level > 89
           if rand(15)
               say Pray to your gods some more!
               hitall
               endif
          endif
      if $i.level > 74
          if $i.level < 90
              if rand(10)
                  say Pray to your gods some more!
                  hitall
                  endif
              endif
          endif
      if $i.level > 50
          if $i.level < 75
              if rand(5)
                  say Pray to your gods some more!
                  hitall
                  endif
              endif
          endif
      endif
  endif
~
>fight_prog 15~
if numpcs() > 2
  hitall
endif
~
>fight_prog 10~
if $i.level > 89
   if cansee($n)
      MPECHOAT $n You scream in pain from $I's deathstroke to your kidneys!   ARRRGGGGHHHHH!
      MPECHOAROUND $n $I deathstrokes $n in $s kidneys!  $n cries out in pain!
      MPDAMAGE $n 10d50+1% undefined
      endif
   endif
if $i.level > 74
    if $i.level < 90
       if cansee($n)
           MPECHOAT $n You scream in pain from $I's deathstroke to your kidneys!   ARRRGGGGHHHHH!
           MPECHOAROUND $n $I deathstrokes $n in $s kidneys!  $n cries out in pain!
           MPDAMAGE $n 10d40+1% undefined
           endif
        endif
   endif
if $i.level > 50
    if $i.level < 75
       if cansee($n)
          MPECHOAT $n You scream in pain from $I's deathstroke to your kidneys!   ARRRGGGGHHHHH!
          MPECHOAROUND $n $I deathstrokes $n in $s kidneys!  $n cries out in pain!
          MPDAMAGE $n 10d30 undefined
          endif
       endif
   endif
~
>fight_prog 10~
if rand(33)
   kick $n
   else
   if rand(50)
      headbutt $n
      else
      bash $n
      endif
   endif
~
>fight_prog 8~
stun $n
~
>fight_prog 5~
if iswielding($n)
   disarm $n
   else
   stun $n
endif
~
>fight_prog 0~
if level($i) > 55
  MPECHOAT $n You scream in pain from $I's deathstroke to your kidneys!   ARRRGGGGHHHHH!
  MPECHOAROUND $n $I deathstrokes $n in $s kidneys!  $n cries out in pain!
  MPDAMAGE $n 10d40 undefined
  break
endif
~
|
S
#113
skillset mob~
PROC MOB BARBARIAN LEVEL 1-20~
This is a Skillset Mob for the Barbarian Class, levels 1-20.
~
~
768 0 -1
2097152 0 0 -1
0 0 0
20 10 0d1+0 1d1+0
0 0
8 8 0 0 0 0
>fight_prog 10~
bash
~
>fight_prog 10~
kick
~
|
S
#114
skillset mob~
PROC MOB BARBARIAN LEVEL 21-35~
This is a Skillset Mob for the Barbarian Class, levels 21-35.
~
~
768 0 -1
2097152 0 0 -1
0 0 0
20 10 0d1+0 1d1+0
0 0
8 8 0 0 0 0
>fight_prog 10~
rage
~
>fight_prog 10~
bash
~
>fight_prog 10~
kick
~
|
S
#115
skillset mob~
PROC MOB BARBARIAN LEVEL 36-50~
This is a Skillset Mob for the Barbarian Class, levels 36-50.
~
~
526080 0 -1
2097152 0 0 -1
0 0 0
20 10 1d1+0 1d1+0
0 0
8 8 0 0 0 0
>fight_prog 20~
if level($i) > 39
  if numpcs() > 2
    hitall
  endif
endif
~
>fight_prog 15~
headbutt
~
>fight_prog 10~
rage
~
>fight_prog 10~
bash
~
>fight_prog 5~
kick
~
|
S
#116
skillset mob~
PROC MOB BARBARIAN LEVEL 51+~
This is a Skillset Mob for the Barbarian Class, levels 51+
~
~
526080 0 -1
2097152 0 0 -1
0 0 0
20 10 0d1+0 1d1+0
0 0
8 8 0 0 0 0
>act_prog p utters the words, 'zawsufuq hajqp'~
if isfight($i)
   if ispc($n)
      if $i.level > 89
         if rand(15)
            grunt
            if rand(33)
               kick $n
               else
               if rand(50)
                  bash $n
                  else
                  headbutt $n
                  endif
               endif
            endif
         endif
      if $i.level > 74
         if $i.level < 90
            if rand(10)
               if rand(33)
                  kick $n
                  else
                  if rand(50)
                     bash $n
                     else
                     headbutt $n
                     endif
                  endif
               endif
            endif
         endif
      if $i.level > 50
         if $i.level < 75
            if rand(5)
               grunt
               if rand(33)
                  kick $n
                  else
                  if rand(50)
                     bash $n
                     else
                     headbutt $n
                     endif
                  endif
               endif
            endif
         endif
      endif
   endif
~
>act_prog p utters the words, 'aque bragh'~
if isfight($i)
   if ispc($n)
      if $i.level > 89
         if rand(15)
            grunt
            if rand(33)
               kick $n
               else
               if rand(50)
                  bash $n
                  else
                  headbutt $n
                  endif
               endif
            endif
         endif
      if $i.level > 74
         if $i.level < 90
            if rand(10)
               if rand(33)
                  kick $n
                  else
                  if rand(50)
                     bash $n
                     else
                     headbutt $n
                     endif
                  endif
               endif
            endif
         endif
      if $i.level > 50
         if $i.level < 75
            if rand(5)
               grunt
               if rand(33)
                  kick $n
                  else
                  if rand(50)
                     bash $n
                     else
                     headbutt $n
                     endif
                  endif
               endif
            endif
         endif
      endif
   endif
~
>act_prog p vibrations make you suffer~
if isfight($i)
   if ispc($n)
      if $i.level > 89
         if rand(15)
            roar
            if rand(33)
               hitall
               else
               knockback $n
               endif
            endif
         endif
      if $i.level > 74
         if $i.level < 90
            if rand(10)
               roar
               if rand(50)
                  hitall
                  else
                  knockback $n
                  endif
               endif
            endif
         endif
      if $i.level > 50
         if $i.level < 75
            if rand(5)
               roar
               if rand(66)
                  hitall
                  else
                  knockback $n
                  endif
               endif
            endif
         endif
      endif
   endif
~
>act_prog p slams a bolt of focused ki energy into the flow of magic around you!~
if isfight($i)
   if ispc($n)
      if $i.level > 89
         if rand(15)
            roar
            knockback $n
            endif
         endif
      if $i.level > 74
         if $i.level < 90
            if rand(10)
               roar
               if rand(33)
                  knockback $n
                  else
                  if rand(50)
                     headbutt $n
                     else
                     bash $n
                     endif
                  endif
               endif
            endif
         endif
      if $i.level > 50
         if $i.level < 75
            if rand(5)
               roar
               if rand(25)
                  knockback $n
                  else
                  if rand(50)
                     headbutt $n
                     else
                     bash $n
                     endif
                  endif
               endif
            endif
         endif
      endif
   endif
~
>act_prog p blasts you across the room, causing you to fall~
if isfight($i)
   if ispc($n)
      if $i.level > 89
         if rand(15)
            roar
            knockback $n
            endif
         endif
      if $i.level > 74
         if $i.level < 90
            if rand(10)
               roar
               if rand(33)
                  knockback $n
                  else
                  if rand(50)
                     headbutt $n
                     else
                     bash $n
                     endif
                  endif
               endif
            endif
         endif
      if $i.level > 50
         if $i.level < 75
            if rand(5)
               roar
               if rand(25)
                  knockback $n
                  else
                  if rand(50)
                     headbutt $n
                     else
                     bash $n
                     endif
                  endif
               endif
            endif
         endif
      endif
   endif
~
>act_prog p focuses and punches you square in the chest~
if isfight($i)
   if ispc($n)
      if $i.level > 89
         if rand(15)
            grunt
            if rand(33)
               kick $n
               else
               if rand(50)
                  bash $n
                  else
                  headbutt $n
                  endif
               endif
            endif
         endif
      if $i.level > 74
         if $i.level < 90
            if rand(10)
               if rand(33)
                  kick $n
                  else
                  if rand(50)
                     bash $n
                     else
                     headbutt $n
                     endif
                  endif
               endif
            endif
         endif
      if $i.level > 50
         if $i.level < 75
            if rand(5)
               grunt
               if rand(33)
                  kick $n
                  else
                  if rand(50)
                     bash $n
                     else
                     headbutt $n
                     endif
                  endif
               endif
            endif
         endif
      endif
   endif
~
>act_prog p sings a witty little limerick to you!~
if isfight($i)
   if ispc($n)
      if $i.level > 89
         if rand(15)
            roar
            knockback $n
            endif
         endif
      if $i.level > 74
         if $i.level < 90
            if rand(10)
               roar
               if rand(33)
                  knockback $n
                  else
                  if rand(50)
                     headbutt $n
                     else
                     bash $n
                     endif
                  endif
               endif
            endif
         endif
      if $i.level > 50
         if $i.level < 75
            if rand(5)
               roar
               if rand(25)
                  knockback $n
                  else
                  if rand(50)
                     headbutt $n
                     else
                     bash $n
                     endif
                  endif
               endif
            endif
         endif
      endif
   endif
~
>act_prog p lashes out quickly and cripples you with a painful blow!~
if isfight($i)
   if ispc($n)
      if $i.level > 89
         if rand(15)
            roar
            if rand(33)
               hitall
               else
               knockback $n
               endif
            endif
         endif
      if $i.level > 74
         if $i.level < 90
            if rand(10)
               roar
               if rand(50)
                  hitall
                  else
                  knockback $n
                  endif
               endif
            endif
         endif
      if $i.level > 50
         if $i.level < 75
            if rand(5)
               roar
               if rand(66)
                  hitall
                  else
                  knockback $n
                  endif
               endif
            endif
         endif
      endif
   endif
~
>act_prog p with lethal accuracy and strength.~
if isfight($i)
   if ispc($n)
      if $i.level > 89
         if rand(15)
            grunt
            if rand(33)
               kick $n
               else
               if rand(50)
                  bash $n
                  else
                  headbutt $n
                  endif
               endif
            endif
         endif
      if $i.level > 74
         if $i.level < 90
            if rand(10)
               if rand(33)
                  kick $n
                  else
                  if rand(50)
                     bash $n
                     else
                     headbutt $n
                     endif
                  endif
               endif
            endif
         endif
      if $i.level > 50
         if $i.level < 75
            if rand(5)
               grunt
               if rand(33)
                  kick $n
                  else
                  if rand(50)
                     bash $n
                     else
                     headbutt $n
                     endif
                  endif
               endif
            endif
         endif
      endif
   endif
~
>act_prog p circles around~
if isfight($i)
   if ispc($n)
      if $i.level > 89
         if rand(15)
            grunt
            if rand(33)
               kick $n
               else
               if rand(50)
                  bash $n
                  else
                  headbutt $n
                  endif
               endif
            endif
         endif
      if $i.level > 74
         if $i.level < 90
            if rand(10)
               if rand(33)
                  kick $n
                  else
                  if rand(50)
                     bash $n
                     else
                     headbutt $n
                     endif
                  endif
               endif
            endif
         endif
      if $i.level > 50
         if $i.level < 75
            if rand(5)
               grunt
               if rand(33)
                  kick $n
                  else
                  if rand(50)
                     bash $n
                     else
                     headbutt $n
                     endif
                  endif
               endif
            endif
         endif
      endif
   endif
~
>act_prog p utters the words, 'unmanifgz'~
if isfight($i)
   if ispc($n)
      if $i.level > 89
         if rand(15)
            roar
            knockback $n
            endif
         endif
      if $i.level > 74
         if $i.level < 90
            if rand(10)
               roar
               if rand(33)
                  knockback $n
                  else
                  if rand(50)
                     headbutt $n
                     else
                     bash $n
                     endif
                  endif
               endif
            endif
         endif
      if $i.level > 50
         if $i.level < 75
            if rand(5)
               roar
               if rand(25)
                  knockback $n
                  else
                  if rand(50)
                     headbutt $n
                     else
                     bash $n
                     endif
                  endif
               endif
            endif
         endif
      endif
   endif
~
>act_prog p utters the words, 'ubralicra'~
if isfight($i)
   if ispc($n)
      if $i.level > 89
         if rand(15)
            roar
            hitall
            endif
         endif
      if $i.level > 74
         if $i.level < 90
            if rand(10)
               roar
               hitall
               endif
            endif
         endif
      if $i.level > 50
         if $i.level < 75
            if rand(5)
               roar
               hitall
               endif
            endif
         endif
      endif
   endif
~
>act_prog p utters the words, 'qahijf gsfal'~
if isfight($i)
   if ispc($n)
      if $i.level > 89
         if rand(15)
            grunt
            if rand(33)
               kick $n
               else
               if rand(50)
                  bash $n
                  else
                  headbutt $n
                  endif
               endif
            endif
         endif
      if $i.level > 74
         if $i.level < 90
            if rand(10)
               if rand(33)
                  kick $n
                  else
                  if rand(50)
                     bash $n
                     else
                     headbutt $n
                     endif
                  endif
               endif
            endif
         endif
      if $i.level > 50
         if $i.level < 75
            if rand(5)
               grunt
               if rand(33)
                  kick $n
                  else
                  if rand(50)
                     bash $n
                     else
                     headbutt $n
                     endif
                  endif
               endif
            endif
         endif
      endif
   endif
~
>act_prog p utters the words, 'qarr solilla'~
if isfight($i)
   if ispc($n)
      if $i.level > 89
         if rand(15)
            grunt
            if rand(33)
               kick $n
               else
               if rand(50)
                  bash $n
                  else
                  headbutt $n
                  endif
               endif
            endif
         endif
      if $i.level > 74
         if $i.level < 90
            if rand(10)
               if rand(33)
                  kick $n
                  else
                  if rand(50)
                     bash $n
                     else
                     headbutt $n
                     endif
                  endif
               endif
            endif
         endif
      if $i.level > 50
         if $i.level < 75
            if rand(5)
               grunt
               if rand(33)
                  kick $n
                  else
                  if rand(50)
                     bash $n
                     else
                     headbutt $n
                     endif
                  endif
               endif
            endif
         endif
      endif
   endif
~
>act_prog p utters the words, 'gji brul'~
if isfight($i)
   if ispc($n)
      if $i.level > 89
         if rand(15)
            grunt
            if rand(33)
               kick $n
               else
               if rand(50)
                  bash $n
                  else
                  headbutt $n
                  endif
               endif
            endif
         endif
      if $i.level > 74
         if $i.level < 90
            if rand(10)
               if rand(33)
                  kick $n
                  else
                  if rand(50)
                     bash $n
                     else
                     headbutt $n
                     endif
                  endif
               endif
            endif
         endif
      if $i.level > 50
         if $i.level < 75
            if rand(5)
               grunt
               if rand(33)
                  kick $n
                  else
                  if rand(50)
                     bash $n
                     else
                     headbutt $n
                     endif
                  endif
               endif
            endif
         endif
      endif
   endif
~
>act_prog p utters the words, 'efaxi'~
if isfight($i)
   if ispc($n)
      if $i.level > 89
         if rand(15)
            grunt
            if rand(33)
               kick $n
               else
               if rand(50)
                  bash $n
                  else
                  headbutt $n
                  endif
               endif
            endif
         endif
      if $i.level > 74
         if $i.level < 90
            if rand(10)
               if rand(33)
                  kick $n
                  else
                  if rand(50)
                     bash $n
                     else
                     headbutt $n
                     endif
                  endif
               endif
            endif
         endif
      if $i.level > 50
         if $i.level < 75
            if rand(5)
               grunt
               if rand(33)
                  kick $n
                  else
                  if rand(50)
                     bash $n
                     else
                     headbutt $n
                     endif
                  endif
               endif
            endif
         endif
      endif
   endif
~
>act_prog p deathstroke to your kidneys!   AARRRGGGGHHHH!~
if isfight($i)
   if ispc($n)
      if $i.level > 89
         if rand(15)
            grunt
            if rand(33)
               kick $n
               else
               if rand(50)
                  bash $n
                  else
                  headbutt $n
                  endif
               endif
            endif
         endif
      if $i.level > 74
         if $i.level < 90
            if rand(10)
               if rand(33)
                  kick $n
                  else
                  if rand(50)
                     bash $n
                     else
                     headbutt $n
                     endif
                  endif
               endif
            endif
         endif
      if $i.level > 50
         if $i.level < 75
            if rand(5)
               grunt
               if rand(33)
                  kick $n
                  else
                  if rand(50)
                     bash $n
                     else
                     headbutt $n
                     endif
                  endif
               endif
            endif
         endif
      endif
   endif
~
>act_prog p deathstroke and falls to the ground.~
if isfight($i)
   if ispc($n)
      if $i.level > 89
         if rand(15)
            grunt
            if rand(33)
               kick $n
               else
               if rand(50)
                  bash $n
                  else
                  headbutt $n
                  endif
               endif
            endif
         endif
      if $i.level > 74
         if $i.level < 90
            if rand(10)
               if rand(33)
                  kick $n
                  else
                  if rand(50)
                     bash $n
                     else
                     headbutt $n
                     endif
                  endif
               endif
            endif
         endif
      if $i.level > 50
         if $i.level < 75
            if rand(5)
               grunt
               if rand(33)
                  kick $n
                  else
                  if rand(50)
                     bash $n
                     else
                     headbutt $n
                     endif
                  endif
               endif
            endif
         endif
      endif
   endif
~
>act_prog p attempts to hit you in your solar plexus!~
if isfight($i)
   if ispc($n)
      if $i.level > 89
         if rand(15)
            roar
            knockback $n
            endif
         endif
      if $i.level > 74
         if $i.level < 90
            if rand(10)
               roar
               if rand(33)
                  knockback $n
                  else
                  if rand(50)
                     headbutt $n
                     else
                     bash $n
                     endif
                  endif
               endif
            endif
         endif
      if $i.level > 50
         if $i.level < 75
            if rand(5)
               roar
               if rand(25)
                  knockback $n
                  else
                  if rand(50)
                     headbutt $n
                     else
                     bash $n
                     endif
                  endif
               endif
            endif
         endif
      endif
   endif
~
>act_prog p lunges forward in an attempt to smash into you but falls flat on~
if isfight($i)
   if ispc($n)
      if $i.level > 89
         if rand(15)
            roar
            if rand(33)
               hitall
               else
               knockback $n
               endif
            endif
         endif
      if $i.level > 74
         if $i.level < 90
            if rand(10)
               roar
               if rand(50)
                  hitall
                  else
                  knockback $n
                  endif
               endif
            endif
         endif
      if $i.level > 50
         if $i.level < 75
            if rand(5)
               roar
               if rand(66)
                  hitall
                  else
                  knockback $n
                  endif
               endif
            endif
         endif
      endif
   endif
~
>act_prog p tries to headbutt you, but fails miserably.~
if isfight($i)
   if ispc($n)
      if $i.level > 89
         if rand(15)
            grunt
            if rand(33)
               kick $n
               else
               if rand(50)
                  bash $n
                  else
                  headbutt $n
                  endif
               endif
            endif
         endif
      if $i.level > 74
         if $i.level < 90
            if rand(10)
               if rand(33)
                  kick $n
                  else
                  if rand(50)
                     bash $n
                     else
                     headbutt $n
                     endif
                  endif
               endif
            endif
         endif
      if $i.level > 50
         if $i.level < 75
            if rand(5)
               grunt
               if rand(33)
                  kick $n
                  else
                  if rand(50)
                     bash $n
                     else
                     headbutt $n
                     endif
                  endif
               endif
            endif
         endif
      endif
   endif
~
>act_prog p You are SHOCKED~
if isfight($i)
   if ispc($n)
      if $i.level > 89
         if rand(15)
            grunt
            if rand(33)
               kick $n
               else
               if rand(50)
                  bash $n
                  else
                  headbutt $n
                  endif
               endif
            endif
         endif
      if $i.level > 74
         if $i.level < 90
            if rand(10)
               if rand(33)
                  kick $n
                  else
                  if rand(50)
                     bash $n
                     else
                     headbutt $n
                     endif
                  endif
               endif
            endif
         endif
      if $i.level > 50
         if $i.level < 75
            if rand(5)
               grunt
               if rand(33)
                  kick $n
                  else
                  if rand(50)
                     bash $n
                     else
                     headbutt $n
                     endif
                  endif
               endif
            endif
         endif
      endif
   endif
~
>act_prog p but trips~
if isfight($i)
   if ispc($n)
      if $i.level > 89
         if rand(15)
            grunt
            if rand(33)
               kick $n
               else
               if rand(50)
                  bash $n
                  else
                  headbutt $n
                  endif
               endif
            endif
         endif
      if $i.level > 74
         if $i.level < 90
            if rand(10)
               if rand(33)
                  kick $n
                  else
                  if rand(50)
                     bash $n
                     else
                     headbutt $n
                     endif
                  endif
               endif
            endif
         endif
      if $i.level > 50
         if $i.level < 75
            if rand(5)
               grunt
               if rand(33)
                  kick $n
                  else
                  if rand(50)
                     bash $n
                     else
                     headbutt $n
                     endif
                  endif
               endif
            endif
         endif
      endif
   endif
~
>act_prog p starts swinging like a MADMAN!~
if isfight($i)
   if ispc($n)
      if $i.level > 89
         if rand(15)
            roar
            hitall
            endif
         endif
      if $i.level > 74
         if $i.level < 90
            if rand(10)
               roar
               hitall
               endif
            endif
         endif
      if $i.level > 50
         if $i.level < 75
            if rand(5)
               roar
               hitall
               endif
            endif
         endif
      endif
   endif
~
>act_prog p advances confidently towards you, and flies into a rage!~
if isfight($i)
   if ispc($n)
      if $i.level > 89
         if rand(15)
            roar
            knockback $n
            endif
         endif
      if $i.level > 74
         if $i.level < 90
            if rand(10)
               roar
               if rand(33)
                  knockback $n
                  else
                  if rand(50)
                     headbutt $n
                     else
                     bash $n
                     endif
                  endif
               endif
            endif
         endif
      if $i.level > 50
         if $i.level < 75
            if rand(5)
               roar
               if rand(25)
                  knockback $n
                  else
                  if rand(50)
                     headbutt $n
                     else
                     bash $n
                     endif
                  endif
               endif
            endif
         endif
      endif
   endif
~
>act_prog p utters the words, 'saugai'~
if isfight($i)
   if ispc($n)
      if $i.level > 89
         if rand(15)
            grunt
            if rand(33)
               kick $n
               else
               if rand(50)
                  bash $n
                  else
                  headbutt $n
                  endif
               endif
            endif
         endif
      if $i.level > 74
         if $i.level < 90
            if rand(10)
               if rand(33)
                  kick $n
                  else
                  if rand(50)
                     bash $n
                     else
                     headbutt $n
                     endif
                  endif
               endif
            endif
         endif
      if $i.level > 50
         if $i.level < 75
            if rand(5)
               grunt
               if rand(33)
                  kick $n
                  else
                  if rand(50)
                     bash $n
                     else
                     headbutt $n
                     endif
                  endif
               endif
            endif
         endif
      endif
   endif
~
>act_prog p utters the words, 'qfzzsuio oculoahp'~
if isfight($i)
   if ispc($n)
      if $i.level > 89
         if rand(15)
            grunt
            if rand(33)
               kick $n
               else
               if rand(50)
                  bash $n
                  else
                  headbutt $n
                  endif
               endif
            endif
         endif
      if $i.level > 74
         if $i.level < 90
            if rand(10)
               if rand(33)
                  kick $n
                  else
                  if rand(50)
                     bash $n
                     else
                     headbutt $n
                     endif
                  endif
               endif
            endif
         endif
      if $i.level > 50
         if $i.level < 75
            if rand(5)
               grunt
               if rand(33)
                  kick $n
                  else
                  if rand(50)
                     bash $n
                     else
                     headbutt $n
                     endif
                  endif
               endif
            endif
         endif
      endif
   endif
~
>act_prog p attempts to disarm you!~
if isfight($i)
   if ispc($n)
      if $i.level > 89
         if rand(15)
            grunt
            if rand(33)
               kick $n
               else
               if rand(50)
                  bash $n
                  else
                  headbutt $n
                  endif
               endif
            endif
         endif
      if $i.level > 74
         if $i.level < 90
            if rand(10)
               if rand(33)
                  kick $n
                  else
                  if rand(50)
                     bash $n
                     else
                     headbutt $n
                     endif
                  endif
               endif
            endif
         endif
      if $i.level > 50
         if $i.level < 75
            if rand(5)
               grunt
               if rand(33)
                  kick $n
                  else
                  if rand(50)
                     bash $n
                     else
                     headbutt $n
                     endif
                  endif
               endif
            endif
         endif
      endif
   endif
~
>act_prog p disarms you and sends your weapon flying!~
if isfight($i)
   if ispc($n)
      if $i.level > 89
         if rand(15)
            say Oh, like I can't whip your ass with my bare hands.
            hitall
            endif
         endif
      if $i.level > 74
         if $i.level < 90
            if rand(10)
               say Oh, like I can't whip your ass with my bare hands.
               hitall
               endif
            endif
         endif
      if $i.level > 50
         if $i.level < 75
            if rand(5)
               say Oh, like I can't whip your ass with my bare hands.
               hitall
               endif
            endif
         endif
      endif
   endif
~
>act_prog p You dodge a bash from~
if isfight($i)
   if ispc($n)
      if $i.level > 89
         if rand(15)
            grunt
            if rand(33)
               kick $n
               else
               if rand(50)
                  bash $n
                  else
                  headbutt $n
                  endif
               endif
            endif
         endif
      if $i.level > 74
         if $i.level < 90
            if rand(10)
               if rand(33)
                  kick $n
                  else
                  if rand(50)
                     bash $n
                     else
                     headbutt $n
                     endif
                  endif
               endif
            endif
         endif
      if $i.level > 50
         if $i.level < 75
            if rand(5)
               grunt
               if rand(33)
                  kick $n
                  else
                  if rand(50)
                     bash $n
                     else
                     headbutt $n
                     endif
                  endif
               endif
            endif
         endif
      endif
   endif
~
>act_prog p utters the words, 'euzuiz yjfl'~
if isfight($i)
   if ispc($n)
      if $i.level > 89
         if rand(15)
            grunt
            if rand(33)
               kick $n
               else
               if rand(50)
                  bash $n
                  else
                  headbutt $n
                  endif
               endif
            endif
         endif
      if $i.level > 74
         if $i.level < 90
            if rand(10)
               if rand(33)
                  kick $n
                  else
                  if rand(50)
                     bash $n
                     else
                     headbutt $n
                     endif
                  endif
               endif
            endif
         endif
      if $i.level > 50
         if $i.level < 75
            if rand(5)
               grunt
               if rand(33)
                  kick $n
                  else
                  if rand(50)
                     bash $n
                     else
                     headbutt $n
                     endif
                  endif
               endif
            endif
         endif
      endif
   endif
~
>act_prog p utters the words, 'garandoa oahz'~
if isfight($i)
   if ispc($n)
      if $i.level > 89
         if rand(15)
            roar
            knockback $n
            endif
         endif
      if $i.level > 74
         if $i.level < 90
            if rand(10)
               roar
               if rand(33)
                  knockback $n
                  else
                  if rand(50)
                     headbutt $n
                     else
                     bash $n
                     endif
                  endif
               endif
            endif
         endif
      if $i.level > 50
         if $i.level < 75
            if rand(5)
               roar
               if rand(25)
                  knockback $n
                  else
                  if rand(50)
                     headbutt $n
                     else
                     bash $n
                     endif
                  endif
               endif
            endif
         endif
endif
endif
~
>act_prog p utters the words, 'wzhzaf gxandoaw'~
if isfight($i)
   if ispc($n)
      if $i.level > 89
         if rand(15)
            grunt
            if rand(33)
               kick $n
               else
               if rand(50)
                  bash $n
                  else
                  headbutt $n
                  endif
               endif
            endif
         endif
      if $i.level > 74
         if $i.level < 90
            if rand(10)
               if rand(33)
                  kick $n
                  else
                  if rand(50)
                     bash $n
                     else
                     headbutt $n
                     endif
                  endif
               endif
            endif
         endif
      if $i.level > 50
         if $i.level < 75
            if rand(5)
               grunt
               if rand(33)
                  kick $n
                  else
                  if rand(50)
                     bash $n
                     else
                     headbutt $n
                     endif
                  endif
               endif
            endif
         endif
      endif
   endif
~
>act_prog p utters the words, 'yufzghafw'~
if isfight($i)
   if ispc($n)
      if $i.level > 89
         if rand(15)
            roar
            hitall
            endif
         endif
      if $i.level > 74
         if $i.level < 90
            if rand(10)
               roar
               hitall
               endif
            endif
         endif
      if $i.level > 50
         if $i.level < 75
            if rand(5)
               roar
               hitall
               endif
            endif
         endif
      endif
   endif
~
>act_prog p utters the words, 'sandoaarlkz'~
if isfight($i)
   if ispc($n)
      if $i.level > 89
         if rand(15)
            roar
            knockback $n
            endif
         endif
      if $i.level > 74
         if $i.level < 90
            if rand(10)
               roar
               if rand(33)
                  knockback $n
                  else
                  if rand(50)
                     headbutt $n
                     else
                     bash $n
                     endif
                  endif
               endif
            endif
         endif
      if $i.level > 50
         if $i.level < 75
            if rand(5)
               roar
               if rand(25)
                  knockback $n
                  else
                  if rand(50)
                     headbutt $n
                     else
                     bash $n
                     endif
                  endif
               endif
            endif
         endif
      endif
   endif
~
>act_prog p utters the words, 'pzrrghxanduaw'~
if isfight($i)
   if ispc($n)
      if $i.level > 89
         if rand(15)
            grunt
            if rand(33)
               kick $n
               else
               if rand(50)
                  bash $n
                  else
                  headbutt $n
                  endif
               endif
            endif
         endif
      if $i.level > 74
         if $i.level < 90
            if rand(10)
               if rand(33)
                  kick $n
                  else
                  if rand(50)
                     bash $n
                     else
                     headbutt $n
                     endif
                  endif
               endif
            endif
         endif
      if $i.level > 50
         if $i.level < 75
            if rand(5)
               grunt
               if rand(33)
                  kick $n
                  else
                  if rand(50)
                     bash $n
                     else
                     headbutt $n
                     endif
                  endif
               endif
            endif
         endif
      endif
   endif
~
>act_prog p utters the words, 'eugszr~
if isfight($i)
   if ispc($n)
      if $i.level > 89
         if rand(15)
            roar
            knockback $n
            endif
         endif
      if $i.level > 74
         if $i.level < 90
            if rand(10)
               roar
               if rand(33)
                  knockback $n
                  else
                  if rand(50)
                     headbutt $n
                     else
                     bash $n
                     endif
                  endif
               endif
            endif
         endif
      if $i.level > 50
         if $i.level < 75
            if rand(5)
               roar
               if rand(25)
                  knockback $n
                  else
                  if rand(50)
                     headbutt $n
                     else
                     bash $n
                     endif
                  endif
               endif
            endif
         endif
      endif
   endif
~
>act_prog p utters the words, 'saxzf pandoaw'~
if isfight($i)
   if ispc($n)
      if $i.level > 89
         if rand(15)
            grunt
            if rand(33)
               kick $n
               else
               if rand(50)
                  bash $n
                  else
                  headbutt $n
                  endif
               endif
            endif
         endif
      if $i.level > 74
         if $i.level < 90
            if rand(10)
               if rand(33)
                  kick $n
                  else
                  if rand(50)
                     bash $n
                     else
                     headbutt $n
                     endif
                  endif
               endif
            endif
         endif
      if $i.level > 50
         if $i.level < 75
            if rand(5)
               grunt
               if rand(33)
                  kick $n
                  else
                  if rand(50)
                     bash $n
                     else
                     headbutt $n
                     endif
                  endif
               endif
            endif
         endif
      endif
   endif
~
>act_prog p utters the words, 'pzar gsfal'~
if isfight($i)
   if ispc($n)
      if $i.level > 89
         if rand(15)
            roar
            if rand(33)
               hitall
               else
               knockback $n
               endif
            endif
         endif
      if $i.level > 74
         if $i.level < 90
            if rand(10)
               roar
               if rand(50)
                  hitall
                  else
                  knockback $n
                  endif
               endif
            endif
         endif
      if $i.level > 50
         if $i.level < 75
            if rand(5)
               roar
               if rand(66)
                  hitall
                  else
                  knockback $n
                  endif
               endif
            endif
         endif
      endif
   endif
~
>act_prog p utters the words, 'yrawzghfutz'~
if isfight($i)
   if ispc($n)
      if $i.level > 89
         if rand(15)
            grunt
            if rand(33)
               kick $n
               else
               if rand(50)
                  bash $n
                  else
                  headbutt $n
                  endif
               endif
            endif
         endif
      if $i.level > 74
         if $i.level < 90
            if rand(10)
               if rand(33)
                  kick $n
                  else
                  if rand(50)
                     bash $n
                     else
                     headbutt $n
                     endif
                  endif
               endif
            endif
         endif
      if $i.level > 50
         if $i.level < 75
            if rand(5)
               grunt
               if rand(33)
                  kick $n
                  else
                  if rand(50)
                     bash $n
                     else
                     headbutt $n
                     endif
                  endif
               endif
            endif
         endif
      endif
   endif
~
>arand_prog 1~
crazed assault
~
>hitprcnt_prog 40~
if isfight($i)
   if $i.level > 89
      if isspelled($i) blood_fury
         berserk
         else
         blood fury
         if rand(25)
            berserk
            endif
         endif
      endif
   if $i.level > 74
      if $i.level < 90
         if isspelled($i) blood_fury
            berserk
            else
            blood fury
            if rand(50)
               berserk
               endif
            endif
          endif
      endif
   if $i.level > 50
      if $i.level < 75
         if isspelled($i) blood_fury
            berserk
            else
            blood fury
            if rand(75)
               berserk
               endif
            endif
         endif
      endif
   endif
~
>fight_prog 20~
rage
~
>fight_prog 20~
if numpcs() > 2
  hitall
endif
~
>fight_prog 15~
if rand(33)
   kick
   else
   if rand(50)
       bash
       else
       headbutt
       endif
    endif
~
|
S
#117
skillset mob~
PROC MOB MONK LEVEL 1-20~
This is a Skillset Mob for the Monk Class, levels 1-20.
~
~
262912 0 -1
2097184 0 0 -1
0 0 0
20 10 1d1+0 1d1+0
0 0
8 8 0 0 0 0
>fight_prog 10~
trip
~
>fight_prog 10~
if level($i) < 6
  $n $I shoves $l hand into your chest, and your lifeblood ebbs away.
  MPECHOAROUND $n $I punches $l fist RIGHT INTO $n!
  MPDAMAGE $n 5d5 undefined
endif
if level($i) > 5
  if level($i) < 11
    MPECHOAT $n $I shoves $l hand into your chest, and your lifeblood ebbs away.
    MPECHOAROUND $n $I punches $l fist RIGHT INTO $n!
    MPDAMAGE $n 5d10 undefined
  endif
endif
if level($i) > 10
  if level($i) < 16
    MPECHOAT $n $I shoves $l hand into your chest, and your lifeblood ebbs away.
    MPECHOAROUND $n $I punches $l fist RIGHT INTO $n!
    MPDAMAGE $n 5d15 undefined
  endif
endif
if level($i) > 15
  if level($i) < 21
    MPECHOAT $n $I shoves $l hand into your chest, and your lifeblood ebbs away.
    MPECHOAROUND $n $I punches $l fist RIGHT INTO $n!
    MPDAMAGE $n 5d20 undefined
  endif
endif
~
|
S
#118
skillset mob~
PROC MOB MONK LEVEL 21-35~
This is a Skillset Mob for the Monk Class, levels 21-35.
~
~
262912 0 -1
2097184 0 0 -1
0 0 0
20 10 1d1+0 1d1+0
0 0
8 8 0 0 0 0
>fight_prog 10~
if level($i) > 20
  if level($i) < 26
    MPECHOAT $n $I shoves $l hand into your chest, and your lifeblood ebbs away.
    MPECHOAROUND $n $I punches $l fist RIGHT INTO $n!
    MPDAMAGE $n 25d5 undefined
endif
else
if level($i) > 25
  if level($i) < 31
    MPECHOAT $n $I shoves $l hand into your chest, and your lifeblood ebbs away.
    MPECHOAROUND $n $I punches $l fist RIGHT INTO $n!
    MPDAMAGE $n 25d6 undefined
endif
else
if level($i) > 30
  if level($i) < 36
    MPECHOAT $n $I shoves $l hand into your chest, and your lifeblood ebbs away.
    MPECHOAROUND $n $I punches $l fist RIGHT INTO $n!
    MPDAMAGE $n 35d5 undefined
endif
endif
endif
endif
~
>fight_prog 10~
stun
~
>fight_prog 10~
trip
~
|
S
#119
skillset mob~
PROC MOB MONK LEVEL 36-50~
This is a Skillset Mob for the Monk Class, levels 36-50.
~
~
263936 0 -1
2097184 0 0 -1
0 0 0
20 10 0d1+0 1d1+0
0 0
8 8 0 0 0 0
>fight_prog 7~
if hitprcnt($n) < 21
  say Eagleclaw!
  eagleclaw $n
endif
~
>fight_prog 7~
if level($i) > 35
  if level($i) < 41
    MPECHOAT $n $I shoves $l hand into your chest, and your lifeblood ebbs away.
    MPECHOAROUND $n $I punches $l fist RIGHT INTO $n!
    MPDAMAGE $n 20d10 undefined
  endif
else
if level($i) > 40
  if level($i) < 46
    MPECHOAT $n $I shoves $l hand into your chest, and your lifeblood ebbs away.
    MPECHOAROUND $n $I punches $l fist RIGHT INTO $n!
    MPDAMAGE $n 25d9 undefined
endif
else
if level($i) > 45
  if level($i) < 51
    MPECHOAT $n $I shoves $l hand into your chest, and your lifeblood ebbs away.
    MPECHOAROUND $n $I punches $l fist RIGHT INTO $n!
    MPDAMAGE $n 25d10 undefined
endif
endif
endif
endif
~
>fight_prog 10~
stun
~
>fight_prog 7~
trip
~
|
S
#120
skillset mob~
PROC MOB MONK LEVEL 51+~
This is a Skillset Mob for the Monk Class, levels 51+.
~
~
263936 0 -1
1075839008 0 0 -1
0 0 0
20 10 0d1+0 1d1+0
0 0
8 8 0 0 0 0
>act_prog p utters the words, 'zawsufuq hajqp'~
if isfight($i)
   if ispc($n)
      if $i.level > 89
         if rand(15)
            if rand(33)
               stun $n
               else
               if rand(50)
                  kick $n
                  else
                  eagleclaw $n
                  endif
               endif
            endif
         endif
      if $i.level > 74
         if $i.level < 90
            if rand(10)
               if rand(33)
                  stun $n
                  else
                  if rand(50)
                     kick $n
                     else
                     eagleclaw $n
                     endif
                  endif
               endif
            endif
         endif
      if $i.level > 51
         if $i.level < 75
            if rand(5)
               if rand(33)
                  stun
                  else
                  if rand(50)
                     kick $n
                     else
                     eagleclaw $n
                     endif
                  endif
               endif
            endif
         endif
      endif
   endif
~
>act_prog p utters the words, 'aque bragh'~
if isfight($i)
   if ispc($n)
      if $i.level > 89
         if rand(15)
            MPECHOAT $n $I shoves $l hand into your chest, and your lifeblood ebbs away.
            MPECHOAROUND $n $I punches $l fist RIGHT INTO $n!
MPDAMAGE $n 1d1% undefined
            MPDAMAGE $n 45d15 undefined
            endif
         endif
      if $i.level > 74
         if $i.level < 90
            if rand(10)
               MPECHOAT $n $I shoves $l hand into your chest, and your lifeblood ebbs away.
               MPECHOAROUND $n $I punches $l fist RIGHT INTO $n!
MPDAMAGE $n 1d1% undefined
               MPDAMAGE $n 35d15 undefined
               endif
            endif
         endif
      if $i.level > 51
         if $i.level < 75
            if rand(5)
               MPECHOAT $n $I shoves $l hand into your chest, and your lifeblood ebbs away.
               MPECHOAROUND $n $I punches $l fist RIGHT INTO $n!
               MPDAMAGE $n 25d15 undefined
               endif
            endif
         endif
      endif
   endif
~
>act_prog p vibrations make you suffer~
if isfight($i)
   if ispc($n)
      if $i.level > 89
         if rand(15)
            quiver $n
            endif
         endif
      if $i.level > 74
         if $i.level < 90
            if rand(10)
               quiver $n
               endif
            endif
         endif
      if $i.level > 51
         if $i.level < 75
            if rand(5)
               quiver $n
               endif
            endif
         endif
      endif
   endif
~
>act_prog p slams a bolt of focused ki energy into the flow of magic around you!~
if isfight($i)
   if ispc($n)
      if $i.level > 89
         if rand(15)
            say Your kung fu is pretty good.
            if rand(50)
               ki blast $n
               else
               stun $n
               endif
            endif
         endif
     if $i.level > 74
        if $i.level < 90
           if rand(10)
              say Your kung fu is pretty good.
              if rand(50)
                 ki blast $n
                 else
                 stun $n
                 endif
              endif
           endif
        endif
     if $i.level > 51
        if $i.level < 75
           if rand(5)
              say Your kung fu is pretty good.
              if rand(50)
                 ki blast $n
                 else
                 stun $n
                 endif
              endif
           endif
        endif
      endif
   endif
~
>act_prog p blasts you across the room, causing you to fall~
if isfight($i)
   if ispc($n)
      if $i.level > 89
         if rand(15)
            say Your kung fu is pretty good.
            ki blast $n
            endif
         endif
     if $i.level > 74
        if $i.level < 90
           if rand(10)
              say Your kung fu is pretty good.
              ki blast $n
              endif
           endif
        endif
     if $i.level > 51
        if $i.level < 75
           if rand(5)
              say Your kung fu is pretty good.
              ki blast $n
              endif
           endif
        endif
      endif
   endif
~
>act_prog p focuses and punches you square in the chest~
if isfight($i)
   if ispc($n)
      if $i.level > 89
         if rand(15)
            say Your kung fu isn't half bad.
            MPECHOAT $n $I shoves $l hand into your chest, and your lifeblood ebbs away.
            MPECHOAROUND $n $I punches $l fist RIGHT INTO $n!
MPDAMAGE $n 1d1% undefined
            MPDAMAGE $n 45d15 undefined
            endif
         endif
      if $i.level > 74
         if $i.level < 90
            if rand(10)
               say Your kung fu isn't half bad.
               MPECHOAT $n $I shoves $l hand into your chest, and your lifeblood ebbs away.
               MPECHOAROUND $n $I punches $l fist RIGHT INTO $n!
MPDAMAGE $n 1d1% undefined
               MPDAMAGE $n 35d15 undefined
               endif
            endif
         endif
      if $i.level > 51
         if $i.level < 75
            if rand(5)
say Your kung fu isn't half bad.
               MPECHOAT $n $I shoves $l hand into your chest, and your lifeblood ebbs away.
               MPECHOAROUND $n $I punches $l fist RIGHT INTO $n!
               MPDAMAGE $n 25d15 undefined
               endif
            endif
         endif
      endif
   endif
~
>act_prog p sings a witty little limerick to you!~
if isfight($i)
   if ispc($n)
      if $i.level > 89
         if rand(15)
            say That caterwauling of yours is disturbing my meditation.
            if rand(50)
               ki blast $n
               else
               redirect $n
               endif
            endif
         endif
     if $i.level > 74
        if $i.level < 90
           if rand(10)
              say That caterwauling of yours is disturbing my meditation.
              if rand(50)
                 ki blast $n
                 else
                 redirect $n
                 endif
              endif
           endif
        endif
     if $i.level > 51
        if $i.level < 75
           if rand(5)
              say That caterwauling of yours is disturbing my meditation.
              if rand(50)
                 ki blast $n
                 else
                 redirect $n
                 endif
              endif
           endif
        endif
      endif
   endif
~
>act_prog p lashes out quickly and cripples you with a painful blow!~
if isfight($i)
   if ispc($n)
      if $i.level > 89
         if rand(15)
            if rand(33)
               stun $n
               else
               if rand(50)
                  kick $n
                  else
                  eagleclaw $n
                  endif
               endif
            endif
         endif
      if $i.level > 74
         if $i.level < 90
            if rand(10)
               if rand(33)
                  stun $n
                  else
                  if rand(50)
                     kick $n
                     else
                     eagleclaw $n
                     endif
                  endif
               endif
            endif
         endif
      if $i.level > 51
         if $i.level < 75
            if rand(5)
               if rand(33)
                  stun
                  else
                  if rand(50)
                     kick $n
                     else
                     eagleclaw $n
                     endif
                  endif
               endif
            endif
         endif
      endif
   endif
~
>act_prog p with lethal accuracy and strength.~
if isfight($i)
   if ispc($n)
      if $i.level > 89
         if rand(15)
            MPECHOAT $n $I shoves $l hand into your chest, and your lifeblood ebbs away.
            MPECHOAROUND $n $I punches $l fist RIGHT INTO $n!
MPDAMAGE $n 1d1% undefined
            MPDAMAGE $n 45d15 undefined
            endif
         endif
      if $i.level > 74
         if $i.level < 90
            if rand(10)
               MPECHOAT $n $I shoves $l hand into your chest, and your lifeblood ebbs away.
               MPECHOAROUND $n $I punches $l fist RIGHT INTO $n!
MPDAMAGE $n 1d1% undefined
               MPDAMAGE $n 35d15 undefined
               endif
            endif
         endif
      if $i.level > 51
         if $i.level < 75
            if rand(5)
               MPECHOAT $n $I shoves $l hand into your chest, and your lifeblood ebbs away.
               MPECHOAROUND $n $I punches $l fist RIGHT INTO $n!
               MPDAMAGE $n 25d15 undefined
               endif
            endif
         endif
      endif
   endif
~
>act_prog p circles around~
if isfight($i)
   if ispc($n)
      if $i.level > 89
         if rand(15)
            if rand(50)
               ki blast $n
               else
               stun $n
               endif
            endif
         endif
     if $i.level > 74
        if $i.level < 90
           if rand(10)
              if rand(50)
                 ki blast $n
                 else
                 stun $n
                 endif
              endif
           endif
        endif
     if $i.level > 51
        if $i.level < 75
           if rand(5)
              if rand(50)
                 ki blast $n
                 else
                 stun $n
                 endif
              endif
           endif
        endif
      endif
   endif
~
>act_prog p utters the words, 'unmanifgz'~
if isfight($i)
   if ispc($n)
      if $i.level > 89
         if rand(15)
            if rand(50)
               ki blast $n
               else
               stun $n
               endif
            endif
         endif
     if $i.level > 74
        if $i.level < 90
           if rand(10)
              if rand(50)
                 ki blast $n
                 else
                 stun $n
                 endif
              endif
           endif
        endif
     if $i.level > 51
        if $i.level < 75
           if rand(5)
              if rand(50)
                 ki blast $n
                 else
                 stun $n
                 endif
              endif
           endif
        endif
      endif
   endif
~
>act_prog p utters the words, 'ubralicra'~
if isfight($i)
   if ispc($n)
      if $i.level > 89
         if rand(15)
            ki storm
            endif
         endif
     if $i.level > 74
        if $i.level < 90
           if rand(10)
              ki storm
              endif
           endif
        endif
     if $i.level > 51
        if $i.level < 75
           if rand(5)
              ki storm
              endif
           endif
        endif
      endif
   endif
~
>act_prog p utters the words, 'qahijf gsfal'~
if isfight($i)
   if ispc($n)
      if $i.level > 89
         if rand(15)
            if rand(33)
               stun $n
               else
               if rand(50)
                  kick $n
                  else
                  eagleclaw $n
                  endif
               endif
            endif
         endif
      if $i.level > 74
         if $i.level < 90
            if rand(10)
               if rand(33)
                  stun $n
                  else
                  if rand(50)
                     kick $n
                     else
                     eagleclaw $n
                     endif
                  endif
               endif
            endif
         endif
      if $i.level > 51
         if $i.level < 75
            if rand(5)
               if rand(33)
                  stun
                  else
                  if rand(50)
                     kick $n
                     else
                     eagleclaw $n
                     endif
                  endif
               endif
            endif
         endif
      endif
   endif
~
>act_prog p utters the words, 'qarr solilla'~
if isfight($i)
   if ispc($n)
      if $i.level > 89
         if rand(15)
            MPECHOAT $n $I shoves $l hand into your chest, and your lifeblood ebbs away.
            MPECHOAROUND $n $I punches $l fist RIGHT INTO $n!
MPDAMAGE $n 1d1% undefined
            MPDAMAGE $n 45d15 undefined
            endif
         endif
      if $i.level > 74
         if $i.level < 90
            if rand(10)
               MPECHOAT $n $I shoves $l hand into your chest, and your lifeblood ebbs away.
               MPECHOAROUND $n $I punches $l fist RIGHT INTO $n!
MPDAMAGE $n 1d1% undefined
               MPDAMAGE $n 35d15 undefined
               endif
            endif
         endif
      if $i.level > 51
         if $i.level < 75
            if rand(5)
               MPECHOAT $n $I shoves $l hand into your chest, and your lifeblood ebbs away.
               MPECHOAROUND $n $I punches $l fist RIGHT INTO $n!
               MPDAMAGE $n 25d15 undefined
               endif
            endif
         endif
      endif
   endif
~
>act_prog p utters the words, 'gji brul'~
if isfight($i)
   if ispc($n)
      if $i.level > 89
         if rand(15)
            MPECHOAT $n $I shoves $l hand into your chest, and your lifeblood ebbs away.
            MPECHOAROUND $n $I punches $l fist RIGHT INTO $n!
MPDAMAGE $n 1d1% undefined
            MPDAMAGE $n 45d15 undefined
            endif
         endif
      if $i.level > 74
         if $i.level < 90
            if rand(10)
               MPECHOAT $n $I shoves $l hand into your chest, and your lifeblood ebbs away.
               MPECHOAROUND $n $I punches $l fist RIGHT INTO $n!
MPDAMAGE $n 1d1% undefined
               MPDAMAGE $n 35d15 undefined
               endif
            endif
         endif
      if $i.level > 51
         if $i.level < 75
            if rand(5)
               MPECHOAT $n $I shoves $l hand into your chest, and your lifeblood ebbs away.
               MPECHOAROUND $n $I punches $l fist RIGHT INTO $n!
MPDAMAGE $n 1d1% undefined
               MPDAMAGE $n 25d15 undefined
               endif
            endif
         endif
      endif
   endif
~
>act_prog p utters the words, 'efaxi'~
if isfight($i)
   if ispc($n)
      if $i.level > 89
         if rand(15)
            MPECHOAT $n $I shoves $l hand into your chest, and your lifeblood ebbs away.
            MPECHOAROUND $n $I punches $l fist RIGHT INTO $n!
MPDAMAGE $n 1d1% undefined
            MPDAMAGE $n 45d15 undefined
            endif
         endif
      if $i.level > 74
         if $i.level < 90
            if rand(10)
               MPECHOAT $n $I shoves $l hand into your chest, and your lifeblood ebbs away.
               MPECHOAROUND $n $I punches $l fist RIGHT INTO $n!
MPDAMAGE $n 1d1% undefined
               MPDAMAGE $n 35d15 undefined
               endif
            endif
         endif
      if $i.level > 51
         if $i.level < 75
            if rand(5)
               MPECHOAT $n $I shoves $l hand into your chest, and your lifeblood ebbs away.
               MPECHOAROUND $n $I punches $l fist RIGHT INTO $n!
               MPDAMAGE $n 25d15 undefined
               endif
            endif
         endif
      endif
   endif
~
>act_prog p deathstroke to your kidneys!   AARRRGGGGHHHH!~
if isfight($i)
   if ispc($n)
      if $i.level > 89
         if rand(15)
            MPECHOAT $n $I shoves $l hand into your chest, and your lifeblood ebbs away.
            MPECHOAROUND $n $I punches $l fist RIGHT INTO $n!
MPDAMAGE $n 1d1% undefined
            MPDAMAGE $n 45d15 undefined
            endif
         endif
      if $i.level > 74
         if $i.level < 90
            if rand(10)
               MPECHOAT $n $I shoves $l hand into your chest, and your lifeblood ebbs away.
               MPECHOAROUND $n $I punches $l fist RIGHT INTO $n!
MPDAMAGE $n 1d1% undefined
               MPDAMAGE $n 35d15 undefined
               endif
            endif
         endif
      if $i.level > 51
         if $i.level < 75
            if rand(5)
               MPECHOAT $n $I shoves $l hand into your chest, and your lifeblood ebbs away.
               MPECHOAROUND $n $I punches $l fist RIGHT INTO $n!
               MPDAMAGE $n 25d15 undefined
               endif
            endif
         endif
      endif
   endif
~
>act_prog p deathstroke and falls to the ground.~
if isfight($i)
   if ispc($n)
      if $i.level > 89
         if rand(15)
            say Your clumsiness is really screwing up the feng shui of the place.
            MPECHOAT $n $I shoves $l hand into your chest, and your lifeblood ebbs away.
            MPECHOAROUND $n $I punches $l fist RIGHT INTO $n!
MPDAMAGE $n 1d1% undefined
            MPDAMAGE $n 45d15 undefined
            endif
         endif
      if $i.level > 74
         if $i.level < 90
            if rand(10)
               say Your clumsiness is really screwing up the feng shui of the place.
               MPECHOAT $n $I shoves $l hand into your chest, and your lifeblood ebbs away.
               MPECHOAROUND $n $I punches $l fist RIGHT INTO $n!
MPDAMAGE $n 1d1% undefined
               MPDAMAGE $n 35d15 undefined
               endif
            endif
         endif
      if $i.level > 51
         if $i.level < 75
            if rand(5)
               say Your clumsiness is really screwing up the feng shui of the place.
               MPECHOAT $n $I shoves $l hand into your chest, and your lifeblood ebbs away.
               MPECHOAROUND $n $I punches $l fist RIGHT INTO $n!
               MPDAMAGE $n 25d15 undefined
               endif
            endif
         endif
      endif
   endif
~
>act_prog p attempts to hit you in your solar plexus! ~
if isfight($i)
   if ispc($n)
      if $i.level > 89
         if rand(15)
            say Your chi is weak, Grasshopper!
            if rand(50)
               ki blast $n
               else
               stun $n
               endif
            endif
         endif
     if $i.level > 74
        if $i.level < 90
           if rand(10)
              say Your chi is weak, Grasshopper!
              if rand(50)
                 ki blast $n
                 else
                 stun $n
                 endif
              endif
           endif
        endif
     if $i.level > 51
        if $i.level < 75
           if rand(5)
              say Your chi is weak, Grasshopper!
              if rand(50)
                 ki blast $n
                 else
                 stun $n
                 endif
              endif
           endif
        endif
      endif
   endif
~
>act_prog p lunges forward in an attempt to smash into you but falls flat on~
if isfight($i)
   if ispc($n)
      if $i.level > 89
         if rand(15)
            say You must try harder, Grasshopper!
            ki blast $n
            endif
         endif
     if $i.level > 74
        if $i.level < 90
           if rand(10)
              say You must try harder, Grasshopper!
              ki blast $n
              endif
           endif
        endif
     if $i.level > 51
        if $i.level < 75
           if rand(5)
              say You must try harder, Grasshopper!
              ki blast $n
              endif
           endif
        endif
      endif
   endif
~
>act_prog p tries to headbutt you, but fails miserably.~
if isfight($i)
   if ispc($n)
      if $i.level > 89
         if rand(15)
            if rand(33)
               stun $n
               else
               if rand(50)
                  kick $n
                  else
                  eagleclaw $n
                  endif
               endif
            endif
         endif
      if $i.level > 74
         if $i.level < 90
            if rand(10)
               if rand(33)
                  stun $n
                  else
                  if rand(50)
                     kick $n
                     else
                     eagleclaw $n
                     endif
                  endif
               endif
            endif
         endif
      if $i.level > 51
         if $i.level < 75
            if rand(5)
               if rand(33)
                  stun
                  else
                  if rand(50)
                     kick $n
                     else
                     eagleclaw $n
                     endif
                  endif
               endif
            endif
         endif
      endif
   endif
~
>act_prog p You are SHOCKED~
if isfight($i)
   if ispc($n)
      if $i.level > 89
         if rand(15)
            if rand(33)
               stun $n
               else
               if rand(50)
                  kick $n
                  else
                  eagleclaw $n
                  endif
               endif
            endif
         endif
      if $i.level > 74
         if $i.level < 90
            if rand(10)
               if rand(33)
                  stun $n
                  else
                  if rand(50)
                     kick $n
                     else
                     eagleclaw $n
                     endif
                  endif
               endif
            endif
         endif
      if $i.level > 51
         if $i.level < 75
            if rand(5)
               if rand(33)
                  stun
                  else
                  if rand(50)
                     kick $n
                     else
                     eagleclaw $n
                     endif
                  endif
               endif
            endif
         endif
      endif
   endif
~
>act_prog p but trips~
if isfight($i)
   if ispc($n)
      if $i.level > 89
         if rand(15)
            say You must try harder, Grasshopper!
            if rand(33)
               stun $n
               else
               if rand(50)
                  kick $n
                  else
                  eagleclaw $n
                  endif
               endif
            endif
         endif
      if $i.level > 74
         if $i.level < 90
            if rand(10)
               say You must try harder, Grasshopper!
               if rand(33)
                  stun $n
                  else
                  if rand(50)
                     kick $n
                     else
                     eagleclaw $n
                     endif
                  endif
               endif
            endif
         endif
      if $i.level > 51
         if $i.level < 75
            if rand(5)
               say You must try harder, Grasshopper!
               if rand(33)
                  stun
                  else
                  if rand(50)
                     kick $n
                     else
                     eagleclaw $n
                     endif
                  endif
               endif
            endif
         endif
      endif
   endif
~
>act_prog p starts swinging like a MADMAN!~
if isfight($i)
   if ispc($n)
      if $i.level > 89
         if rand(15)
            MPECHOAT $n $I shoves $l hand into your chest, and your lifeblood ebbs away.
            MPECHOAROUND $n $I punches $l fist RIGHT INTO $n!
MPDAMAGE $n 1d1% undefined
            MPDAMAGE $n 45d15 undefined
            endif
         endif
      if $i.level > 74
         if $i.level < 90
            if rand(10)
               MPECHOAT $n $I shoves $l hand into your chest, and your lifeblood ebbs away.
               MPECHOAROUND $n $I punches $l fist RIGHT INTO $n!
MPDAMAGE $n 1d1% undefined
               MPDAMAGE $n 35d15 undefined
               endif
            endif
         endif
      if $i.level > 51
         if $i.level < 75
            if rand(5)
               MPECHOAT $n $I shoves $l hand into your chest, and your lifeblood ebbs away.
               MPECHOAROUND $n $I punches $l fist RIGHT INTO $n!
               MPDAMAGE $n 25d15 undefined
               endif
            endif
         endif
      endif
   endif
~
>act_prog p advances confidently towards you, and flies into a rage!~
if isfight($i)
   if ispc($n)
      if $i.level > 89
         if rand(15)
            if rand(33)
               stun $n
               else
               if rand(50)
                  kick $n
                  else
                  eagleclaw $n
                  endif
               endif
            endif
         endif
      if $i.level > 74
         if $i.level < 90
            if rand(10)
               if rand(33)
                  stun $n
                  else
                  if rand(50)
                     kick $n
                     else
                     eagleclaw $n
                     endif
                  endif
               endif
            endif
         endif
      if $i.level > 51
         if $i.level < 75
            if rand(5)
               if rand(33)
                  stun
                  else
                  if rand(50)
                     kick $n
                     else
                     eagleclaw $n
                     endif
                  endif
               endif
            endif
         endif
      endif
   endif
~
>act_prog p utters the words, 'saugai'~
if isfight($i)
   if ispc($n)
      if $i.level > 89
         if rand(15)
            if rand(33)
               stun $n
               else
               if rand(50)
                  kick $n
                  else
                  eagleclaw $n
                  endif
               endif
            endif
         endif
      if $i.level > 74
         if $i.level < 90
            if rand(10)
               if rand(33)
                  stun $n
                  else
                  if rand(50)
                     kick $n
                     else
                     eagleclaw $n
                     endif
                  endif
               endif
            endif
         endif
      if $i.level > 51
         if $i.level < 75
            if rand(5)
               if rand(33)
                  stun
                  else
                  if rand(50)
                     kick $n
                     else
                     eagleclaw $n
                     endif
                  endif
               endif
            endif
         endif
      endif
   endif
~
>act_prog p utters the words, 'qfzzsuio oculoahp'~
if isfight($i)
   if ispc($n)
      if $i.level > 89
         if rand(15)
            if rand(33)
               stun $n
               else
               if rand(50)
                  kick $n
                  else
                  eagleclaw $n
                  endif
               endif
            endif
         endif
      if $i.level > 74
         if $i.level < 90
            if rand(10)
               if rand(33)
                  stun $n
                  else
                  if rand(50)
                     kick $n
                     else
                     eagleclaw $n
                     endif
                  endif
               endif
            endif
         endif
      if $i.level > 51
         if $i.level < 75
            if rand(5)
               if rand(33)
                  stun
                  else
                  if rand(50)
                     kick $n
                     else
                     eagleclaw $n
                     endif
                  endif
               endif
            endif
         endif
      endif
   endif
~
>act_prog p You dodge a bash from~
if isfight($i)
   if ispc($n)
      if $i.level > 89
         if rand(15)
            if rand(33)
               stun $n
               else
               if rand(50)
                  kick $n
                  else
                  eagleclaw $n
                  endif
               endif
            endif
         endif
      if $i.level > 74
         if $i.level < 90
            if rand(10)
               if rand(33)
                  stun $n
                  else
                  if rand(50)
                     kick $n
                     else
                     eagleclaw $n
                     endif
                  endif
               endif
            endif
         endif
      if $i.level > 51
         if $i.level < 75
            if rand(5)
               if rand(33)
                  stun
                  else
                  if rand(50)
                     kick $n
                     else
                     eagleclaw $n
                     endif
                  endif
               endif
            endif
         endif
      endif
   endif
~
>act_prog p utters the words, 'euzuiz yjfl'~
if isfight($i)
   if ispc($n)
      if $i.level > 89
         if rand(15)
            MPECHOAT $n $I shoves $l hand into your chest, and your lifeblood ebbs away.
            MPECHOAROUND $n $I punches $l fist RIGHT INTO $n!
MPDAMAGE $n 1d1% undefined
            MPDAMAGE $n 45d15 undefined
            endif
         endif
      if $i.level > 74
         if $i.level < 90
            if rand(10)
               MPECHOAT $n $I shoves $l hand into your chest, and your lifeblood ebbs away.
               MPECHOAROUND $n $I punches $l fist RIGHT INTO $n!
MPDAMAGE $n 1d1% undefined
               MPDAMAGE $n 35d15 undefined
               endif
            endif
         endif
      if $i.level > 51
         if $i.level < 75
            if rand(5)
               MPECHOAT $n $I shoves $l hand into your chest, and your lifeblood ebbs away.
               MPECHOAROUND $n $I punches $l fist RIGHT INTO $n!
               MPDAMAGE $n 25d15 undefined
               endif
            endif
         endif
      endif
   endif
~
>act_prog p utters the words, 'garandoa oahz'~
if isfight($i)
   if ispc($n)
      if $i.level > 89
         if rand(15)
            if rand(50)
               ki blast $n
               else
               redirect $n
               endif
            endif
         endif
     if $i.level > 74
        if $i.level < 90
           if rand(10)
              if rand(50)
                 ki blast $n
                 else
                 redirect $n
                 endif
              endif
           endif
        endif
     if $i.level > 51
        if $i.level < 75
           if rand(5)
              if rand(50)
                 ki blast $n
                 else
                 redirect $n
                 endif
              endif
           endif
        endif
      endif
   endif
~
>act_prog p utters the words, 'wzhzaf gxandoaw'~
if isfight($i)
   if ispc($n)
      if $i.level > 89
         if rand(15)
            if rand(33)
               stun $n
               else
               if rand(50)
                  kick $n
                  else
                  eagleclaw $n
                  endif
               endif
            endif
         endif
      if $i.level > 74
         if $i.level < 90
            if rand(10)
               if rand(33)
                  stun $n
                  else
                  if rand(50)
                     kick $n
                     else
                     eagleclaw $n
                     endif
                  endif
               endif
            endif
         endif
      if $i.level > 51
         if $i.level < 75
            if rand(5)
               if rand(33)
                  stun
                  else
                  if rand(50)
                     kick $n
                     else
                     eagleclaw $n
                     endif
                  endif
               endif
            endif
         endif
      endif
   endif
~
>act_prog p utters the words, 'yufzghafw'~
if isfight($i)
   if ispc($n)
      if $i.level > 89
         if rand(15)
            if rand(33)
               stun $n
               else
               if rand(50)
                  kick $n
                  else
                  eagleclaw $n
                  endif
               endif
            endif
         endif
      if $i.level > 74
         if $i.level < 90
            if rand(10)
               if rand(33)
                  stun $n
                  else
                  if rand(50)
                     kick $n
                     else
                     eagleclaw $n
                     endif
                  endif
               endif
            endif
         endif
      if $i.level > 51
         if $i.level < 75
            if rand(5)
               if rand(33)
                  stun
                  else
                  if rand(50)
                     kick $n
                     else
                     eagleclaw $n
                     endif
                  endif
               endif
            endif
         endif
      endif
   endif
~
>act_prog p utters the words, 'sandoaarlkz'~
if isfight($i)
   if ispc($n)
      if $i.level > 89
         if rand(15)
            if rand(50)
               ki blast $n
               else
               redirect $n
               endif
            endif
         endif
     if $i.level > 74
        if $i.level < 90
           if rand(10)
              if rand(50)
                 ki blast $n
                 else
                 redirect $n
                 endif
              endif
           endif
        endif
     if $i.level > 51
        if $i.level < 75
           if rand(5)
              if rand(50)
                 ki blast $n
                 else
                 redirect $n
                 endif
              endif
           endif
        endif
      endif
   endif
~
>act_prog p utters the words, 'pzrrghxanduaw'~
if isfight($i)
   if ispc($n)
      if $i.level > 89
         if rand(15)
            MPECHOAT $n $I shoves $l hand into your chest, and your lifeblood ebbs away.
            MPECHOAROUND $n $I punches $l fist RIGHT INTO $n!
MPDAMAGE $n 1d1% undefined
            MPDAMAGE $n 45d15 undefined
            endif
         endif
      if $i.level > 74
         if $i.level < 90
            if rand(10)
               MPECHOAT $n $I shoves $l hand into your chest, and your lifeblood ebbs away.
               MPECHOAROUND $n $I punches $l fist RIGHT INTO $n!
MPDAMAGE $n 1d1% undefined
               MPDAMAGE $n 35d15 undefined
               endif
            endif
         endif
      if $i.level > 51
         if $i.level < 75
            if rand(5)
               MPECHOAT $n $I shoves $l hand into your chest, and your lifeblood ebbs away.
               MPECHOAROUND $n $I punches $l fist RIGHT INTO $n!
               MPDAMAGE $n 25d15 undefined
               endif
            endif
         endif
      endif
   endif
~
>act_prog p utters the words, 'eugszr~
if isfight($i)
   if ispc($n)
      if $i.level > 89
         if rand(15)
            if rand(50)
               ki blast $n
               else
               redirect $n
               endif
            endif
         endif
     if $i.level > 74
        if $i.level < 90
           if rand(10)
              if rand(50)
                 ki blast $n
                 else
                 redirect $n
                 endif
              endif
           endif
        endif
     if $i.level > 51
        if $i.level < 75
           if rand(5)
              if rand(50)
                 ki blast $n
                 else
                 redirect $n
                 endif
              endif
           endif
        endif
      endif
   endif
~
>act_prog p utters the words, 'saxzf pandoaw'~
if isfight($i)
   if ispc($n)
      if $i.level > 89
         if rand(15)
            eagleclaw $n
            endif
         endif
     if $i.level > 74
        if $i.level < 90
           if rand(10)
              eagleclaw $n
              endif
           endif
        endif
     if $i.level > 51
        if $i.level < 75
           if rand(5)
              eagleclaw $n
              endif
           endif
        endif
      endif
   endif
~
>act_prog p utters the words, 'yrawzghfutz'~
if isfight($i)
   if ispc($n)
      if $i.level > 89
         if rand(15)
            stun $n
            endif
         endif
     if $i.level > 74
        if $i.level < 90
           if rand(10)
              stun $n
              endif
           endif
        endif
     if $i.level > 51
        if $i.level < 75
           if rand(5)
              stun $n
              endif
           endif
        endif
      endif
   endif
~
>act_prog p utters the words, 'pzar gsfal'~
if isfight($i)
   if ispc($n)
      if $i.level > 89
         if rand(15)
            say You spread your karma like a diseased whore spreads her legs.
            redirect $n
            endif
         endif
     if $i.level > 74
        if $i.level < 90
           if rand(10)
              say You spread your karma like a diseased whore spreads her legs.
              redirect $n
              endif
           endif
        endif
     if $i.level > 51
        if $i.level < 75
           if rand(5)
              say You spread your karma like a diseased whore spreads her legs.
              redirect $n
              endif
           endif
        endif
      endif
   endif
~
>arand_prog 1~
ki meditation
ki speed
ki purify
~
>fight_prog 100~
if isspelled($n) sanctuary
    ki disrupt $n
else
  if isspelled($n) fireshield
    ki disrupt $n
  else
    if isspelled($n) stoneshield
      ki disrupt $n
    else
      if isspelled($n) lightning_shield
        ki disrupt $n
      else
        if isspelled($n) acid_shield
          ki disrupt $n
        endif
      endif
    endif
  endif
endif
~
>fight_prog 10~
if $i.level > 74
   if rand(50)
      kick
      else
      trip
      endif
   endif
if $i.level > 51
   if $i.level < 75
      if rand(50)
         eagleclaw
         else
         stun
         endif
      endif
   endif
~
>fight_prog 7~
if $i.level > 74
   if rand(50)
      eagleclaw
      else
      stun
      endif
   endif
if $i.level > 51
   if $i.level < 75
      if rand(50)
         kick
         else
         trip
         endif
      endif
   endif
~
>fight_prog 5~
if $i.level > 60
   MPECHOAT $n $I shoves their hand into your chest, and your lifeblood ebbs away.
   MPECHOAROUND $n $I punches their fist RIGHT INTO $n!
MPDAMAGE $n 1d1% undefined
   MPDAMAGE $n 40d10 undefined
   endif
if $i.level > 55
   if $i.level < 61
      MPECHOAT $n $I shoves their hand into your chest, and your lifeblood ebbs away.
      MPECHOAROUND $n $I punches their fist RIGHT INTO $n!
MPDAMAGE $n 1d1% undefined
      MPDAMAGE $n 30d10 undefined
      endif
   endif
if $i.level > 50
   if $i.level < 56
      MPECHOAT $n $I shoves their hand into your chest, and your lifeblood ebbs away.
      MPECHOAROUND $n $I punches their fist RIGHT INTO $n!
      MPDAMAGE $n 25d11 undefined
      endif
   endif
~
>fight_prog 3~
ki stance
~
>fight_prog 2~
quiver
~
|
S
#121
skillset mob~
PROC MOB THIEF LEVEL 1-20~
This is a Skillset Mob for the Thief Class, levels 1-20.
~
~
262400 0 -1
2621440 0 0 -1
0 0 0
20 10 0d1+0 1d1+0
0 0
8 8 0 0 0 0
>fight_prog 15~
trip
~
|
S
#122
skillset mob~
PROC MOB THIEF LEVEL 21-35~
This is a Skillset Mob for the Thief Class, levels 21-35.
~
~
262400 0 -1
2621440 0 0 -1
0 0 0
20 10 0d1+0 1d1+0
0 0
8 8 0 0 0 0
>arand_prog 5~
hide
~
>fight_prog 15~
eyegouge
~
>fight_prog 15~
trip
~
|
S
#123
skillset mob~
PROC MOB THIEF LEVEL 36-50~
This is a Skillset Mob for the Thief Class, levels 36-50.
~
~
262400 0 -1
2621440 0 0 -1
0 0 0
20 10 0d1+0 1d1+0
0 0
8 8 0 0 0 0
>arand_prog 5~
hide
~
>fight_prog 15~
disarm
~
>fight_prog 15~
eyegouge
~
>fight_prog 10~
trip
~
|
S
#124
skillset mob~
PROC MOB THIEF LEVEL 51+~
This is a Skillset Mob for the Thief Class, levesl 51+.
~
~
262400 0 -1
2621440 4096 0 -1
0 0 0
20 10 500d1+0 1d1+0
0 0
8 8 0 0 0 0
>act_prog null~
if ispc($n)
MPSETTEMP $i bstarg null
endif
~
>hitprcnt_prog 50~
if isfight($i)
if $i.level > 50
    if $i.level < 75
        if rand(25)
            flee
            endif
         endif
      endif
endif
~
>hitprcnt_prog 25~
if isfight($i)
if $i.level > 89
    if rand(5)
        flee
        endif
    endif
if $i.level > 74
    if $i.level < 90
       if rand(15)
          flee
          endif
       endif
    endif
if $i.level > 50
    if $i.level < 75
        if rand(25)
            flee
            endif
         endif
      endif
endif
~
>attack_prog 100~
if iswielding($i)
or isspelled($n) alert
   hide
   else
   if rand(1)
      MPECHOAT $n The world turns to black as $I slips a blade between your ribs, piercing your heart.
      MPECHOAROUND $n $n makes a muffled groan as $I thrusts a blade between $n's ribs, piercing $s heart.
      MPDAMAGE $n 100000d1 undefined
      else
      if $i.level > 89
         if rand(85)
            MPECHOAT $n $I pulls out a concealed blade and stabs you in the back.
            MPECHOAROUND $n $n makes a strange gargling sound as $I pulls out a concealed blade and thrusts it in $s back.
            MPDAMAGE $n 1d1% undefined
            MPDAMAGE $n 80d10 undefined
            else
            MPECHOAT $n $I clumsily tries to backstab you, but you easily avoid $l attempt.
            MPECHOAAROUND $n $I tries to backstab $n, but is exposed!
            endif
         endif
      if $i.level > 74
         if $i.level < 90
            if rand(85)
               if rand(80)
                  MPECHOAT $n $I pulls out a concealed blade and stabs you in the back.
                  MPECHOAROUND $n $n makes a strange gargling sound as $I pulls out a concealed blade and thrusts it in $s back.
                  MPDAMAGE $n 1d1% undefined
                  MPDAMAGE $n 65d10 undefined
                  else
                  MPECHOAT $n $I clumsily tries to backstab you, but you easily avoid $l attempt.
                  MPECHOAAROUND $n $I tries to backstab $n, but is exposed!
                  endif
               endif
            endif
         endif
      if $i.level > 50
         if $i.level < 75
            if rand(75)
               if rand(75)
                  MPECHOAT $n $I pulls out a concealed blade and stabs you in the back.
                  MPECHOAROUND $n $n makes a strange gargling sound as $I pulls out a concealed blade and thrusts it in $s back.
                  MPDAMAGE $n 50d10 undefined
                  else
                  MPECHOAT $n $I clumsily tries to backstab you, but you easily avoid $l attempt.
                  MPECHOAAROUND $n $I tries to backstab $n, but is exposed!
                  endif
               endif
            endif
         endif
      endif
   endif
~
>fight_prog 10~
if cansee($n)
MPECHOAT $n $I pulls out a concealed knife, quickly darts behind you and STABS you in the back!
MPECHOAROUND $n $I pulls out a concealed knife, quickly darts around $n and STABS $m in the back!
MPDAMAGE $n 1d1% undefined
MPDAMAGE $n 25d10 undefined
endif
~
>fight_prog 10~
if iswielding($n)
   disarm
   else
   trip
endif
~
>fight_prog 10~
eyegouge
~
>arand_prog 5~
hide
~
>fight_prog 10~
if cansee($n)
MPECHO $I begins jabbing $l weapons with lethal accuracy and strength.
MPECHOAT $n $I's lethal strike causes CRITICAL damage!
MPECHOAROUND $n $I's lethal strike causes CRITICAL damage to $n!
MPDAMAGE $n 1d1% undefined
MPDAMAGE $n 25d10 undefined
endif
~
>fight_prog 10~
if cansee($n)
if sex($n) == 1
   MPECHOAT $n $I slips past your guard and delivers a painful knee to the groin, crushing your nutsack!
   MPECHOAROUND $n $I slips past $n's guard and delivers a swift knee to $n's groin, crushing his nutsack!
   endif
if sex($n) == 2
   MPECHOAT $n $I slips past your guard and delivers a swift kick to your clam!
   MPECHOAROUND $n $I slips past $n's guard and delivers a swift kick to $n's clam!
   endif
if sex($n) == 0
   MPECHOAT $n $I slips past your guard and delivers a swift kick to your crotch!
   MPECHOAROUND $n $I slips past $n's guard and delivers a swift kick to $n's crotch!
   endif
if $i.level > 89
MPDAMAGE $n 1d1% undefined
     MPDAMAGE $n 60d5 undefined
     MPADDLAG $n 8
     endif
if $i.level > 74
   if $i.level < 90
MPDAMAGE $n 1d1% undefined
      MPDAMAGE $n 55d5 undefined
      MPADDLAG $n 8
      endif
   endif
if $i.level > 51
   if $i.level < 75
      MPDAMAGE $n 50d5 undefined
      MPADDLAG $n 8
      endif
   endif
endif
~
>fight_prog 8~
if cansee($n)
   MPECHOAT $n $I pulls a glass vial from a hidden pocket and hurls it at your face.
   MPECHOAROUND $n $I pulls a glass vial from a hidden pocket and hurls it at $n's face.
   MPECHO The vial explodes in a shower of glass and $$2acid$$R!
if $i.level > 89
MPDAMAGE $n 1d1% undefined
   MPDAMAGE $n 50d5 acid
   endif
if $i.level > 74
   if $i.level < 90
MPDAMAGE $n 1d1% undefined
      MPDAMAGE $n 45d5 acid
      endif
   endif
if $i.level > 51
   if $i.level < 75
      MPDAMAGE $n 40d5 acid
      endif
   endif
endif
~
>fight_prog 5~
if cansee($n)
MPECHOAT $n $I pulls a handful of poisonous darts from a concealed holster and throws them at you!
MPECHOAROUND $n $I pulls a handful of poisonous darts from a concealed holster and throws them at $n!
if $i.level > 89
MPDAMAGE $n 1d1% undefined
   MPDAMAGE $n 45d5 poison
   endif
if $i.level > 74
   if $i.level < 90
MPDAMAGE $n 1d1% undefined
      MPDAMAGE $n 40d5 poison
      endif
   endif
if $i.level > 51
   if $i.level < 75
      MPDAMAGE $n 35d5 poison
      endif
   endif
MPBESTOW $n poison 4 2
endif
~
>act_prog null~
if tempvar($i[bstarg]) != null
   MPPAUSE 1
   MPGOTO $v[bstarg]
   if iswielding($i)
      or isspelled($v[bstarg]) alert
      hide
      else
      if rand(1)
         MPECHOAT $v[bstarg] The world turns to black as $I slips a blade between your ribs, piercing your heart.
         MPECHOAROUND $v[bstarg] $v[bstarg] makes a muffled groan as $I thrusts a blade between $v[bstarg]'s ribs, piercing $s heart.
        MPDAMAGE $v[bstarg] 100000d1 undefined
        else
        if $i.level > 89
           if rand(85)
              MPECHOAT $v[bstarg] $I pulls out a concealed blade and stabs you in the back.
              MPECHOAROUND $v[bstarg] $v[bstarg] makes a strange gargling sound as $I pulls out a concealed blade and thrusts it into $s back.
              MPDAMAGE $v[bstarg] 1d1% undefined
              MPDAMAGE $v[bstarg] 80d10 undefined
              kill $v[bstarg]
              else
              MPECHOAT $v[bstarg] $I clumsily tries to backstab you, but you easily avoid $l attempt.
              MPECHOAROUND $v[bstarg] $I tries to backstab $v[bstarg], but is exposed!
              kill $v[bstarg]
              endif
           endif
        if $i.level > 74
           if $i.level < 90
              if rand(80)
                 MPECHOAT $v[bstarg] $I pulls out a concealed blade and stabs you in the back.
                 MPECHOAROUND $v[bstarg] $v[bstarg] makes a strange gargling sound as $I pulls out a concealed blade and thrusts it into $s back.
                 MPDAMAGE $v[bstarg] 1d1% undefined
                 MPDAMAGE $v[bstarg] 65d10 undefined
                 kill $v[bstarg]
                 else
                 MPECHOAT $v[bstarg] $I clumsily tries to backstab you, but you easily avoid $l attempt.
                 MPECHOAROUND $v[bstarg] $I tries to backstab $v[bstarg], but is exposed!
                 kill $v[bstarg]
                 endif
              endif
           endif
        if $i.level > 50
           if $i.level < 75
              if rand(75)
                 MPECHOAT $v[bstarg] $I pulls out a concealed blade and stabs you in the back.
                 MPECHOAROUND $v[bstarg] $v[bstarg] makes a strange gargling sound as $I pulls out a concealed blade and thrusts it into $s back.
                 MPDAMAGE $v[bstarg] 50d10 undefined
                 kill $v[bstarg]
                 else
                 MPECHOAT $v[bstarg] $I clumsily tries to backstab you, but you easily avoid $l attempt.
                 MPECHOAROUND $v[bstarg] $I tries to backstab $v[bstarg], but is exposed!
                 kill $v[bstarg]
                 endif
              endif
           endif
        endif
     endif
  endif
~
|
S
#125
skillset mob~
PROC MOB PALADIN LEVEL 1-20~
This is a Skillset Mob for the Paladin Class, levesl 1-20.
~
~
768 0 -1
2097192 0 0 -1
0 0 0
20 10 0d1+0 1d1+0
0 0
8 8 0 0 0 0
>load_prog 100~
cast 'bless' $i
~
>hitprcnt_prog 50~
if rand(25)
  cast 'cure serious' $i
endif
~
>fight_prog 10~
bash
~
>fight_prog 10~
kick
~
>arand_prog 1~
if isspelled($i) bless
  else
    cast 'bless' $i
  break
endif
~
|
S
#126
skillset mob~
PROC MOB PALADIN LEVEL 21-35~
This is a Skillset Mob for the Paladin Class, levels 21-35.
~
~
525056 0 -1
2097192 0 0 -1
0 0 0
20 10 0d1+0 1d1+0
0 0
8 8 0 0 0 0
>load_prog 100~
cast 'bless' $i
cast 'strength' $i
~
>fight_prog 10~
cast 'earthquake'
~
>fight_prog 10~
bash
~
>fight_prog 10~
kick
~
>hitprcnt_prog 50~
if rand(25)
  cast 'cure critic' $i
endif
~
>arand_prog 1~
if isspelled($i) bless
  else
    cast 'bless' $i
  break
endif
if isspelled($i) strength
  else
    cast 'strength' $i
  break
endif
~
|
S
#127
skillset mob~
PROC MOB PALADIN LEVEL 36-50~
This is a Skillset Mob for the Paladin Class, levels 36-50.
~
~
526080 0 -1
2097192 0 0 -1
0 0 0
20 10 0d1+0 1d1+0
0 0
8 8 0 0 0 0
>load_prog 100~
cast 'bless' $i
cast 'strength' $i
cast 'armor' $i
cast 'sanctuary' $i
~
>fight_prog 15~
cast 'earthquake'
~
>fight_prog 15~
bash
~
>fight_prog 10~
kick
~
>hitprcnt_prog 50~
if rand(25)
  cast 'heal' $i
endif
~
>arand_prog 1~
if isspelled($i) bless
  else
    cast 'bless' $i
  break
endif
if isspelled($i) strength
  else
    cast 'strength' $i
  break
endif
if isspelled($i) armor
  else
    cast 'armor' $i
  break
endif
if isspelled($i) sanctuary
  else
    cast 'sanctuary' $i
  break
endif
~
|
S
#128
skillset mob~
PROC MOB PALADIN LEVEL 51+~
This is a Skillset Mob for the Paladin Class, levels 51+.
~
~
526080 0 -1
2097192 0 0 -1
0 0 0
20 10 0d1+0 1d1+0
0 0
8 8 0 0 0 0
>act_prog p vibrations make you suffer~
if isfight($i)
   if ispc($n)
      if $i.level > 89
         if rand(15)
            MPECHOAT $n $I conjures a hammer of pure spiritual energy and hurls it at you!
            MPECHOAT $n The hammer disperses into the air as it slams into you.
            MPECHOAROUND $n $I conjures a hammer of pure spiritual energy and hurls it at $n!
            MPECHOAROUND $n The hammer disperses into the air as it slams into $n's face.
            MPDAMAGE $n 1d1% undefined
            if isgood($n)
               MPDAMAGE $n 30d5 energy
               else
               MPDAMAGE $n 40d5 energy
               endif
            endif
         endif
      if $i.level > 74
         if $i.level < 90
            if rand(10)
               MPECHOAT $n $I conjures a hammer of pure spiritual energy and hurls it at you!
               MPECHOAT $n The hammer disperses into the air as it slams into you.
               MPECHOAROUND $n $I conjures a hammer of pure spiritual energy and hurls it at $n!
               MPECHOAROUND $n The hammer disperses into the air as it slams into $n's face.
               MPDAMAGE $n 1d1% undefined
               if isgood($n)
                  MPDAMAGE $n 30d5 energy
                  else
                  MPDAMAGE $n 40d5 energy
                  endif
               endif
            endif
         endif
      if $i.level > 50
         if $i.level < 75
            if rand(5)
               MPECHOAT $n $I conjures a hammer of pure spiritual energy and hurls it at you!
               MPECHOAT $n The hammer disperses into the air as it slams into you.
               MPECHOAROUND $n $I conjures a hammer of pure spiritual energy and hurls it at $n!
               MPECHOAROUND $n The hammer disperses into the air as it slams into $n's face.
               if isgood($n)
                  MPDAMAGE $n 30d5 energy
                  else
                  MPDAMAGE $n 40d5 energy
                  endif
               endif
            endif
         endif
      endif
   endif
~
>act_prog p utters the words, 'yufzghafw'~
if isfight($i)
   if ispc($n)
      if $i.level > 89
         if rand(15)
            bash $n
            endif
         endif
      if $i.level > 74
         if $i.level < 90
            if rand(10)
               bash $n
               endif
            endif
         endif
      if $i.level > 50
         if $i.level < 75
            if rand(5)
               bash $n
               endif
            endif
         endif
      endif
   endif
~
>act_prog p deathstroke to your kidneys!   AARRRGGGGHHHH!~
if isfight($i)
   if ispc($n)
      if $i.level > 89
         if rand(15)
            MPECHOAT $n $I conjures a hammer of pure spiritual energy and hurls it at you!
            MPECHOAT $n The hammer disperses into the air as it slams into you.
            MPECHOAROUND $n $I conjures a hammer of pure spiritual energy and hurls it at $n!
            MPECHOAROUND $n The hammer disperses into the air as it slams into $n's face.
            MPDAMAGE $n 1d1% undefined
            if isgood($n)
               MPDAMAGE $n 30d5 energy
               else
               MPDAMAGE $n 40d5 energy
               endif
            endif
         endif
      if $i.level > 74
         if $i.level < 90
            if rand(10)
               MPECHOAT $n $I conjures a hammer of pure spiritual energy and hurls it at you!
               MPECHOAT $n The hammer disperses into the air as it slams into you.
               MPECHOAROUND $n $I conjures a hammer of pure spiritual energy and hurls it at $n!
               MPECHOAROUND $n The hammer disperses into the air as it slams into $n's face.
               MPDAMAGE $n 1d1% undefined
               if isgood($n)
                  MPDAMAGE $n 30d5 energy
                  else
                  MPDAMAGE $n 40d5 energy
                  endif
               endif
            endif
         endif
      if $i.level > 50
         if $i.level < 75
            if rand(5)
               MPECHOAT $n $I conjures a hammer of pure spiritual energy and hurls it at you!
               MPECHOAT $n The hammer disperses into the air as it slams into you.
               MPECHOAROUND $n $I conjures a hammer of pure spiritual energy and hurls it at $n!
               MPECHOAROUND $n The hammer disperses into the air as it slams into $n's face.
               if isgood($n)
                  MPDAMAGE $n 30d5 energy
                  else
                  MPDAMAGE $n 40d5 energy
                  endif
               endif
            endif
         endif
      endif
   endif
~
>act_prog p utters the words, 'garandoa oahz'~
if isfight($i)
   if ispc($n)
      if $i.level > 89
         if rand(15)
            bash $n
            endif
         endif
      if $i.level > 74
         if $i.level < 90
            if rand(10)
               bash $n
               endif
            endif
         endif
      if $i.level > 50
         if $i.level < 75
            if rand(5)
               bash $n
               endif
            endif
         endif
      endif
   endif
~
>act_prog p with lethal accuracy and strength.~
if isfight($i)
   if ispc($n)
      if $i.level > 89
         if rand(15)
            if isevil($n)
               cast 'dispel evil' $n
               else
               cast 'divine fury' $n
               endif
            endif
         endif
      if $i.level > 74
         if $i.level < 90
            if rand(10)
               if isevil($n)
                  cast 'dispel evil' $n
                  else
                  cast 'divine fury' $n
                  endif
               endif
            endif
         endif
     if $i.level > 50
        if $i.level < 75
           if rand(5)
              if isevil($n)
                 cast 'dispel evil' $n
                 else
                 cast 'divine fury' $n
                 endif
              endif
           endif
        endif
      endif
   endif
~
>act_prog p lashes out quickly and cripples you with a painful blow!~
if isfight($i)
   if ispc($n)
      if $i.level > 89
         if rand(15)
            MPECHOAT $n $I conjures a hammer of pure spiritual energy and hurls it at you!
            MPECHOAT $n The hammer disperses into the air as it slams into you.
            MPECHOAROUND $n $I conjures a hammer of pure spiritual energy and hurls it at $n!
            MPECHOAROUND $n The hammer disperses into the air as it slams into $n's face.
            MPDAMAGE $n 1d1% undefined
            if isgood($n)
               MPDAMAGE $n 30d5 energy
               else
               MPDAMAGE $n 40d5 energy
               endif
            endif
         endif
      if $i.level > 74
         if $i.level < 90
            if rand(10)
               MPECHOAT $n $I conjures a hammer of pure spiritual energy and hurls it at you!
               MPECHOAT $n The hammer disperses into the air as it slams into you.
               MPECHOAROUND $n $I conjures a hammer of pure spiritual energy and hurls it at $n!
               MPECHOAROUND $n The hammer disperses into the air as it slams into $n's face.
               MPDAMAGE $n 1d1% undefined
               if isgood($n)
                  MPDAMAGE $n 30d5 energy
                  else
                  MPDAMAGE $n 40d5 energy
                  endif
               endif
            endif
         endif
      if $i.level > 50
         if $i.level < 75
            if rand(5)
               MPECHOAT $n $I conjures a hammer of pure spiritual energy and hurls it at you!
               MPECHOAT $n The hammer disperses into the air as it slams into you.
               MPECHOAROUND $n $I conjures a hammer of pure spiritual energy and hurls it at $n!
               MPECHOAROUND $n The hammer disperses into the air as it slams into $n's face.
               if isgood($n)
                  MPDAMAGE $n 30d5 energy
                  else
                  MPDAMAGE $n 40d5 energy
                  endif
               endif
            endif
         endif
      endif
   endif
~
>act_prog p slams a bolt of focused ki energy into the flow of magic around you!~
if isfight($i)
   if ispc($n)
      if $i.level > 89
         if rand(15)
            bash $n
            endif
         endif
      if $i.level > 74
         if $i.level < 90
            if rand(10)
               bash $n
               endif
            endif
         endif
      if $i.level > 50
         if $i.level < 75
            if rand(5)
               bash $n
               endif
            endif
         endif
      endif
   endif
~
>act_prog p sings a witty little limerick to you!~
if isfight($i)
   if ispc($n)
      if $i.level > 89
         if rand(15)
            bash $n
            endif
         endif
      if $i.level > 74
         if $i.level < 90
            if rand(10)
               bash $n
               endif
            endif
         endif
      if $i.level > 50
         if $i.level < 75
            if rand(5)
               bash $n
               endif
            endif
         endif
      endif
   endif
~
>act_prog p utters the words, 'zawsufuq hajqp'~
if isfight($i)
   if ispc($n)
      if $i.level > 89
         if rand(15)
               cast 'dispel evil' $n
            endif
         endif
      if $i.level > 74
         if $i.level < 90
            if rand(10)
                  cast 'dispel evil' $n
               endif
            endif
         endif
     if $i.level > 50
        if $i.level < 75
           if rand(5)
                 cast 'dispel evil' $n
              endif
           endif
        endif
      endif
   endif
~
>act_prog p utters the words, 'aque bragh'~
if isfight($i)
   if ispc($n)
      if $i.level > 89
         if rand(15)
               cast 'dispel evil' $n
            endif
         endif
      if $i.level > 74
         if $i.level < 90
            if rand(10)
                  cast 'dispel evil' $n
               endif
            endif
         endif
     if $i.level > 50
        if $i.level < 75
           if rand(5)
                 cast 'dispel evil' $n
              endif
           endif
        endif
      endif
   endif
~
>act_prog p circles around~
if isfight($i)
   if ispc($n)
      if $i.level > 89
         if rand(15)
            bash $n
            endif
         endif
      if $i.level > 74
         if $i.level < 90
            if rand(10)
               bash $n
               endif
            endif
         endif
      if $i.level > 50
         if $i.level < 75
            if rand(5)
               bash $n
               endif
            endif
         endif
      endif
   endif
~
>act_prog p utters the words, 'unmanifgz'~
if isfight($i)
   if ispc($n)
      if $i.level > 89
         if rand(15)
            bash $n
            endif
         endif
      if $i.level > 74
         if $i.level < 90
            if rand(10)
               bash $n
               endif
            endif
         endif
      if $i.level > 50
         if $i.level < 75
            if rand(5)
               bash $n
               endif
            endif
         endif
      endif
   endif
~
>act_prog p utters the words, 'qahijf gsfal'~
if isfight($i)
   if ispc($n)
      if $i.level > 89
         if rand(15)
            if isevil($n)
               cast 'dispel evil' $n
               else
               cast 'divine fury' $n
               endif
            endif
         endif
      if $i.level > 74
         if $i.level < 90
            if rand(10)
               if isevil($n)
                  cast 'dispel evil' $n
                  else
                  cast 'divine fury' $n
                  endif
               endif
            endif
         endif
     if $i.level > 50
        if $i.level < 75
           if rand(5)
              if isevil($n)
                 cast 'dispel evil' $n
                 else
                 cast 'divine fury' $n
                 endif
              endif
           endif
        endif
      endif
   endif
~
>act_prog p utters the words, 'qarr solilla'~
if isfight($i)
   if ispc($n)
      if $i.level > 89
         if rand(15)
            bash $n
            endif
         endif
      if $i.level > 74
         if $i.level < 90
            if rand(10)
               bash $n
               endif
            endif
         endif
      if $i.level > 50
         if $i.level < 75
            if rand(5)
               bash $n
               endif
            endif
         endif
      endif
   endif
~
>act_prog p utters the words, 'gji brul'~
if isfight($i)
   if ispc($n)
      if $i.level > 89
         if rand(15)
            bash $n
            endif
         endif
      if $i.level > 74
         if $i.level < 90
            if rand(10)
               bash $n
               endif
            endif
         endif
      if $i.level > 50
         if $i.level < 75
            if rand(5)
               bash $n
               endif
            endif
         endif
      endif
   endif
~
>act_prog p utters the words, 'efaxi'~
if isfight($i)
   if ispc($n)
      if $i.level > 89
         if rand(15)
            if isevil($n)
               cast 'dispel evil' $n
               else
               cast 'divine fury' $n
               endif
            endif
         endif
      if $i.level > 74
         if $i.level < 90
            if rand(10)
               if isevil($n)
                  cast 'dispel evil' $n
                  else
                  cast 'divine fury' $n
                  endif
               endif
            endif
         endif
     if $i.level > 50
        if $i.level < 75
           if rand(5)
              if isevil($n)
                 cast 'dispel evil' $n
                 else
                 cast 'divine fury' $n
                 endif
              endif
           endif
        endif
      endif
   endif
~
>act_prog p deathstroke and falls to the ground.~
if isfight($i)
   if ispc($n)
      if $i.level > 89
         if rand(15)
            MPECHOAT $n $I conjures a hammer of pure spiritual energy and hurls it at you!
            MPECHOAT $n The hammer disperses into the air as it slams into you.
            MPECHOAROUND $n $I conjures a hammer of pure spiritual energy and hurls it at $n!
            MPECHOAROUND $n The hammer disperses into the air as it slams into $n's face.
            MPDAMAGE $n 1d1% undefined
            if isgood($n)
               MPDAMAGE $n 30d5 energy
               else
               MPDAMAGE $n 40d5 energy
               endif
            endif
         endif
      if $i.level > 74
         if $i.level < 90
            if rand(10)
               MPECHOAT $n $I conjures a hammer of pure spiritual energy and hurls it at you!
               MPECHOAT $n The hammer disperses into the air as it slams into you.
               MPECHOAROUND $n $I conjures a hammer of pure spiritual energy and hurls it at $n!
               MPECHOAROUND $n The hammer disperses into the air as it slams into $n's face.
               MPDAMAGE $n 1d1% undefined
               if isgood($n)
                  MPDAMAGE $n 30d5 energy
                  else
                  MPDAMAGE $n 40d5 energy
                  endif
               endif
            endif
         endif
      if $i.level > 50
         if $i.level < 75
            if rand(5)
               MPECHOAT $n $I conjures a hammer of pure spiritual energy and hurls it at you!
               MPECHOAT $n The hammer disperses into the air as it slams into you.
               MPECHOAROUND $n $I conjures a hammer of pure spiritual energy and hurls it at $n!
               MPECHOAROUND $n The hammer disperses into the air as it slams into $n's face.
               if isgood($n)
                  MPDAMAGE $n 30d5 energy
                  else
                  MPDAMAGE $n 40d5 energy
                  endif
               endif
            endif
         endif
      endif
   endif
~
>act_prog p attempts to hit you in your solar plexus!~
if isfight($i)
   if ispc($n)
      if $i.level > 89
         if rand(15)
            bash $n
            endif
         endif
      if $i.level > 74
         if $i.level < 90
            if rand(10)
               bash $n
               endif
            endif
         endif
      if $i.level > 50
         if $i.level < 75
            if rand(5)
               bash $n
               endif
            endif
         endif
      endif
   endif
~
>act_prog p lunges forward in an attempt to smash into you but falls flat on~
if isfight($i)
   if ispc($n)
      if $i.level > 89
         if rand(15)
            if isevil($n)
               cast 'dispel evil' $n
               else
               cast 'divine fury' $n
               endif
            endif
         endif
      if $i.level > 74
         if $i.level < 90
            if rand(10)
               if isevil($n)
                  cast 'dispel evil' $n
                  else
                  cast 'divine fury' $n
                  endif
               endif
            endif
         endif
     if $i.level > 50
        if $i.level < 75
           if rand(5)
              if isevil($n)
                 cast 'dispel evil' $n
                 else
                 cast 'divine fury' $n
                 endif
              endif
           endif
        endif
      endif
   endif
~
>act_prog p The glowing circle pulses gently.~
if $i.level > 89
    MPDAMAGE allpc 1d1% undefined
    MPDAMAGE allpc 15d5 undefined
    endif
if $i.level > 74
    if $i.level < 90
        MPDAMAGE allpc 1d1% undefined
        MPDAMAGE allpc 10d5 undefined
        endif
    endif
if $i.level > 50
    if $i.level < 75
        MPDAMAGE allpc 5d5 undefined
        endif
    endif
~
>act_prog p tries to headbutt you, but fails miserably.~
if isfight($i)
   if ispc($n)
      if $i.level > 89
         if rand(15)
            bash $n
            endif
         endif
      if $i.level > 74
         if $i.level < 90
            if rand(10)
               bash $n
               endif
            endif
         endif
      if $i.level > 50
         if $i.level < 75
            if rand(5)
               bash $n
               endif
            endif
         endif
      endif
   endif
~
>act_prog p You are SHOCKED~
if isfight($i)
   if ispc($n)
      if $i.level > 89
         if rand(15)
            MPECHOAT $n $I conjures a hammer of pure spiritual energy and hurls it at you!
            MPECHOAT $n The hammer disperses into the air as it slams into you.
            MPECHOAROUND $n $I conjures a hammer of pure spiritual energy and hurls it at $n!
            MPECHOAROUND $n The hammer disperses into the air as it slams into $n's face.
            MPDAMAGE $n 1d1% undefined
            if isgood($n)
               MPDAMAGE $n 30d5 energy
               else
               MPDAMAGE $n 40d5 energy
               endif
            endif
         endif
      if $i.level > 74
         if $i.level < 90
            if rand(10)
               MPECHOAT $n $I conjures a hammer of pure spiritual energy and hurls it at you!
               MPECHOAT $n The hammer disperses into the air as it slams into you.
               MPECHOAROUND $n $I conjures a hammer of pure spiritual energy and hurls it at $n!
               MPECHOAROUND $n The hammer disperses into the air as it slams into $n's face.
               MPDAMAGE $n 1d1% undefined
               if isgood($n)
                  MPDAMAGE $n 30d5 energy
                  else
                  MPDAMAGE $n 40d5 energy
                  endif
               endif
            endif
         endif
      if $i.level > 50
         if $i.level < 75
            if rand(5)
               MPECHOAT $n $I conjures a hammer of pure spiritual energy and hurls it at you!
               MPECHOAT $n The hammer disperses into the air as it slams into you.
               MPECHOAROUND $n $I conjures a hammer of pure spiritual energy and hurls it at $n!
               MPECHOAROUND $n The hammer disperses into the air as it slams into $n's face.
               if isgood($n)
                  MPDAMAGE $n 30d5 energy
                  else
                  MPDAMAGE $n 40d5 energy
                  endif
               endif
            endif
         endif
      endif
   endif
~
>act_prog p but trips~
if isfight($i)
   if ispc($n)
      if $i.level > 89
         if rand(15)
            if isevil($n)
               cast 'dispel evil' $n
               else
               cast 'divine fury' $n
               endif
            endif
         endif
      if $i.level > 74
         if $i.level < 90
            if rand(10)
               if isevil($n)
                  cast 'dispel evil' $n
                  else
                  cast 'divine fury' $n
                  endif
               endif
            endif
         endif
     if $i.level > 50
        if $i.level < 75
           if rand(5)
              if isevil($n)
                 cast 'dispel evil' $n
                 else
                 cast 'divine fury' $n
                 endif
              endif
           endif
        endif
      endif
   endif
~
>act_prog p starts swinging like a MADMAN!~
if isfight($i)
   if ispc($n)
      if $i.level > 89
         if rand(15)
            MPECHOAT $n $I conjures a hammer of pure spiritual energy and hurls it at you!
            MPECHOAT $n The hammer disperses into the air as it slams into you.
            MPECHOAROUND $n $I conjures a hammer of pure spiritual energy and hurls it at $n!
            MPECHOAROUND $n The hammer disperses into the air as it slams into $n's face.
            MPDAMAGE $n 1d1% undefined
            if isgood($n)
               MPDAMAGE $n 30d5 energy
               else
               MPDAMAGE $n 40d5 energy
               endif
            endif
         endif
      if $i.level > 74
         if $i.level < 90
            if rand(10)
               MPECHOAT $n $I conjures a hammer of pure spiritual energy and hurls it at you!
               MPECHOAT $n The hammer disperses into the air as it slams into you.
               MPECHOAROUND $n $I conjures a hammer of pure spiritual energy and hurls it at $n!
               MPECHOAROUND $n The hammer disperses into the air as it slams into $n's face.
               MPDAMAGE $n 1d1% undefined
               if isgood($n)
                  MPDAMAGE $n 30d5 energy
                  else
                  MPDAMAGE $n 40d5 energy
                  endif
               endif
            endif
         endif
      if $i.level > 50
         if $i.level < 75
            if rand(5)
               MPECHOAT $n $I conjures a hammer of pure spiritual energy and hurls it at you!
               MPECHOAT $n The hammer disperses into the air as it slams into you.
               MPECHOAROUND $n $I conjures a hammer of pure spiritual energy and hurls it at $n!
               MPECHOAROUND $n The hammer disperses into the air as it slams into $n's face.
               if isgood($n)
                  MPDAMAGE $n 30d5 energy
                  else
                  MPDAMAGE $n 40d5 energy
                  endif
               endif
            endif
         endif
      endif
   endif
~
>act_prog p advances confidently towards you, and flies into a rage!~
if isfight($i)
   if ispc($n)
      if $i.level > 89
         if rand(15)
            if isevil($n)
               cast 'dispel evil' $n
               else
               cast 'divine fury' $n
               endif
            endif
         endif
      if $i.level > 74
         if $i.level < 90
            if rand(10)
               if isevil($n)
                  cast 'dispel evil' $n
                  else
                  cast 'divine fury' $n
                  endif
               endif
            endif
         endif
     if $i.level > 50
        if $i.level < 75
           if rand(5)
              if isevil($n)
                 cast 'dispel evil' $n
                 else
                 cast 'divine fury' $n
                 endif
              endif
           endif
        endif
      endif
   endif
~
>act_prog p utters the words, 'saugai'~
if isfight($i)
   if ispc($n)
      if $i.level > 89
         if rand(15)
            bash $n
            endif
         endif
      if $i.level > 74
         if $i.level < 90
            if rand(10)
               bash $n
               endif
            endif
         endif
      if $i.level > 50
         if $i.level < 75
            if rand(5)
               bash $n
               endif
            endif
         endif
      endif
   endif
~
>act_prog p utters the words, 'qfzzsuio oculoahp'~
if isfight($i)
   if ispc($n)
      if $i.level > 89
         if rand(15)
            bash $n
            endif
         endif
      if $i.level > 74
         if $i.level < 90
            if rand(10)
               bash $n
               endif
            endif
         endif
      if $i.level > 50
         if $i.level < 75
            if rand(5)
               bash $n
               endif
            endif
         endif
      endif
   endif
~
>act_prog p You dodge a bash from~
if isfight($i)
   if ispc($n)
      if $i.level > 89
         if rand(15)
            MPECHOAT $n $I conjures a hammer of pure spiritual energy and hurls it at you!
            MPECHOAT $n The hammer disperses into the air as it slams into you.
            MPECHOAROUND $n $I conjures a hammer of pure spiritual energy and hurls it at $n!
            MPECHOAROUND $n The hammer disperses into the air as it slams into $n's face.
            MPDAMAGE $n 1d1% undefined
            if isgood($n)
               MPDAMAGE $n 30d5 energy
               else
               MPDAMAGE $n 40d5 energy
               endif
            endif
         endif
      if $i.level > 74
         if $i.level < 90
            if rand(10)
               MPECHOAT $n $I conjures a hammer of pure spiritual energy and hurls it at you!
               MPECHOAT $n The hammer disperses into the air as it slams into you.
               MPECHOAROUND $n $I conjures a hammer of pure spiritual energy and hurls it at $n!
               MPECHOAROUND $n The hammer disperses into the air as it slams into $n's face.
               MPDAMAGE $n 1d1% undefined
               if isgood($n)
                  MPDAMAGE $n 30d5 energy
                  else
                  MPDAMAGE $n 40d5 energy
                  endif
               endif
            endif
         endif
      if $i.level > 50
         if $i.level < 75
            if rand(5)
               MPECHOAT $n $I conjures a hammer of pure spiritual energy and hurls it at you!
               MPECHOAT $n The hammer disperses into the air as it slams into you.
               MPECHOAROUND $n $I conjures a hammer of pure spiritual energy and hurls it at $n!
               MPECHOAROUND $n The hammer disperses into the air as it slams into $n's face.
               if isgood($n)
                  MPDAMAGE $n 30d5 energy
                  else
                  MPDAMAGE $n 40d5 energy
                  endif
               endif
            endif
         endif
      endif
   endif
~
>act_prog p utters the words, 'divine fury'~
if isfight($i)
   if ispc($n)
      if $i.level > 89
         if rand(15)
            bash $n
            endif
         endif
      if $i.level > 74
         if $i.level < 90
            if rand(10)
               bash $n
               endif
            endif
         endif
      if $i.level > 50
         if $i.level < 75
            if rand(5)
               bash $n
               endif
            endif
         endif
      endif
   endif
~
>act_prog p utters the words, 'wzhzaf gxandoaw'~
if isfight($i)
   if ispc($n)
      if $i.level > 89
         if rand(15)
            bash $n
            endif
         endif
      if $i.level > 74
         if $i.level < 90
            if rand(10)
               bash $n
               endif
            endif
         endif
      if $i.level > 50
         if $i.level < 75
            if rand(5)
               bash $n
               endif
            endif
         endif
      endif
   endif
~
>act_prog p utters the words, 'sandoaarlkz'~
if isfight($i)
   if ispc($n)
      if $i.level > 89
         if rand(15)
            bash $n
            endif
         endif
      if $i.level > 74
         if $i.level < 90
            if rand(10)
               bash $n
               endif
            endif
         endif
      if $i.level > 50
         if $i.level < 75
            if rand(5)
               bash $n
               endif
            endif
         endif
      endif
   endif
~
>act_prog p utters the words, 'pzrrghxanduaw'~
if isfight($i)
   if ispc($n)
      if $i.level > 89
         if rand(15)
            MPECHOAT $n $I conjures a hammer of pure spiritual energy and hurls it at you!
            MPECHOAT $n The hammer disperses into the air as it slams into you.
            MPECHOAROUND $n $I conjures a hammer of pure spiritual energy and hurls it at $n!
            MPECHOAROUND $n The hammer disperses into the air as it slams into $n's face.
            MPDAMAGE $n 1d1% undefined
            if isgood($n)
               MPDAMAGE $n 30d5 energy
               else
               MPDAMAGE $n 40d5 energy
               endif
            endif
         endif
      if $i.level > 74
         if $i.level < 90
            if rand(10)
               MPECHOAT $n $I conjures a hammer of pure spiritual energy and hurls it at you!
               MPECHOAT $n The hammer disperses into the air as it slams into you.
               MPECHOAROUND $n $I conjures a hammer of pure spiritual energy and hurls it at $n!
               MPECHOAROUND $n The hammer disperses into the air as it slams into $n's face.
               MPDAMAGE $n 1d1% undefined
               if isgood($n)
                  MPDAMAGE $n 30d5 energy
                  else
                  MPDAMAGE $n 40d5 energy
                  endif
               endif
            endif
         endif
      if $i.level > 50
         if $i.level < 75
            if rand(5)
               MPECHOAT $n $I conjures a hammer of pure spiritual energy and hurls it at you!
               MPECHOAT $n The hammer disperses into the air as it slams into you.
               MPECHOAROUND $n $I conjures a hammer of pure spiritual energy and hurls it at $n!
               MPECHOAROUND $n The hammer disperses into the air as it slams into $n's face.
               if isgood($n)
                  MPDAMAGE $n 30d5 energy
                  else
                  MPDAMAGE $n 40d5 energy
                  endif
               endif
            endif
         endif
      endif
   endif
~
>act_prog p utters the words, 'saxzf pandoaw'~
if isfight($i)
   if ispc($n)
      if $i.level > 89
         if rand(15)
            bash $n
            endif
         endif
      if $i.level > 74
         if $i.level < 90
            if rand(10)
               bash $n
               endif
            endif
         endif
      if $i.level > 50
         if $i.level < 75
            if rand(5)
               bash $n
               endif
            endif
         endif
      endif
   endif
~
>act_prog p utters the words, 'yrawzghfutz'~
if isfight($i)
   if ispc($n)
      if $i.level > 89
         if rand(15)
            if isevil($n)
               cast 'dispel evil' $n
               else
               cast 'divine fury' $n
               endif
            endif
         endif
      if $i.level > 74
         if $i.level < 90
            if rand(10)
               if isevil($n)
                  cast 'dispel evil' $n
                  else
                  cast 'divine fury' $n
                  endif
               endif
            endif
         endif
     if $i.level > 50
        if $i.level < 75
           if rand(5)
              if isevil($n)
                 cast 'dispel evil' $n
                 else
                 cast 'divine fury' $n
                 endif
              endif
           endif
        endif
      endif
   endif
~
>act_prog p utters the words, 'eugszr~
if isfight($i)
   if ispc($n)
      if $i.level > 89
         if rand(15)
            bash $n
            endif
         endif
      if $i.level > 74
         if $i.level < 90
            if rand(10)
               bash $n
               endif
            endif
         endif
      if $i.level > 50
         if $i.level < 75
            if rand(5)
               bash $n
               endif
            endif
         endif
      endif
   endif
~
>act_prog p utters the words, 'pzar gsfal'~
if isfight($i)
   if ispc($n)
      if $i.level > 89
         if rand(15)
            bash $n
            endif
         endif
      if $i.level > 74
         if $i.level < 90
            if rand(10)
               bash $n
               endif
            endif
         endif
      if $i.level > 50
         if $i.level < 75
            if rand(5)
               bash $n
               endif
            endif
         endif
      endif
   endif
~
>death_prog 100~
if $i.level > 89
   MPECHO At Death's door, $I makes one final prayer of contrition.
   MPECHO $I's body flares brightly and then bursts in a blast of pure energy.
   MPDAMAGE allpc 1d1% undefined
   MPDAMAGE allpc 120d5 energy
   endif
if $i.level > 74
   if $i.level < 90
      if rand(50)
         MPECHO At Death's door, $I makes one final prayer of contrition.
         MPECHO $I's body flares brightly and then bursts in a blast of pure energy.
         MPDAMAGE allpc 1d1% undefined
         MPDAMAGE allpc 80d5 energy
         endif
      endif
   endif
if $i.level > 50
   if $i.level < 75
      if rand(25)
         MPECHO At Death's door, $I makes one final prayer of contrition.
         MPECHO $I's body flares brightly and then bursts in a blast of pure energy.
         MPDAMAGE allpc 40d5 energy
         endif
      endif
   endif
~
>fight_prog 100~
if isobjvnuminroom(112)
   if $i.level > 89
      MPSETMATH $$i.hitpoints $$i.hitpoints+75
      endif
   if $i.level > 74
      if $i.level < 90
         MPSETMATH $$i.hitpoints $$i.hitpoints+50
         endif
      endif
  if $i.level > 50
     if $i.level < 75
        MPSETMATH $$i.hitpoints $$i.hitpoints+25
        endif
      endif
   endif
~
>fight_prog 15~
if isobjvnuminroom(112)
   if $i.level > 89
      MPSETMATH $$i.hitpoints $$i.hitpoints+75
      endif
   if $i.level > 74
   if $i.level < 90
         MPSETMATH $$i.hitpoints $$i.hitpoints+50
         endif
      endif
  if $i.level > 50
     if $i.level < 75
        MPSETMATH $$i.hitpoints $$i.hitpoints+25
        endif
      endif
   endif
bash
~
>fight_prog 15~
if isobjvnuminroom(112)
   if $i.level > 89
      MPSETMATH $$i.hitpoints $$i.hitpoints+75
      endif
   if $i.level > 74
      if $i.level < 90
         MPSETMATH $$i.hitpoints $$i.hitpoints+50
         endif
      endif
  if $i.level > 50
     if $i.level < 75
        MPSETMATH $$i.hitpoints $$i.hitpoints+25
        endif
      endif
   endif
MPECHO $I's eyes glaze over with rage!
if rand(50)
    cast 'divine fury'
    else
    if isevil($n)
        cast 'dispel evil'
        else
        cast 'divine fury'
        endif
   endif
~
>fight_prog 8~
if isobjvnuminroom(112)
   if $i.level > 89
      MPSETMATH $$i.hitpoints $$i.hitpoints+75
      endif
   if $i.level > 74
      if $i.level < 90
         MPSETMATH $$i.hitpoints $$i.hitpoints+50
         endif
      endif
  if $i.level > 50
     if $i.level < 75
        MPSETMATH $$i.hitpoints $$i.hitpoints+25
        endif
      endif
   endif
MPECHOAT $n $I conjures a hammer of pure spiritual energy and hurls it at you!
MPECHOAT $n The hammer disperses into the air as it slams into you.
MPECHOAROUND $n $I conjures a hammer of pure spiritual energy and hurls it at $n!
MPECHOAROUND $n The hammer disperses into the air as it slams into $n's face.
MPDAMAGE $n 1d1% undefined
if $i.level > 89
   if isgood($n)
      MPDAMAGE $n 30d5 energy
      else
      MPDAMAGE $n 40d5 energy
      endif
    endif
if $i.level > 74
   if $i.level < 90
      if isgood($n)
         MPDAMAGE $n 20d5 energy
         else
         MPDAMAGE $n 30d5 energy
         endif
      endif
   endif
if $i.level > 50
   if $i.level < 75
      if isgood($n)
         MPDAMAGE $n 15d5 energy
         else
         MPDAMAGE $n 20d5 energy
         endif
      endif
   endif
~
>rand_prog 5~
if isfight($r)
  if isnpc($r)
    if isfollow($r)
      break
    else
      cast 'heal' $r
      break
    endif
  endif
endif
~
>load_prog 100~
cast 'bless'
cast 'strength'
cast 'armor'
cast 'resist cold'
if rand(70)
  cast 'sanctuary'
  break
else
  if rand(70)
    cast 'holy aura' physical
    break
  else
    cast 'holy aura' magic
    break
  endif
endif
~
>fight_prog 8~
if numofmobsinroom(22380) != 0
   MPECHO $I makes an odd clicking sound with $l mouth.
   MPTHROW 22380 2 0
   else
   MPECHO $I lets loose a piercing whistle!
   if $i.level > 89
      MPMLOAD 22380
      MPTHROW 22380 1 0 $i
      endif
   if $i.level > 74
      if $i.level < 90
         if rand(50)
            MPMLOAD 22380
            MPTHROW 22380 1 0 $i
            endif
         endif
      endif
   if $i.level > 50
      if $i.level < 75
         if rand(15)
            MPMLOAD 22380
            MPTHROW 22380 1 0 $i
            endif
         endif
      endif
   endif
if isobjvnuminroom(112)
   if $i.level > 89
      MPSETMATH $$i.hitpoints $$i.hitpoints+75
      endif
   if $i.level > 74
      if $i.level < 90
         MPSETMATH $$i.hitpoints $$i.hitpoints+50
         endif
      endif
  if $i.level > 50
     if $i.level < 75
        MPSETMATH $$i.hitpoints $$i.hitpoints+25
        endif
      endif
   endif
~
>fight_prog 5~
if isobjvnuminroom(112)
   if $i.level > 89
      MPSETMATH $$i.hitpoints $$i.hitpoints+75
      endif
   if $i.level > 74
      if $i.level < 90
         MPSETMATH $$i.hitpoints $$i.hitpoints+50
         endif
      endif
  if $i.level > 50
     if $i.level < 75
        MPSETMATH $$i.hitpoints $$i.hitpoints+25
        endif
      endif
   else
   MPECHO With a quick prayer $I consecrates the surrounding ground.
   MPOLOAD 112
   MPOTHROW 112 1 0 $i
   endif
~
>fight_prog 5~
if isobjvnuminroom(112)
   if $i.level > 89
      MPSETMATH $$i.hitpoints $$i.hitpoints+75
      endif
   if $i.level > 74
      if $i.level < 90
         MPSETMATH $$i.hitpoints $$i.hitpoints+50
         endif
      endif
  if $i.level > 50
     if $i.level < 75
        MPSETMATH $$i.hitpoints $$i.hitpoints+25
        endif
      endif
   endif
if ismobvnuminroom(22393)
or ismobvnuminroom(22398)
    if $i.level > 89
       MPECHO Glowing runes suddenly appear on the ground as $I softly chants a prayer.
       MPECHO The runes explode in a bright flash, banishing the undead from the realm of the living.
       MPPURGE undead
       endif
    if $i.level > 74
       if $i.level < 90
          if rand(50)
             MPECHO Glowing runes suddenly appear on the ground as $I softly chants a prayer.
             MPECHO The runes explode in a bright flash, banishing the undead from the realm of the living.
             MPPURGE undead
             else
             kick
             endif
          endif
       endif
    if $i.level > 50
       if $i.level < 75
          if rand(25)
             MPECHO Glowing runes suddenly appear on the ground as $I softly chants a prayer.
             MPECHO The runes explode in a bright flash, banishing the undead from the realm of the living.
             MPPURGE undead
             else
             kick
             endif
          endif
       endif
       else
       kick
    endif
~
>fight_prog 1~
if isobjvnuminroom(112)
   if $i.level > 89
      MPSETMATH $$i.hitpoints $$i.hitpoints+75
      endif
   if $i.level > 74
      if $i.level < 90
         MPSETMATH $$i.hitpoints $$i.hitpoints+50
         endif
      endif
  if $i.level > 50
     if $i.level < 75
        MPSETMATH $$i.hitpoints $$i.hitpoints+25
        endif
      endif
   endif
if $i.level > 89
   say You have been judged, and found wanting.
   MPECHOAT $n Spearing down from the heavens, a beam of light engulfs you in a conflagration of pain.
   MPECHOAROUND $n A beam of light spears down from the heavens, engulfing $n.
   MPDAMAGE $n 1d1% undefined
   if isgood($n)
      MPDAMAGE $n 140d5 energy
      else
      MPDAMAGE $n 160d5 energy
      endif
   endif
if $i.level > 74
   if $i.level < 90
      say You have been judged, and found wanting.
      MPECHOAT $n Spearing down from the heavens, a beam of light engulfs you in a conflagration of pain.
      MPECHOAROUND $n A beam of light spears down from the heavens, engulfing $n.
      MPDAMAGE $n 1d1% undefined
      if isgood($n)
         MPDAMAGE $n 100d5 energy
         else
         MPDAMAGE $n 120d5 energy
         endif
      endif
   endif
if $i.level > 50
   if $i.level < 75
      say You have been judged, and found wanting.
      MPECHOAT $n Spearing down from the heavens, a beam of light engulfs you in a conflagration of pain.
      MPECHOAROUND $n A beam of light spears down from the heavens, engulfing $n.
      if isgood($n)
         MPDAMAGE $n 60d5 energy
         else
         MPDAMAGE $n 80d5 energy
         endif
      endif
   endif
~
>hitprcnt_prog 50~
if rand(25)
  cast 'heal'
endif
~
>arand_prog 1~
if isspelled($i) resist_cold
    else
    cast 'resist cold'
    endif
if isspelled($i) bless
  else
    cast 'bless'
  break
endif
if isspelled($i) strength
  else
    cast 'strength'
  break
endif
if isspelled($i) holy aegis
  else
    cast 'holy aegis'
  break
endif
if rand(70)
  if isspelled($i) sanctuary
  or isspelled($i) holy_aura
    break
  else
    cast 'sanctuary'
    break
  endif
  break
endif
if rand(30)
  if isspelled($i) holy_aura
  or isspelled($i) sanctuary
    break
  else
    if rand(70)
      cast 'holy aura' physical
      break
    else
      cast 'holy aura' magic
      break
    endif
    break
  endif
  break
endif
~
|
S
#129
skillset mob~
PROC MOB ANTIPALADIN LEVEL 1-20~
This is a Skillset Mob for the Anti-Paladin Class, levels 1-20.
~
~
256 0 -1
36175872 0 0 -1
0 0 0
20 10 0d1+0 1d1+0
0 0
8 8 0 0 0 0
>fight_prog 10~
cast 'shocking grasp' $n
~
>fight_prog 5~
trip
~
>fight_prog 5~
kick
~
|
S
#130
skillset mob~
PROC MOB ANTIPALADIN LEVEL 21-35~
This is a Skillset Mob for the Anti-Paladin Class, levels 21-35.
~
~
262912 0 -1
36175880 0 0 -1
0 0 0
20 10 0d1+0 1d1+0
0 0
8 8 0 0 0 0
>arand_prog 5~
hide
~
>fight_prog 10~
MPECHO $I flashes an evil grin, and you can almost catch a glimpse of some fangs...
cast 'vampiric touch' $n
~
>fight_prog 5~
cast 'shocking grasp' $n
~
>fight_prog 10~
trip
~
>fight_prog 5~
kick
~
|
S
#131
skillset mob~
PROC MOB ANTIPALADIN LEVEL 36-50~
This is a Skillset Mob for the Anti-Paladin Class, levels 36-50.
~
~
262912 0 -1
36175880 0 0 -1
0 0 0
20 10 0d1+0 1d1+0
0 0
8 8 0 0 0 0
>arand_prog 5~
hide
~
>load_prog 100~
cast 'stoneskin' $i
cast 'invisible' $i
~
>fight_prog 15~
if ispc($n)
  if isspelled($n) poison
    cast 'vampiric touch' $n
  else
    cast 'poison' $n
    break
  endif
endif
~
>fight_prog 15~
MPECHO $I flashes an evil grin, and you can almost catch a glimpse of some fangs...
cast 'vampiric touch' $n
~
>fight_prog 5~
cast 'shocking grasp' $n
~
>fight_prog 5~
trip
~
>arand_prog 1~
if isspelled($i) stoneskin
else
  cast 'stoneskin' $i
  break
endif
if isspelled($i) invisible
else
  cast 'invisible' $i
  break
endif
~
|
S
#132
skillset mob~
PROC MOB ANTIPALADIN LEVEL 51+~
This is a Skillset Mob for the Anti-Paladin Class, levels 50+.
~
~
262912 0 -1
2621480 0 0 -1
0 0 0
20 10 0d1+0 1d1+0
0 0
8 8 0 0 0 0
>act_prog p utters the words 'vampiric touch'~
if isfight($i)
   if ispc($n)
      if $i.level > 89
         if rand(15)
            cast 'vampiric touch' $n
            endif
         endif
      if $i.level > 74
         if $i.level < 90
            if rand(10)
               cast 'vampiric touch' $n
               endif
            endif
         endif
     if $i.level > 50
         if $i.level < 75
            if rand(5)
               cast 'vampiric touch' $n
               endif
            endif
         endif
      endif
   endif
~
>act_prog p utters the words 'acid blast'~
if isfight($i)
   if ispc($n)
      if $i.level > 89
         if rand(15)
            cast 'acid blast' $n
            endif
         endif
      if $i.level > 74
         if $i.level < 90
            if rand(10)
               cast 'acid blast' $n
               endif
            endif
         endif
     if $i.level > 50
         if $i.level < 75
            if rand(5)
               cast 'acid blast' $n
               endif
            endif
         endif
      endif
   endif
~
>act_prog p eyes flash~
if isfight($i)
   if ispc($n)
      if $i.level > 89
         if rand(15)
            cast 'vampiric touch' $n
            endif
         endif
      if $i.level > 74
         if $i.level < 90
            if rand(10)
               cast 'vampiric touch' $n
               endif
            endif
         endif
     if $i.level > 50
         if $i.level < 75
            if rand(5)
               cast 'vampiric touch' $n
               endif
            endif
         endif
      endif
   endif
~
>act_prog p vibrations make you suffer~
if isfight($i)
   if ispc($n)
      if $i.level > 89
         if rand(15)
            cast 'vampiric touch' $n
            endif
         endif
      if $i.level > 74
         if $i.level < 90
            if rand(10)
               cast 'vampiric touch' $n
               endif
            endif
         endif
     if $i.level > 50
         if $i.level < 75
            if rand(5)
               cast 'vampiric touch' $n
               endif
            endif
         endif
      endif
   endif
~
>act_prog p slams a bolt of focused ki energy into the flow of magic around you!~
if isfight($i)
   if ispc($n)
      if $i.level > 89
         if rand(15)
            cast 'fear' $n
            endif
         endif
      if $i.level > 74
         if $i.level < 90
            if rand(10)
               cast 'fear' $n
               endif
             endif
           endif
     if $i.level > 50
         if $i.level < 75
            if rand(5)
               cast 'fear' $n
               endif
             endif
           endif
      endif
  endif
~
>act_prog p focuses and punches you square in the chest~
if isfight($i)
   if ispc($n)
      if $i.level > 89
         if rand(15)
            cast 'vampiric touch' $n
            endif
         endif
     if $i.level > 74
        if $i.level < 90
           if rand(10)
              cast 'vampiric touch' $n
              endif
           endif
         endif
    if $i.level > 50
        if $i.level < 75
           if rand(5)
              cast 'vampiric touch' $n
              endif
           endif
         endif
      endif
   endif
~
>act_prog p sings a witty little limerick to you!~
if isfight($i)
   if ispc($n)
      if $i.level > 89
         if rand(15)
            cast 'fear' $n
            endif
         endif
      if $i.level > 74
         if $i.level < 90
            if rand(10)
               cast 'fear' $n
               endif
             endif
           endif
     if $i.level > 50
         if $i.level < 75
            if rand(5)
               cast 'fear' $n
               endif
             endif
           endif
      endif
  endif
~
>act_prog p lashes out quickly and cripples you with a painful blow!~
if isfight($i)
   if ispc($n)
      if $i.level > 89
         if rand(15)
            cast 'vampiric touch' $n
            endif
         endif
      if $i.level > 74
         if $i.level < 90
            if rand(10)
               cast 'vampiric touch' $n
               endif
            endif
         endif
     if $i.level > 50
         if $i.level < 75
            if rand(5)
               cast 'vampiric touch' $n
               endif
            endif
         endif
      endif
   endif
~
>act_prog p with lethal accuracy and strength.~
if isfight($i)
   if ispc($n)
     if $i.level > 89
        if rand(15)
           say $n just touched me in my special places.
           cast 'vampiric touch' $n
           endif
        endif
     if $i.level > 74
        if $i.level < 90
           if rand(10)
              say $n just touched me in my special places.
              cast 'vampiric touch' $n
              endif
           endif
        endif
    if $i.level > 50
        if $i.level < 75
           if rand(5)
              say $n just touched me in my special places.
              cast 'vampiric touch' $n
              endif
           endif
        endif
     endif
   endif
~
>act_prog p circles around~
if isfight($i)
   if ispc($n)
      if $i.level > 89
         if rand(15)
            say Enough already, you're making me dizzy!
            cast 'acid blast' $n
            endif
         endif
      if $i.level > 74
         if $i.level < 90
            if rand(10)
               say Enough already, you're making me dizzy!
               cast 'acid blast' $n
               endif
            endif
         endif
     if $i.level > 50
         if $i.level < 75
            if rand(5)
               say Enough already, you're making me dizzy!
               cast 'acid blast' $n
               endif
            endif
         endif
      endif
   endif
~
>act_prog p utters the words, 'unmanifgz'~
if isfight($i)
   if ispc($n)
      if $i.level > 89
         if rand(15)
            c 'fear' $n
            endif
         endif
      if $i.level > 74
         if $i.level < 90
            if rand(10)
               cast 'fear' $n
               endif
            endif
         endif
     if $i.level > 50
         if $i.level < 75
            if rand(5)
               cast 'fear' $n
               endif
            endif
         endif
      endif
   endif
~
>act_prog p utters the words, 'qahijf gsfal'~
if isfight($i)
   if ispc($n)
     if $i.level > 89
        if rand(15)
           say Whoa, it's Rainbow Brite!
           cast 'acid blast' $n
           endif
        endif
     if $i.level > 74
        if $i.level < 90
           if rand(10)
              say Whoa, it's Rainbow Brite!
              cast 'acid blast' $n
              endif
           endif
        endif
    if $i.level > 50
        if $i.level < 75
           if rand(5)
              say Whoa, it's Rainbow Brite!
              cast 'acid blast' $n
              endif
           endif
        endif
      endif
   endif
~
>act_prog p utters the words, 'qarr solilla'~
if isfight($i)
   if ispc($n)
      if $i.level > 89
         if rand(15)
            cast 'vampiric touch' $n
            endif
         endif
      if $i.level > 74
         if $i.level < 90
            if rand(10)
               cast 'vampiric touch' $n
               endif
            endif
         endif
     if $i.level > 50
         if $i.level < 75
            if rand(5)
               cast 'vampiric touch' $n
               endif
            endif
         endif
      endif
   endif
~
>act_prog p utters the words, 'gji brul'~
if isfight($i)
   if ispc($n)
      if $i.level > 89
         if rand(15)
            say It burns! It burns!
            cast 'acid blast' $n
            endif
         endif
      if $i.level > 74
         if $i.level < 90
           if rand(10)
              say It burns! It burns!
              cast 'acid blast' $n
              endif
           endif
         endif
      if $i.level > 50
         if $i.level < 75
            if rand(5)
               say It burns! It burns!
               cast 'acid blast' $n
               endif
            endif
         endif
      endif
   endif
~
>act_prog p utters the words, 'efaxi'~
if isfight($i)
   if ispc($n)
      if $i.level > 89
         if rand(15)
            say I'm melting, I'm melt...just kidding!
            cast 'acid blast' $n
            endif
         endif
      if $i.level > 74
         if $i.level < 90
           if rand(10)
              say I'm melting, I'm melt...just kidding!
              cast 'acid blast' $n
              endif
           endif
         endif
      if $i.level > 50
         if $i.level < 75
            if rand(5)
               say I'm melting, I'm melt...just kidding!
               cast 'acid blast' $n
               endif
            endif
         endif
      endif
   endif
~
>act_prog p deathstroke~
if isfight($i)
   if ispc($n)
      if $i.level > 89
         if rand(15)
            if rand(50)
               cast 'vampiric touch' $n
               else
               cast 'acid blast' $n
               endif
            endif
         endif
      if $i.level > 74
         if $i.level < 90
            if rand(10)
               if rand(50)
                  cast 'vampiric touch' $n
                  else
                   cast 'acid blast' $n
                   endif
               endif
            endif
         endif
      if $i.level > 50
         if $i.level < 75
            if rand(5)
               if rand(50)
                  cast 'vampiric touch' $n
                  else
                   cast 'acid blast' $n
                   endif
               endif
            endif
         endif
      endif
   endif
~
>act_prog p attempts to hit you in your solar plexus!~
if isfight($i)
   if ispc($n)
      if $i.level > 89
         if rand(15)
            kick $n
            endif
         endif
      if $i.level > 74
         if $i.level < 90
            if rand(10)
               kick $n
               endif
            endif
         endif
      if $i.level > 50
         if $i.level < 75
            if rand(5)
               kick $n
               endif
            endif
         endif
      endif
   endif
~
>act_prog p lunges forward in an attempt to smash into you but falls flat on~
if isfight($i)
   if ispc($n)
      if $i.level > 89
         if rand(15)
            cast 'fear' $n
            endif
         endif
      if $i.level > 74
         if $i.level < 90
            if rand(10)
               cast 'fear' $n
               endif
            endif
         endif
      if $i.level > 50
         if $i.level < 75
            if rand(5)
               cast 'fear' $n
               endif
            endif
         endif
      endif
   endif
~
>act_prog p tries to headbutt you, but fails miserably.~
if isfight($i)
   if ispc($n)
      if $i.level > 89
         if rand(15)
            cast 'acid blast' $n
            endif
         endif
      if $i.level > 74
         if $i.level < 90
            if rand(10)
               cast 'acid blast' $n
               endif
            endif
         endif
      if $i.level > 50
         if $i.level < 75
            if rand(5)
               cast 'acid blast' $n
               endif
            endif
         endif
      endif
   endif
~
>act_prog p You are SHOCKED~
if isfight($i)
   if ispc($n)
     if $i.level > 89
        if rand(15)
           cast 'vampiric touch' $n
           endif
        endif
      if $i.level > 74
         if $i.level < 90
            if rand(10)
               cast 'vampiric touch' $n
               endif
            endif
         endif
      if $i.level > 50
         if $i.level < 75
            if rand(5)
               cast 'vampiric touch' $n
               endif
            endif
         endif
      endif
   endif
~
>act_prog p starts swinging~
if isfight($i)
   if ispc($n)
      if $i.level > 89
         if rand(15)
            if rand(50)
               kick $n
               else
               trip $n
               endif
            endif
         endif
      if $i.level > 74
         if $i.level < 90
            if rand(10)
               if rand(50)
                  kick $n
                  else
                  trip $n
                  endif
               endif
            endif
         endif
     if $i.level > 50
         if $i.level < 75
            if rand(5)
               if rand(50)
                  kick $n
                  else
                  trip $n
                  endif
               endif
            endif
         endif
      endif
   endif
~
>act_prog p starts FOAMING at the mouth, and goes BERSERK on you!~
if isfight($i)
   if ispc($n)
      if $i.level > 89
         if rand(15)
            cast 'vampiric touch' $n
            endif
         endif
      if $i.level > 74
         if $i.level < 90
            if rand(10)
               cast 'vampiric touch' $n
               endif
            endif
         endif
     if $i.level > 50
         if $i.level < 75
            if rand(10)
               cast 'vampiric touch' $n
               endif
            endif
         endif
      endif
   endif
~
>act_prog p starts advancing toward you, but trips over~
if isfight($i)
   if ispc($n)
      if $i.level > 89
         if rand(15)
            if rand(50)
               cast 'vampiric touch' $n
               else
               kick $n
               endif
            endif
         endif
      if $i.level > 74
         if $i.level < 90
            if rand(10)
               if rand(50)
                  cast 'vampiric touch' $n
                  else
                  kick $n
                  endif
               endif
            endif
         endif
     if $i.level > 50
         if $i.level < 74
            if rand(10)
               if rand(50)
                  cast 'vampiric touch' $n
                  else
                  kick $n
                  endif
               endif
            endif
         endif
      endif
   endif
~
>act_prog p advances confidently towards you, and flies into a rage!~
if isfight($i)
   if ispc($n)
      if $i.level > 89
         if rand(15)
            if rand(50)
               cast 'acid blast' $n
               else
               kick $n
               endif
            endif
         endif
      if $i.level > 74
         if $i.level < 90
            if rand(10)
               if rand(50)
                  cast 'acid blast' $n
                  else
                  kick $n
                  endif
               endif
            endif
         endif
     if $i.level > 50
         if $i.level < 74
            if rand(10)
               if rand(50)
                  cast 'acid blast' $n
                  else
                  kick $n
                  endif
               endif
            endif
         endif
      endif
   endif
~
>act_prog p utters the words, 'saugai'~
if isfight($i)
   if ispc($n)
      if $i.level > 89
         if rand(15)
            cast 'poison' $n
            endif
         endif
      if $i.level > 74
         if $i.level < 90
           if rand(10)
              cast 'poison' $n
              endif
            endif
         endif
      if $i.level > 50
         if $i.level < 75
            if rand(5)
               cast 'poison' $n
               endif
            endif
         endif
      endif
   endif
~
>act_prog p utters the words, 'unsohaiorz'~
if isfight($i)
   if ispc($n)
      if $i.level > 89
         if rand(15)
            if rand(50)
               cast 'acid blast' $n
               else
               cast 'vampiric touch' $n
               endif
            endif
         endif
      if $i.level > 74
         if $i.level < 90
           if rand(10)
              if rand(50)
                 cast 'acid blast' $n
                 else
                 cast 'vampiric touch' $n
                 endif
              endif
           endif
         endif
      if $i.level > 50
         if $i.level < 75
            if rand(5)
               if rand(50)
                  cast 'acid blast' $n
                  else
                  cast 'vampiric touch' $n
                  endif
               endif
            endif
         endif
      endif
   endif
~
>act_prog p utters the words, 'qfzzsuio oculoahp'~
if isfight($i)
   if ispc($n)
      if $i.level > 89
         if rand(15)
            if rand(50)
               cast 'acid blast' $n
               else
               cast 'vampiric touch' $n
               endif
            endif
         endif
      if $i.level > 74
         if $i.level < 90
           if rand(10)
              if rand(50)
                 cast 'acid blast' $n
                 else
                 cast 'vampiric touch' $n
                 endif
              endif
           endif
         endif
      if $i.level > 50
         if $i.level < 75
            if rand(5)
               if rand(50)
                  cast 'acid blast' $n
                  else
                  cast 'vampiric touch' $n
                  endif
               endif
            endif
         endif
      endif
   endif
~
>act_prog p You dodge a bash from~
if isfight($i)
   if ispc($n)
      if $i.level > 89
         if rand(15)
            if rand(50)
               cast 'vampiric touch' $n
               else
               kick $n
               endif
           endif
         endif
      if isfight($i)
         if ispc($n)
            if $i.level > 74
               if $i.level < 90
                  if rand(10)
                     if rand(50)
                        cast 'vampiric touch' $n
                        else
                        kick $n
                         endif
                     endif
                  endif
               endif
            endif
         endif
     if isfight($i)
         if ispc($n)
            if $i.level > 50
               if $i.level < 75
                  if rand(10)
                     if rand(50)
                        cast 'vampiric touch' $n
                        else
                        kick $n
                         endif
                     endif
                  endif
               endif
            endif
         endif
      endif
   endif
~
>act_prog p sends you sprawling.~
if isfight($i)
   if ispc($n)
      if $i.level > 89
         if rand(15)
            if rand(50)
               trip $n
               else
               kick $n
               endif
           endif
         endif
      if isfight($i)
         if ispc($n)
            if $i.level > 74
               if $i.level < 90
                  if rand(10)
                     if rand(50)
                        trip $n
                        else
                        kick $n
                         endif
                     endif
                  endif
               endif
            endif
         endif
     if isfight($i)
         if ispc($n)
            if $i.level > 50
               if $i.level < 75
                  if rand(10)
                     if rand(50)
                        trip $n
                        else
                        kick $n
                         endif
                     endif
                  endif
               endif
            endif
         endif
      endif
   endif
~
>act_prog p utters the words, 'euzuiz yjfl'~
if isfight($i)
   if ispc($n)
      if $i.level > 89
         if rand(15)
           cast 'dispel good' $n
           endif
         endif
      if $i.level > 74
         if $i.level < 90
            if rand(10)
               cast 'dispel good' $n
               endif
            endif
         endif
      if $i.level > 50
         if $i.level < 75
            if rand(5)
               cast 'dispel good' $n
               endif
            endif
         endif
      endif
   endif
~
>act_prog p utters the words, 'garandoa oahz'~
if isfight($i)
   if ispc($n)
      if $i.level > 89
         if rand(15)
            if rand(50)
               cast 'acid blast' $n
               else
               cast 'vampiric touch' $n
               endif
            endif
         endif
      if $i.level > 74
         if $i.level < 90
           if rand(10)
              if rand(50)
                 cast 'acid blast' $n
                 else
                 cast 'vampiric touch' $n
                 endif
              endif
           endif
         endif
      if $i.level > 50
         if $i.level < 75
            if rand(5)
               if rand(50)
                  cast 'acid blast' $n
                  else
                  cast 'vampiric touch' $n
                  endif
               endif
            endif
         endif
      endif
   endif
~
>act_prog p utters the words, 'wzhzaf gxandoaw'~
if isfight($i)
   if ispc($n)
      if $i$$i.level > 89
         if rand(15)
            cast 'vampiric touch' $n
            endif
         endif
      if $i$$i.level > 74
         if $i$$i.level < 90
           if rand(10)
              cast 'vampiric touch' $n
              endif
           endif
         endif
      if $i$$i.level > 50
         if $i$$i.level < 75
            if rand(5)
               cast 'vampiric touch' $n
               endif
            endif
         endif
      endif
   endif
~
>act_prog p utters the words, 'yufzghafw'~
if isfight($i)
   if ispc($n)
      if $i.level > 89
         if rand(15)
            cast 'vampiric touch' $n
            endif
         endif
      if $i.level > 74
         if $i.level < 90
           if rand(10)
              cast 'vampiric touch' $n
              endif
           endif
         endif
      if $i.level > 50
         if $i.level < 75
            if rand(5)
               cast 'vampiric touch' $n
               endif
            endif
         endif
      endif
   endif
~
>act_prog p utters the words, 'sandoaarlkz'~
if isfight($i)
   if ispc($n)
      if $i.level > 89
         if rand(15)
            cast 'fear' $n
            endif
         endif
      if $i.level > 74
         if $i.level < 90
            if rand(10)
               cast 'fear' $n
               endif
            endif
         endif
      if $i.level > 50
         if $i.level < 75
            if rand(5)
               cast 'fear' $n
               endif
            endif
         endif
      endif
   endif
~
>act_prog p utters the words, 'pzrrghxanduaw'~
if isfight($i)
   if ispc($n)
     if $i.level > 89
        if rand(15)
          cast 'acid blast' $n
          endif
        endif
      if $i.level > 74
         if $i.level < 90
            if rand(10)
               cast 'acid blast' $n
               endif
            endif
         endif
      if $i.level > 50
         if $i.level < 75
            if rand(5)
               cast 'acid blast' $n
               endif
            endif
         endif
      endif
   endif
~
>act_prog p utters the words, 'eugszr~
if isfight($i)
   if ispc($n)
      if $i.level > 89
         if rand(15)
            cast 'fear' $n
            endif
         endif
      if $i.level > 74
         if $i.level < 90
            if rand(10)
               cast 'fear' $n
               endif
            endif
         endif
      if $i.level > 50
         if $i.level < 75
            if rand(5)
               cast 'fear' $n
               endif
            endif
         endif
      endif
   endif
~
>act_prog p utters the words, 'saxzf pandoaw'~
if isfight($i)
   if ispc($n)
      if $i.level > 89
         if rand(15)
            if rand(50)
               if isgood($n)
                  cast 'dispel good' $n
                  else
                  kick $n
                  endif
              else
              cast 'acid blast' $n
              endif
            endif
         endif
      if $i.level > 74
         if $i.level < 90
            if rand(10)
               if isgood($n)
                  cast 'dispel good' $n
                  else
                  kick $n
                  endif
               else
               cast 'acid blast' $n
               endif
            endif
         endif
      if $i.level > 50
         if $i.level < 75
            if rand(5)
               if rand(50)
                  if isgood($n)
                     cast 'dispel good' $n
                     else
                      kick $n
                      endif
                  else
                  cast 'acid blast' $n
                  endif
               endif
            endif
         endif
      endif
   endif
~
>act_prog p utters the words, 'yrawzghfutz'~
if isfight($i)
   if ispc($n)
      if $i.level > 89
         if rand(15)
            if rand(50)
               trip $n
               else
               cast 'vampiric touch' $n
               endif
            endif
         endif
      if $i.level > 74
         if $i.level < 90
            if rand(10)
               if rand(50)
                  trip $n
                  else
                  cast 'vampiric touch' $n
                  endif
               endif
            endif
         endif
      if $i.level > 50
         if $i.level < 75
           if rand(5)
              if rand(50)
                 trip $n
                 else
                 cast 'vampiric touch' $n
                 endif
              endif
           endif
         endif
      endif
   endif
~
>act_prog p utters the words, 'pzar gsfal'~
if isfight($i)
   if ispc($n)
      if $i.level > 89
         if rand(15)
            cast 'fear' $n
            endif
         endif
     if $i.level > 74
        if $i.level < 90
           if rand(10)
              cast 'fear' $n
              endif
           endif
        endif
      if $i.level > 50
         if $i.level < 75
            if rand(5)
               cast 'fear' $n
               endif
            endif
         endif
      endif
   endif
~
>act_prog p The glowing circle pulses gently.~
if isobjvnuminroom(112)
    if $i.level > 89
        MPSETMATH $$i.hitpoints $$i.hitpoints+75
        MPDAMAGE allpc 1d1% undefined
        MPDAMAGE allpc 15d5 undefined
        endif
   if $i.level > 74
       if $i.level < 90
           MPSETMATH $$i.hitpoints $$i.hitpoints+50
           MPDAMAGE allpc 1d1% undefined
           MPDAMAGE allpc 10d5 undefined
           endif
       endif
   if $i.level > 50
       if $i.level < 75
           MPSETMATH $$i.hitpoints $$i.hitpoints+25
           MPDAMAGE allpc 5d5 undefined
           endif
        endif
    endif
~
>arand_prog 5~
hide
~
>load_prog 100~
cast 'acid shield' $i
cast 'stoneskin' $i
cast 'invisible' $i
cast 'unholy aegis'
cast 'infravision'
cast 'detect invisible'
~
>fight_prog 10~
if numpcs() > 2
  cast 'firestorm'
endif
~
>hitprcnt_prog 40~
if rand(20)
  if ispc($n)
    cast 'fear' $n
    break
  endif
endif
~
>fight_prog 10~
if rand(50)
   cast 'acid blast' $n
   else
   if isobjvnuminroom(101)
       cast 'acid blast' $n
       else
       cast 'globe of darkness'
       endif
   endif
~
>fight_prog 8~
if rand(50)
    if isgood($n)
        cast 'dispel good' $n
        else
        cast 'vampiric touch' $n
        endif
    else
    cast 'vampiric touch' $n
    endif
~
>fight_prog 5~
if isobjvnuminroom(112)
   else
   MPECHO With a sinister invocation, $I desecrates the surrounding ground.
   MPOLOAD 112
   MPOTHROW 112 2 0 $i
   endif
~
>fight_prog 5~
if rand(50)
    if isspelled($n) curse
        cast 'chill touch' $n
        else
        cast 'curse' $n
        endif
    else
    if isspelled($n) poison
        cast 'shocking grasp' $n
        else
        cast 'poison' $n
        endif
    endif
~
>fight_prog 5~
if rand(25)
    trip
    else
    if rand(33)
        cast 'shocking grasp'
        else
        if rand(50)
            cast 'chill touch'
            else
            kick
            endif
        endif
    endif
~
>arand_prog 1~
if isspelled($i) infravision
    else
    cast 'infravision'
    endif
if isspelled($i) detect_invisible
    else
    cast 'detect invisible'
    endif
if isspelled($i) unholy_aegis
    else
    cast 'unholy aegis'
    endif
if isspelled($i) acid_shield
else
  cast 'acid shield' $i
  break
endif
if isspelled($i) stoneskin
else
  cast 'stoneskin' $i
  break
endif
if isspelled($i) invisible
else
  cast 'invisible' $i
  break
endif
~
|
S
#133
skillset mob~
PROC MOB RANGER LEVEL 1-20~
This is a Skillset Mob for the Ranger Class, levels 1-20.
~
~
524544 0 -1
35651584 0 0 -1
0 0 0
20 10 0d1+0 1d1+0
0 0
8 8 0 0 0 0
>fight_prog 10~
MPECHO $I begins to make a loud buzzing sound...
cast 'bee sting' $n
~
>fight_prog 10~
kick
~
|
S
#134
skillset mob~
PROC MOB RANGER LEVEL 21-35~
This is a Skillset Mob for the Ranger Class, levels 21-35.
~
~
525056 0 -1
35651584 0 0 -1
0 0 0
20 10 0d1+0 1d1+0
0 0
8 8 0 0 0 0
>load_prog 100~
cast 'feline agility' $i
~
>fight_prog 15~
cast 'entangle' $n
~
>fight_prog 10~
MPECHO $I begins to make a loud buzzing sound...
cast 'bee sting' $n
~
>fight_prog 5~
kick
~
>arand_prog 1~
if isspelled($i) feline_agility
  else
    cast 'feline agility' $i
  break
endif
~
|
S
#135
skillset mob~
PROC MOB RANGER LEVEL 36-50~
This is a Skillset Mob for the Ranger Class, levels 36-50.
~
~
525056 0 -1
35651584 0 0 -1
0 0 0
20 10 0d1+0 1d1+0
0 0
8 8 0 0 0 0
>load_prog 100~
cast 'feline agility' $i
cast 'barkskin' $i
~
>fight_prog 20~
if level($i) > 39
  if numpcs() > 2
    cast 'bee swarm'
    break
  endif
endif
~
>fight_prog 10~
stun
~
>fight_prog 15~
cast 'entangle' $n
~
>fight_prog 10~
MPECHO $I begins to make a loud buzzing sound...
cast 'bee sting' $n
~
>hitprcnt_prog 50~
if rand(25)
  cast 'herb lore' $i
endif
~
>arand_prog 1~
if isspelled($i) feline_agility
else
  cast 'feline agility' $i
  break
endif
if isspelled($i) barkskin
else
  cast 'barkskin' $i
  break
endif
~
|
S
#136
skillset mob~
PROC MOB RANGER LEVEL 51+~
This is a Skillset Mob for the Ranger Class, levels 51+.
~
~
525056 0 -1
35651584 0 0 -1
0 0 0
20 10 500d1+0 1d1+0
0 0
8 8 0 0 0 0
>act_prog p utters the words, 'zawsufuq hajqp'~
if isfight($i)
   if ispc($n)
      if $i.level > 89
         if rand(15)
            if rand(50)
               trip $n
               else
               cast 'creeping death' $n
               endif
            endif
         endif
      if $i.level > 74
         if $i.level < 90
            if rand(10)
               if rand(50)
                  trip $n
                  else
                  cast 'creeping death' $n
                  endif
               endif
            endif
         endif
     if $i.level > 50
         if $i.level < 75
            if rand(5)
               if rand(50)
                  trip $n
                  else
                  cast 'creeping death' $n
                  endif
               endif
            endif
         endif
      endif
   endif
~
>act_prog p utters the words, 'aque bragh'~
if isfight($i)
   if ispc($n)
      if $i.level > 89
         if rand(15)
            if rand(50)
               cast 'entangle' $n
               else
               cast 'creeping death' $n
               endif
            endif
         endif
      if $i.level > 74
         if $i.level < 90
            if rand(10)
               if rand(50)
                  cast 'entangle' $n
                  else
                  cast 'creeping death' $n
                  endif
               endif
            endif
         endif
     if $i.level > 50
         if $i.level < 75
            if rand(5)
               if rand(50)
                  cast 'entangle' $n
                  else
                  cast 'creeping death' $n
                  endif
               endif
            endif
         endif
      endif
   endif
~
>act_prog p eyes flash~
if isfight($i)
   if ispc($n)
      if $i.level > 89
         if rand(15)
            if rand(50)
               stun $n
               else
               cast 'creeping death' $n
               endif
            endif
         endif
      if $i.level > 74
         if $i.level < 90
            if rand(10)
               if rand(50)
                  stun $n
                  else
                  cast 'creeping death' $n
                  endif
               endif
            endif
         endif
     if $i.level > 50
         if $i.level < 75
            if rand(5)
               if rand(50)
                  stun $n
                  else
                  cast 'creeping death' $n
                  endif
               endif
            endif
         endif
      endif
   endif
~
>act_prog p vibrations make you suffer~
if isfight($i)
   if ispc($n)
      if $i.level > 89
         if rand(15)
           cast 'creeping death' $n
            endif
         endif
      if $i.level > 74
         if $i.level < 90
            if rand(10)
               cast 'creeping death' $n
               endif
            endif
         endif
     if $i.level > 50
         if $i.level < 75
            if rand(5)
               cast 'creeping death' $n
               endif
            endif
         endif
      endif
   endif
~
>act_prog p slams a bolt of focused ki energy into the flow of magic around you!~
if isfight($i)
   if ispc($n)
      if $i.level > 89
         if rand(15)
            redirect $n
            endif
         endif
      if $i.level > 74
         if $i.level < 90
            if rand(10)
               redirect $n
               endif
            endif
         endif
     if $i.level > 50
         if $i.level < 75
            if rand(5)
               redirect $n
               endif
            endif
         endif
      endif
   endif
~
>act_prog p focuses and punches you square in the chest~
if isfight($i)
   if ispc($n)
      if $i.level > 89
         if rand(15)
            if rand(50)
               kick $n
               else
               cast 'creeping death' $n
               endif
            endif
         endif
      if $i.level > 74
         if $i.level < 90
            if rand(10)
               if rand(50)
                  kick $n
                  else
                  cast 'creeping death' $n
                  endif
               endif
            endif
         endif
     if $i.level > 50
         if $i.level < 75
            if rand(5)
               if rand(50)
                  kick $n
                  else
                  cast 'creeping death' $n
                  endif
               endif
            endif
         endif
      endif
   endif
~
>act_prog p sings a witty little limerick to you!~
if isfight($i)
   if ispc($n)
      if $i.level > 89
         if rand(15)
            redirect $n
            endif
         endif
      if $i.level > 74
         if $i.level < 90
            if rand(10)
               redirect $n
               endif
            endif
         endif
     if $i.level > 50
         if $i.level < 75
            if rand(5)
               redirect $n
               endif
            endif
         endif
      endif
   endif
~
>act_prog p lashes out quickly and cripples you with a painful blow!~
if isfight($i)
   if ispc($n)
      if $i.level > 89
         if rand(15)
            if rand(50)
               stun $n
               else
               cast 'creeping death' $n
               endif
            endif
         endif
      if $i.level > 74
         if $i.level < 90
            if rand(10)
               if rand(50)
                  stun $n
                  else
                  cast 'creeping death' $n
                  endif
               endif
            endif
         endif
     if $i.level > 50
         if $i.level < 75
            if rand(5)
               if rand(50)
                  stun $n
                  else
                  cast 'creeping death' $n
                  endif
               endif
            endif
         endif
      endif
   endif
~
>act_prog p with lethal accuracy and strength.~
if isfight($i)
   if ispc($n)
      if $i.level > 89
         if rand(15)
            if rand(50)
               kick $n
               else
               cast 'creeping death' $n
               endif
            endif
         endif
      if $i.level > 74
         if $i.level < 90
            if rand(10)
               if rand(50)
                  kick $n
                  else
                  cast 'creeping death' $n
                  endif
               endif
            endif
         endif
     if $i.level > 50
         if $i.level < 75
            if rand(5)
               if rand(50)
                  kick $n
                  else
                  cast 'creeping death' $n
                  endif
               endif
            endif
         endif
      endif
   endif
~
>act_prog p circles around~
if isfight($i)
   if ispc($n)
      if $i.level > 89
         if rand(15)
            if rand(50)
               redirect $n
               else
               cast 'entangle' $n
               endif
            endif
         endif
      if $i.level > 74
         if $i.level < 90
            if rand(10)
               if rand(50)
                  redirect $n
                  else
                  cast 'entangle' $n
                  endif
               endif
            endif
         endif
     if $i.level > 50
         if $i.level < 75
            if rand(5)
               if rand(50)
                  redirect $n
                  else
                  cast 'entangle' $n
                  endif
               endif
            endif
         endif
      endif
   endif
~
>act_prog p utters the words, 'unmanifgz'~
if isfight($i)
   if ispc($n)
      if $i.level > 89
         if rand(15)
            cast 'creeping death' $n
            endif
         endif
      if $i.level > 74
         if $i.level < 90
            if rand(10)
               cast 'creeping death' $n
               endif
            endif
         endif
     if $i.level > 50
         if $i.level < 75
            if rand(5)
               cast 'creeping death' $n
               endif
            endif
         endif
      endif
   endif
~
>act_prog p utters the words, 'qahijf gsfal'~
if isfight($i)
   if ispc($n)
      if $i.level > 89
         if rand(15)
            if rand(50)
               kick $n
               else
               cast 'bee sting' $n
               endif
            endif
         endif
      if $i.level > 74
         if $i.level < 90
            if rand(10)
               if rand(50)
                  kick $n
                  else
                  cast 'bee sting' $n
                  endif
               endif
            endif
         endif
     if $i.level > 50
         if $i.level < 75
            if rand(5)
               if rand(50)
                  kick $n
                  else
                  cast 'bee sting' $n
                  endif
               endif
            endif
         endif
      endif
   endif
~
>act_prog p utters the words, 'qarr solilla'~
if isfight($i)
   if ispc($n)
      if $i.level > 89
         if rand(15)
            if rand(50)
               kick $n
               else
               cast 'creeping death' $n
               endif
            endif
         endif
      if $i.level > 74
         if $i.level < 90
            if rand(10)
               if rand(50)
                  kick $n
                  else
                  cast 'creeping death' $n
                  endif
               endif
            endif
         endif
     if $i.level > 50
         if $i.level < 75
            if rand(5)
               if rand(50)
                  kick $n
                  else
                  cast 'creeping death' $n
                  endif
               endif
            endif
         endif
      endif
   endif
~
>act_prog p utters the words, 'gji brul'~
if isfight($i)
   if ispc($n)
      if $i.level > 89
         if rand(15)
            if rand(50)
               kick $n
               else
               cast 'creeping death' $n
               endif
            endif
         endif
      if $i.level > 74
         if $i.level < 90
            if rand(10)
               if rand(50)
                  kick $n
                  else
                  cast 'creeping death' $n
                  endif
               endif
            endif
         endif
     if $i.level > 50
         if $i.level < 75
            if rand(5)
               if rand(50)
                  kick $n
                  else
                  cast 'creeping death' $n
                  endif
               endif
            endif
         endif
      endif
   endif
~
>act_prog p utters the words, 'efaxi'~
if isfight($i)
   if ispc($n)
      if $i.level > 89
         if rand(15)
            if rand(50)
               kick $n
               else
               cast 'creeping death' $n
               endif
            endif
         endif
      if $i.level > 74
         if $i.level < 90
            if rand(10)
               if rand(50)
                  kick $n
                  else
                  cast 'creeping death' $n
                  endif
               endif
            endif
         endif
     if $i.level > 50
         if $i.level < 75
            if rand(5)
               if rand(50)
                  kick $n
                  else
                  cast 'creeping death' $n
                  endif
               endif
            endif
         endif
      endif
   endif
~
>act_prog p deathstroke~
if isfight($i)
   if ispc($n)
      if $i.level > 89
         if rand(15)
            if rand(50)
               kick $n
               else
               cast 'creeping death' $n
               endif
            endif
         endif
      if $i.level > 74
         if $i.level < 90
            if rand(10)
               if rand(50)
                  kick $n
                  else
                  cast 'creeping death' $n
                  endif
               endif
            endif
         endif
     if $i.level > 50
         if $i.level < 75
            if rand(5)
               if rand(50)
                  kick $n
                  else
                  cast 'creeping death' $n
                  endif
               endif
            endif
         endif
      endif
   endif
~
>act_prog p attempts to hit you in your solar plexus!~
if isfight($i)
   if ispc($n)
      if $i.level > 89
         if rand(15)
            stun $n
            endif
         endif
      if $i.level > 74
         if $i.level < 90
            if rand(10)
               stun $n
               endif
            endif
         endif
     if $i.level > 50
         if $i.level < 75
            if rand(5)
               stun $n
               endif
            endif
         endif
      endif
   endif
~
>act_prog p lunges forward in an attempt to smash into you but falls flat on~
if isfight($i)
   if ispc($n)
      if $i.level > 89
         if rand(15)
            if rand(50)
               stun $n
               else
               cast 'creeping death' $n
               endif
            endif
         endif
      if $i.level > 74
         if $i.level < 90
            if rand(10)
               if rand(50)
                  stun $n
                  else
                  cast 'creeping death' $n
                  endif
               endif
            endif
         endif
     if $i.level > 50
         if $i.level < 75
            if rand(5)
               if rand(50)
                  stun $n
                  else
                  cast 'creeping death' $n
                  endif
               endif
            endif
         endif
      endif
   endif
~
>act_prog p tries to headbutt you, but fails miserably.~
if isfight($i)
   if ispc($n)
      if $i.level > 89
         if rand(15)
            if rand(50)
               trip $n
               else
               cast 'entangle' $n
               endif
            endif
         endif
      if $i.level > 74
         if $i.level < 90
            if rand(10)
               if rand(50)
                  trip $n
                  else
                  cast 'entangle' $n
                  endif
               endif
            endif
         endif
     if $i.level > 50
         if $i.level < 75
            if rand(5)
               if rand(50)
                  trip $n
                  else
                  cast 'entangle' $n
                  endif
               endif
            endif
         endif
      endif
   endif
~
>act_prog p You are SHOCKED~
if isfight($i)
   if ispc($n)
      if $i.level > 89
         if rand(15)
            if rand(50)
               kick $n
               else
               trip $n
               endif
            endif
         endif
      if $i.level > 74
         if $i.level < 90
            if rand(10)
               if rand(50)
                  kick $n
                  else
                  trip $n
                  endif
               endif
            endif
         endif
     if $i.level > 50
         if $i.level < 75
            if rand(5)
               if rand(50)
                  kick $n
                  else
                  trip $n
                  endif
               endif
            endif
         endif
      endif
   endif
~
>act_prog p starts swinging~
if isfight($i)
   if ispc($n)
      if $i.level > 89
         if rand(15)
            cast 'bee swarm'
            endif
         endif
      if $i.level > 74
         if $i.level < 90
            if rand(10)
               cast 'bee swarm'
               endif
            endif
         endif
     if $i.level > 50
         if $i.level < 75
            if rand(5)
               cast 'bee swarm'
               endif
            endif
         endif
      endif
   endif
~
>act_prog p starts FOAMING at the mouth, and goes BERSERK on you!~
if isfight($i)
   if ispc($n)
      if $i.level > 89
         if rand(15)
            cast 'creeping death' $n
            endif
         endif
      if $i.level > 74
         if $i.level < 90
            if rand(10)
               cast 'creeping death' $n
               endif
            endif
         endif
     if $i.level > 50
         if $i.level < 75
            if rand(5)
               cast 'creeping death' $n
               endif
            endif
         endif
      endif
   endif
~
>act_prog p starts advancing toward you, but trips over~
if isfight($i)
   if ispc($n)
      if $i.level > 89
         if rand(15)
            if rand(50)
               kick $n
               else
               cast 'bee sting' $n
               endif
            endif
         endif
      if $i.level > 74
         if $i.level < 90
            if rand(10)
               if rand(50)
                  kick $n
                  else
                  cast 'bee sting' $n
                  endif
               endif
            endif
         endif
     if $i.level > 50
         if $i.level < 75
            if rand(5)
               if rand(50)
                  kick $n
                  else
                  cast 'bee sting' $n
                  endif
               endif
            endif
         endif
      endif
   endif
~
>act_prog p advances confidently towards you, and flies into a rage!~
if isfight($i)
   if ispc($n)
      if $i.level > 89
         if rand(15)
            if rand(50)
               trip $n
               else
               cast 'entangle' $n
               endif
            endif
         endif
      if $i.level > 74
         if $i.level < 90
            if rand(10)
               if rand(50)
                  trip $n
                  else
                  cast 'entangle' $n
                  endif
               endif
            endif
         endif
     if $i.level > 50
         if $i.level < 75
            if rand(5)
               if rand(50)
                  trip $n
                  else
                  cast 'entangle' $n
                  endif
               endif
            endif
         endif
      endif
   endif
~
>act_prog p utters the words, 'entangle'~
if isfight($i)
   if ispc($n)
      if $i.level > 89
         if rand(15)
            cast 'entangle' $n
            endif
         endif
      if $i.level > 74
         if $i.level < 90
            if rand(10)
               cast 'entangle' $n
               endif
            endif
         endif
     if $i.level > 50
         if $i.level < 75
            if rand(5)
               cast 'entangle' $n
               endif
            endif
         endif
      endif
   endif
~
>act_prog p utters the words, 'creeping death'~
if isfight($i)
   if ispc($n)
      if $i.level > 89
         if rand(15)
            cast 'creeping death' $n
            endif
         endif
      if $i.level > 74
         if $i.level < 90
            if rand(10)
               cast 'creeping death' $n
               endif
            endif
         endif
     if $i.level > 50
         if $i.level < 75
            if rand(5)
               cast 'creeping death' $n
               endif
            endif
         endif
      endif
   endif
~
>act_prog p You dodge a bash from~
if isfight($i)
   if ispc($n)
      if $i.level > 89
         if rand(15)
            if rand(50)
               kick $n
               else
               cast 'creeping death' $n
               endif
            endif
         endif
      if $i.level > 74
         if $i.level < 90
            if rand(10)
               if rand(50)
                  kick $n
                  else
                  cast 'creeping death' $n
                  endif
               endif
            endif
         endif
     if $i.level > 50
         if $i.level < 75
            if rand(5)
               if rand(50)
                  kick $n
                  else
                  cast 'creeping death' $n
                  endif
               endif
            endif
         endif
      endif
   endif
~
>act_prog p sends you sprawling.~
if isfight($i)
   if ispc($n)
      if $i.level > 89
         if rand(15)
            if rand(50)
               kick $n
               else
               cast 'bee sting' $n
               endif
            endif
         endif
      if $i.level > 74
         if $i.level < 90
            if rand(10)
               if rand(50)
                  kick $n
                  else
                  cast 'bee sting' $n
                  endif
               endif
            endif
         endif
     if $i.level > 50
         if $i.level < 75
            if rand(5)
               if rand(50)
                  kick $n
                  else
                  cast 'bee sting' $n
                  endif
               endif
            endif
         endif
      endif
   endif
~
>act_prog p utters the words, 'euzuiz yjfl'~
if isfight($i)
   if ispc($n)
      if $i.level > 89
         if rand(15)
            if rand(50)
               kick $n
               else
               cast 'entangle' $n
               endif
            endif
         endif
      if $i.level > 74
         if $i.level < 90
            if rand(10)
               if rand(50)
                  kick $n
                  else
                  cast 'entangle' $n
                  endif
               endif
            endif
         endif
     if $i.level > 50
         if $i.level < 75
            if rand(5)
               if rand(50)
                  kick $n
                  else
                  cast 'entangle' $n
                  endif
               endif
            endif
         endif
      endif
   endif
~
>act_prog p utters the words, 'garandoa oahz'~
if isfight($i)
   if ispc($n)
      if $i.level > 89
         if rand(15)
            cast 'bee swarm'
            endif
         endif
      if $i.level > 74
         if $i.level < 90
            if rand(10)
               cast 'bee swarm'
               endif
            endif
         endif
     if $i.level > 50
         if $i.level < 75
            if rand(5)
               cast 'bee swarm'
               endif
            endif
         endif
      endif
   endif
~
>act_prog p utters the words, 'wzhzaf gxandoaw'~
if isfight($i)
   if ispc($n)
      if $i.level > 89
         if rand(15)
            if rand(50)
               trip $n
               else
               cast 'bee sting' $n
               endif
            endif
         endif
      if $i.level > 74
         if $i.level < 90
            if rand(10)
               if rand(50)
                  trip $n
                  else
                  cast 'bee sting' $n
                  endif
               endif
            endif
         endif
     if $i.level > 50
         if $i.level < 75
            if rand(5)
               if rand(50)
                  trip $n
                  else
                  cast 'bee sting' $n
                  endif
               endif
            endif
         endif
      endif
   endif
~
>act_prog p utters the words, 'yufzghafw'~
if isfight($i)
   if ispc($n)
      if $i.level > 89
         if rand(15)
            cast 'bee swarm'
            endif
         endif
      if $i.level > 74
         if $i.level < 90
            if rand(10)
               cast 'bee swarm'
               endif
            endif
         endif
     if $i.level > 50
         if $i.level < 75
            if rand(5)
               cast 'bee swarm'
               endif
            endif
         endif
      endif
   endif
~
>act_prog p utters the words, 'sandoaarlkz'~
if isfight($i)
   if ispc($n)
      if $i.level > 89
         if rand(15)
            if rand(50)
               cast 'entangle' $n
               else
               stun $n
               endif
            endif
         endif
      if $i.level > 74
         if $i.level < 90
            if rand(10)
               if rand(50)
                  cast 'entangle' $n
                   else
                   stun $n
                   endif
               endif
            endif
         endif
     if $i.level > 50
         if $i.level < 75
            if rand(5)
               if rand(50)
                  cast 'entangle' $n
                   else
                   stun $n
                   endif
               endif
            endif
         endif
      endif
   endif
~
>act_prog p utters the words, 'pzrrghxanduaw'~
if isfight($i)
   if ispc($n)
      if $i.level > 89
         if rand(15)
            if rand(50)
               stun $n
               else
               cast 'creeping death' $n
               endif
            endif
         endif
      if $i.level > 74
         if $i.level < 90
            if rand(10)
               if rand(50)
                  stun $n
                  else
                  cast 'creeping death' $n
                  endif
               endif
            endif
         endif
     if $i.level > 50
         if $i.level < 75
            if rand(5)
               if rand(50)
                  stun $n
                  else
                  cast 'creeping death' $n
                  endif
               endif
            endif
         endif
      endif
   endif
~
>act_prog p utters the words, 'eugszr~
if isfight($i)
   if ispc($n)
      if $i.level > 89
         if rand(15)
            redirect $n
            endif
         endif
      if $i.level > 74
         if $i.level < 90
            if rand(10)
               redirect $n
               endif
            endif
         endif
     if $i.level > 50
         if $i.level < 75
            if rand(5)
               redirect $n
               endif
            endif
         endif
      endif
   endif
~
>act_prog p utters the words, 'saxzf pandoaw'~
if isfight($i)
   if ispc($n)
      if $i.level > 89
         if rand(15)
            if rand(50)
               cast 'entangle' $n
               else
               kick $n
               endif
            endif
         endif
      if $i.level > 74
         if $i.level < 90
            if rand(10)
               if rand(50)
                  cast 'entangle' $n
                   else
                   kick $n
                   endif
               endif
            endif
         endif
     if $i.level > 50
         if $i.level < 75
            if rand(5)
               if rand(50)
                  cast 'entangle' $n
                   else
                   kick $n
                   endif
               endif
            endif
         endif
      endif
   endif
~
>act_prog p utters the words, 'yrawzghfutz'~
if isfight($i)
   if ispc($n)
      if $i.level > 89
         if rand(15)
            if rand(50)
               cast 'entangle' $n
               else
               trip $n
               endif
            endif
         endif
      if $i.level > 74
         if $i.level < 90
            if rand(10)
               if rand(50)
                  cast 'entangle' $n
                   else
                   trip $n
                   endif
               endif
            endif
         endif
     if $i.level > 50
         if $i.level < 75
            if rand(5)
               if rand(50)
                  cast 'entangle' $n
                   else
                   trip $n
                   endif
               endif
            endif
         endif
      endif
   endif
~
>act_prog p utters the words, 'pzar gsfal'~
if isfight($i)
   if ispc($n)
      if $i.level > 89
         if rand(15)
            redirect $n
            endif
         endif
      if $i.level > 74
         if $i.level < 90
            if rand(10)
               redirect $n
               endif
            endif
         endif
     if $i.level > 50
         if $i.level < 75
            if rand(5)
               redirect $n
               endif
            endif
         endif
      endif
   endif
~
>load_prog 100~
cast 'feline agility'
cast 'barkskin'
cast 'camouflage'
cast 'eyes of the owl'
cast 'sense life'
cast 'staunchblood'
~
>rand_prog 5~
hide
~
>hitprcnt_prog 50~
if rand(25)
  cast 'herb lore' $i
endif
~
>fight_prog 10~
if isspelled($n) poison
    if rand(50)
        cast 'bee swarm'
        else
        cast 'bee sting'
        endif
    else
    cast 'poison'
    endif
~
>fight_prog 10~
if rand(25)
    cast 'bee sting'
    else
    if rand(33)
        disarm $n
        else
        if rand(50)
            kick
            else
            trip
            endif
        endif
    endif
~
>fight_prog 10~
if rand(50)
    cast 'entangle'
    else
    say Killer Bees where ya at?
    cast 'bee swarm'
    endif
~
>fight_prog 8~
if rand(50)
    stun $n
    else
    say Bug spray can't save you now!
    cast 'creeping death'
    endif
~
>arand_prog 1~
if isspelled($i) feline_agility
    else
    cast 'feline agility'
    endif
if isspelled($i) barkskin
   else
   cast 'barkskin'
   endif
if isspelled($i) camouflage
    else
    cast 'camouflage'
    endif
if isspelled($i) staunchblood
    else
    cast 'staunchblood'
    endif
if isspelled($i) infravision
    else
    cast 'eyes of the owl'
    endif
if isspelled($i) sense_life
    else
    cast 'sense life'
    endif
~
|
S
#137
skillset mob~
PROC MOB BARD LEVEL 1-20~
This is a Skillset Mob for the Bard Class, levels 1-20.
~
~
262400 0 -1
2097152 0 0 -1
0 0 0
20 10 0d1+0 1d1+0
0 0
8 8 0 0 0 0
>fight_prog 10~
sing whistle
~
>fight_prog 10~
trip
~
|
S
#138
skillset mob~
PROC MOB BARD LEVEL 21-35~
This is a Skillset Mob for the Bard Class, levels 21-35.
~
~
262400 0 -1
2097152 0 0 -1
0 0 0
20 10 1d1+0 0d0+0
0 0
8 8 0 0 0 0
C 10
>act_prog p resists your sticky lullaby~
MPSETTEMP $i issing false
~
>act_prog p feet falls into a numbing sleep.~
MPSETTEMP $i issing false
~
>act_prog p You run out~
MPSETTEMP $i issing false
~
>act_prog p You forgot~
MPSETTEMP $i issing false
~
>act_prog p You begin~
MPSETTEMP $i issing true
~
>fight_prog 10~
trip
~
>fight_prog 10~
if tempvar($i[issing]) == true
  trip
  break
else
  if rand(50)
    sing whistle
    break
  else
    sing sticky
    break
  endif
endif
~
|
S
#139
skillset mob~
PROC MOB BARD LEVEL 36-50~
This is a Skillset Mob for the Bard Class, levels 36-50.
~
~
262400 0 -1
2097152 0 0 -1
0 0 0
20 10 1d1+0 1d1+0
0 0
8 8 0 0 0 0
>fight_prog 10~
if tempvar($i[issing]) == true
  trip
  break
else
  if rand(50)
    sing whistle
    break
  else
    if rand(50)
      sing sticky
      break
    else
      sing terrible
      break
    endif
  endif
endif
~
>act_prog p feet falls into a numbing sleep.~
MPSETTEMP $i issing false
~
>act_prog p You run out~
MPSETTEMP $i issing false
~
>act_prog p You forgot~
MPSETTEMP $i issing false
~
>act_prog p You begin~
MPSETTEMP $i issing true
~
>act_prog p Your singing hurts your~
MPSETTEMP $i issing true
~
>fight_prog 10~
trip
~
|
S
#140
skillset mob~
PROC MOB BARD LEVEL 51+~
This is a Skillset Mob for the Bard Class, levels 51+.
~
~
262400 0 -1
2097152 0 0 -1
0 0 0
20 10 1d1+0 1d1+0
0 0
8 8 0 0 0 0
>act_prog p You can't keep singing in this position!~
MPSETTEMP $i issing false
~
>rand_prog 5~
if isfight($r)
   if isnpc($r)
      if isfollow($r)
      or isfight($i)
      or tempvar($i[issing]) == ncctrue
      or tempvar($i[issing]) == nctrue
          else
          if $i.level > 50
             if $i.level < 75
                if rand(33)
                   sing insane chant
                   MPSETTEMP $i issing ctrue
                   else
                   if rand(50)
                      sing irresistaable ditty
                      MPSETTEMP $i issing ctrue
                      else
                      sing 'sticky lullaby' $r
                      MPSETTEMP $i issing ctrue
                      endif
                   endif
                endif
             endif
         endif
      endif
   endif
~
>act_prog p resists your sticky lullaby!~
MPSETTEMP $i issing false
~
>hitprcnt_prog 80~
if isfight($i)
or tempvar($i[issing]) == ncctrue
or tempvar($i[issing]) == nctrue
    else
    sing healing melody
    MPSETTEMP $i issing ncctrue
    endif
~
>load_prog 100~
MPSETTEMP $i issing false
~
>act_prog p is DEAD!!~
if ispc($n)
    if rand(5)
        if rand(25)
            say Who put the quarter in $n's ass? Because, they just got played!
            else
            if rand(33)
                say $n has been to band camp before.  That flute slid up there real easy.
                else
                if rand(50)
                    say Wow! I didn't know $n could deep-throat an oboe like that!
                    else
                    say I'm not a playa, I just crush a lot!
                    endif
                endif
            endif
        endif
    endif
~
>act_prog p You dance a small jig on the corpse.~
MPSETTEMP $i issing false
~
>act_prog p such beautiful music.~
MPSETTEMP $i issing false
~
>act_prog p Your singing drives everyone around you INSANE!!!~
MPSETTEMP $i issing false
~
>arand_prog 5~
if isfight($i)
   else
   if tempvar($i[issing]) == false
       sing revealing staccato
       MPSETTEMP $i issing ncctrue
      endif
   endif
~
>speech_prog testes~
say $q
say vnum $q
mpsettemp $i issing false
~
>fight_prog 20~
if tempvar($i[issing]) == cctrue
or tempvar($i[issing]) == ctrue
   if rand(50)
      kick
      else
      trip
      endif
   endif
if tempvar($i[issing]) == ncctrue
or tempvar($i[issing]) == nctrue
or tempvar($i[issing]) == false
   sing 'stop'
   if $i.level > 89
      sing crushing crescendo
      MPSETTEMP $i issing cctrue
      endif
   if $i.level > 74
      if $i.level < 90
         if rand(50)
            sing terrible clef
            MPSETTEMP $i issing cctrue
            else
            sing crushing crescendo
            MPSETTEMP $i issing cctrue
            endif
         endif
      endif
  if $i.level > 50
      if $i.level < 75
         if rand(80)
            sing terrible clef
            MPSETTEMP $i issing cctrue
            else
            sing 'whistle sharp'
            sing 'whistle sharp'
            sing 'whistle sharp'
            endif
         endif
      endif
   endif
~
>fight_prog 5~
if tempvar($i[issing]) == cctrue
or tempvar($i[issing]) == ctrue
   if rand(50)
      kick
      else
      trip
      endif
   endif
if tempvar($i[issing]) == ncctrue
or tempvar($i[issing]) == nctrue
or tempvar($i[issing]) == false
   sing stop
   if $i.level > 89
      if rand(33)
         sing 'sticky lullaby' $n
         MPSETTEMP $i issing ctrue
         else
         if rand(50)
            sing insane chant
            MPSETTEMP $i issing ctrue
            else
            sing irresistable ditty
            MPSETTEMP $i issing ctrue
            endif
         endif
      endif
   if $i.level > 74
      if $i.level < 90
         if rand(50)
            if rand(33)
               sing 'sticky lullaby' $n
               MPSETTEMP $i issing ctrue
               else
               if rand(50)
                  sing insane chant
                  MPSETTEMP $i issing ctrue
                  else
                  sing irresistable ditty
                  MPSETTEMP $i issing ctrue
                  endif
               endif
            endif
         endif
      endif
  if $i.level > 50
      if $i.level < 75
         if rand(25)
            if rand(33)
               sing 'sticky lullaby' $n
               MPSETTEMP $i issing ctrue
               else
               if rand(50)
                  sing insane chant
                  MPSETTEMP $i issing ctrue
                  else
                  sing irresistable ditty
                  MPSETTEMP $i issing ctrue
                  endif
               endif
            endif
         endif
      endif
   endif
~
>act_prog p feet fall into a numbing sleep.~
MPSETTEMP $i issing false
~
>act_prog p You run out~
MPSETTEMP $i issing false
~
>act_prog p You forgot~
MPSETTEMP $i issing false
~
>act_prog p With the battle broken you end your crescendo.~
MPSETTEMP $i issing false
~
>act_prog p Your song fades outside of battle.~
MPSETTEMP $i issing false
~
|
S
#141
skillset mob~
PROC MOB DRUID LEVEL 1-20~
This is a Skillset Mob for the Druid Class, levels 1-20.
~
~
0 0 -1
35651616 0 0 -1
0 0 0
20 10 0d1+0 1d1+0
0 0
8 8 0 0 0 0
>greet_prog 100~
cast 'oaken fortitude' $i
cast 'barkskin' $i
~
>fight_prog 15~
cast 'blue bird' $n
~
>hitprcnt_prog 50~
if rand(25)
  cast 'cure serious' $i
endif
~
>arand_prog 1~
if isspelled($i) oaken_fortitude
else
  cast 'oaken fortitude' $i
  break
endif
if isspelled($i) barkskin
else
  cast 'barkskin' $i
  break
endif
~
|
S
#142
skillset mob~
PROC MOB DRUID LEVEL 21-35~
This is a Skillset Mob for the Druid Class, levels 21-35.
~
~
0 0 -1
35651616 0 0 -1
0 0 0
20 10 1d1+0 1d1+0
0 0
8 8 0 0 0 0
>load_prog 100~
cast 'oaken fortitude' $i
cast 'barkskin' $i
cast 'camo' $i
cast 'stoneshield' $i
~
>fight_prog 10~
if level($i) > 27
  cast 'drown' $n
else
  cast 'blue bird' $n
endif
~
>hitprcnt_prog 50~
if rand(25)
  cast 'cure critic' $i
endif
~
>arand_prog 1~
if isspelled($i) oaken_fortitude
else
  cast 'oaken fortitude' $i
  break
endif
if isspelled($i) barkskin
else
  cast 'barkskin' $i
  break
endif
if isspelled($i) camouflague
else
  cast 'camo' $i
  break
endif
if isspelled($i) stoneshield
else
  cast 'stoneshield' $i
  break
endif
~
|
S
#143
skillset mob~
PROC MOB DRUID LEVEL 36-50~
This is a Skillset Mob for the Druid Class, levels 36-50.
~
~
0 0 -1
35651616 32 0 -1
0 0 0
20 10 1d1+0 1d1+0
0 0
8 8 0 0 0 0
>load_prog 80~
cast 'oaken fortitude' $i
cast 'barkskin' $i
cast 'camo' $i
cast 'stoneshield' $i
~
>fight_prog 10~
if tempvar($i[israiny]) == true
  cast 'drown' $n
  break
else
  if tempvar($i[issunny]) == true
    cast 'sun ray' $n
    break
  endif
endif
~
>act_prog cloudless~
MPSETTEMP $i issunny true
MPSETTEMP $i israiny false
~
>act_prog cloudy~
MPSETTEMP $i issunny true
MPSETTEMP $i israiny false
~
>act_prog flashes~
MPSETTEMP $i israiny true
MPSETTEMP $i issunny false
~
>act_prog rainy~
MPSETTEMP $i israiny true
MPSETTEMP $i issunny false
~
>fight_prog 50~
weather
~
>fight_prog 10~
cast 'drown' $n
~
>hitprcnt_prog 50~
if rand(25)
  cast 'herb lore' $i
endif
~
>arand_prog 1~
if isspelled($i) oaken_fortitude
else
  cast 'oaken fortitude' $i
  break
endif
if isspelled($i) barkskin
else
  cast 'barkskin' $i
  break
endif
if isspelled($i) camouflague
else
  cast 'camo' $i
  break
endif
if isspelled($i) stoneshield
else
  cast 'stoneshield' $i
  break
endif
~
|
S
#144
skillset mob~
PROC MOB DRUID LEVEL 51+~
This is a Skillset Mob for the Druid Class, levels 51+.
~
~
0 0 -1
35651616 32 0 -1
0 0 0
20 10 0d1+0 1d1+0
0 0
8 8 0 0 0 0
>act_prog p focuses and punches you square in the chest~
if isfight($i)
   if ispc($n)
      if $i.level > 89
         if rand(15)
            if isspelled($n) curse
               if rand(50)
                  cast 'colour spray' $n
                  else
                  cast 'drown' $n
                  endif
               else
               cast 'curse' $n
               endif
            endif
         endif
      if $i.level > 74
         if $i.level < 90
            if rand(10)
               if isspelled($n) curse
                  if rand(50)
                     cast 'colour spray' $n
                     else
                     cast 'drown' $n
                     endif
                  else
                  cast 'curse' $n
                  endif
               endif
            endif
         endif
     if $i.level > 50
         if $i.level < 75
            if rand(5)
               if isspelled($n) curse
                  if rand(50)
                     cast 'colour spray' $n
                     else
                     cast 'drown' $n
                     endif
                  else
                  cast 'curse' $n
                  endif
               endif
            endif
         endif
      endif
   endif
~
>act_prog p utters the words, 'zawsufuq hajqp'~
if isfight($i)
   if ispc($n)
      if $i.level > 89
         if rand(15)
            if isspelled($n) curse
               if rand(50)
                  cast 'colour spray' $n
                  else
                  cast 'drown' $n
                  endif
               else
               cast 'curse' $n
               endif
            endif
         endif
      if $i.level > 74
         if $i.level < 90
            if rand(10)
               if isspelled($n) curse
                  if rand(50)
                     cast 'colour spray' $n
                     else
                     cast 'drown' $n
                     endif
                  else
                  cast 'curse' $n
                  endif
               endif
            endif
         endif
     if $i.level > 50
         if $i.level < 75
            if rand(5)
               if isspelled($n) curse
                  if rand(50)
                     cast 'colour spray' $n
                     else
                     cast 'drown' $n
                     endif
                  else
                  cast 'curse' $n
                  endif
               endif
            endif
         endif
      endif
   endif
~
>act_prog p utters the words, 'aque bragh'~
if isfight($i)
   if ispc($n)
      if $i.level > 89
         if rand(15)
            if isspelled($n) curse
               if rand(50)
                  cast 'colour spray' $n
                  else
                  cast 'drown' $n
                  endif
               else
               cast 'curse' $n
               endif
            endif
         endif
      if $i.level > 74
         if $i.level < 90
            if rand(10)
               if isspelled($n) curse
                  if rand(50)
                     cast 'colour spray' $n
                     else
                     cast 'drown' $n
                     endif
                  else
                  cast 'curse' $n
                  endif
               endif
            endif
         endif
     if $i.level > 50
         if $i.level < 75
            if rand(5)
               if isspelled($n) curse
                  if rand(50)
                     cast 'colour spray' $n
                     else
                     cast 'drown' $n
                     endif
                  else
                  cast 'curse' $n
                  endif
               endif
            endif
         endif
      endif
   endif
~
>act_prog p slams a bolt of focused ki energy into the flow of magic around you!~
if isfight($i)
   if ispc($n)
      if $i.level > 89
         if rand(15)
            if isspelled($n) curse
               if rand(50)
                  cast 'colour spray' $n
                  else
                  cast 'drown' $n
                  endif
               else
               cast 'curse' $n
               endif
            endif
         endif
      if $i.level > 74
         if $i.level < 90
            if rand(10)
               if isspelled($n) curse
                  if rand(50)
                     cast 'colour spray' $n
                     else
                     cast 'drown' $n
                     endif
                  else
                  cast 'curse' $n
                  endif
               endif
            endif
         endif
     if $i.level > 50
         if $i.level < 75
            if rand(5)
               if isspelled($n) curse
                  if rand(50)
                     cast 'colour spray' $n
                     else
                     cast 'drown' $n
                     endif
                  else
                  cast 'curse' $n
                  endif
               endif
            endif
         endif
      endif
   endif
~
>act_prog p sings a witty little limerick to you!~
if isfight($i)
   if ispc($n)
      if $i.level > 89
         if rand(15)
            if isspelled($n) curse
               if rand(50)
                  cast 'colour spray' $n
                  else
                  cast 'drown' $n
                  endif
               else
               cast 'curse' $n
               endif
            endif
         endif
      if $i.level > 74
         if $i.level < 90
            if rand(10)
               if isspelled($n) curse
                  if rand(50)
                     cast 'colour spray' $n
                     else
                     cast 'drown' $n
                     endif
                  else
                  cast 'curse' $n
                  endif
               endif
            endif
         endif
     if $i.level > 50
         if $i.level < 75
            if rand(5)
               if isspelled($n) curse
                  if rand(50)
                     cast 'colour spray' $n
                     else
                     cast 'drown' $n
                     endif
                  else
                  cast 'curse' $n
                  endif
               endif
            endif
         endif
      endif
   endif
~
>act_prog p lashes out quickly and cripples you with a painful blow!~
if isfight($i)
   if ispc($n)
      if $i.level > 89
         if rand(15)
            if isspelled($n) curse
               if rand(50)
                  cast 'colour spray' $n
                  else
                  cast 'drown' $n
                  endif
               else
               cast 'curse' $n
               endif
            endif
         endif
      if $i.level > 74
         if $i.level < 90
            if rand(10)
               if isspelled($n) curse
                  if rand(50)
                     cast 'colour spray' $n
                     else
                     cast 'drown' $n
                     endif
                  else
                  cast 'curse' $n
                  endif
               endif
            endif
         endif
     if $i.level > 50
         if $i.level < 75
            if rand(5)
               if isspelled($n) curse
                  if rand(50)
                     cast 'colour spray' $n
                     else
                     cast 'drown' $n
                     endif
                  else
                  cast 'curse' $n
                  endif
               endif
            endif
         endif
      endif
   endif
~
>act_prog p with lethal accuracy and strength.~
if isfight($i)
   if ispc($n)
      if $i.level > 89
         if rand(15)
            if isspelled($n) curse
               if rand(50)
                  cast 'colour spray' $n
                  else
                  cast 'drown' $n
                  endif
               else
               cast 'curse' $n
               endif
            endif
         endif
      if $i.level > 74
         if $i.level < 90
            if rand(10)
               if isspelled($n) curse
                  if rand(50)
                     cast 'colour spray' $n
                     else
                     cast 'drown' $n
                     endif
                  else
                  cast 'curse' $n
                  endif
               endif
            endif
         endif
     if $i.level > 50
         if $i.level < 75
            if rand(5)
               if isspelled($n) curse
                  if rand(50)
                     cast 'colour spray' $n
                     else
                     cast 'drown' $n
                     endif
                  else
                  cast 'curse' $n
                  endif
               endif
            endif
         endif
      endif
   endif
~
>act_prog p circles around~
if isfight($i)
   if ispc($n)
      if $i.level > 89
         if rand(15)
            if isspelled($n) curse
               if rand(50)
                  cast 'colour spray' $n
                  else
                  cast 'drown' $n
                  endif
               else
               cast 'curse' $n
               endif
            endif
         endif
      if $i.level > 74
         if $i.level < 90
            if rand(10)
               if isspelled($n) curse
                  if rand(50)
                     cast 'colour spray' $n
                     else
                     cast 'drown' $n
                     endif
                  else
                  cast 'curse' $n
                  endif
               endif
            endif
         endif
     if $i.level > 50
         if $i.level < 75
            if rand(5)
               if isspelled($n) curse
                  if rand(50)
                     cast 'colour spray' $n
                     else
                     cast 'drown' $n
                     endif
                  else
                  cast 'curse' $n
                  endif
               endif
            endif
         endif
      endif
   endif
~
>act_prog p utters the words, 'colour spray'~
if isfight($i)
   if ispc($n)
      if $i.level > 89
         if rand(15)
            if isspelled($n) curse
               if rand(50)
                  cast 'colour spray' $n
                  else
                  cast 'drown' $n
                  endif
               else
               cast 'curse' $n
               endif
            endif
         endif
      if $i.level > 74
         if $i.level < 90
            if rand(10)
               if isspelled($n) curse
                  if rand(50)
                     cast 'colour spray' $n
                     else
                     cast 'drown' $n
                     endif
                  else
                  cast 'curse' $n
                  endif
               endif
            endif
         endif
     if $i.level > 50
         if $i.level < 75
            if rand(5)
               if isspelled($n) curse
                  if rand(50)
                     cast 'colour spray' $n
                     else
                     cast 'drown' $n
                     endif
                  else
                  cast 'curse' $n
                  endif
               endif
            endif
         endif
      endif
   endif
~
>act_prog p utters the words, 'call lightning'~
if isfight($i)
   if ispc($n)
      if $i.level > 89
         if rand(15)
            if isspelled($n) curse
               if rand(50)
                  cast 'colour spray' $n
                  else
                  cast 'drown' $n
                  endif
               else
               cast 'curse' $n
               endif
            endif
         endif
      if $i.level > 74
         if $i.level < 90
            if rand(10)
               if isspelled($n) curse
                  if rand(50)
                     cast 'colour spray' $n
                     else
                     cast 'drown' $n
                     endif
                  else
                  cast 'curse' $n
                  endif
               endif
            endif
         endif
     if $i.level > 50
         if $i.level < 75
            if rand(5)
               if isspelled($n) curse
                  if rand(50)
                     cast 'colour spray' $n
                     else
                     cast 'drown' $n
                     endif
                  else
                  cast 'curse' $n
                  endif
               endif
            endif
         endif
      endif
   endif
~
>act_prog p utters the words, 'sun ray'~
if isfight($i)
   if ispc($n)
      if $i.level > 89
         if rand(15)
            if isspelled($n) curse
               if rand(50)
                  cast 'colour spray' $n
                  else
                  cast 'drown' $n
                  endif
               else
               cast 'curse' $n
               endif
            endif
         endif
      if $i.level > 74
         if $i.level < 90
            if rand(10)
               if isspelled($n) curse
                  if rand(50)
                     cast 'colour spray' $n
                     else
                     cast 'drown' $n
                     endif
                  else
                  cast 'curse' $n
                  endif
               endif
            endif
         endif
     if $i.level > 50
         if $i.level < 75
            if rand(5)
               if isspelled($n) curse
                  if rand(50)
                     cast 'colour spray' $n
                     else
                     cast 'drown' $n
                     endif
                  else
                  cast 'curse' $n
                  endif
               endif
            endif
         endif
      endif
   endif
~
>act_prog p utters the words, 'drown'~
if isfight($i)
   if ispc($n)
      if $i.level > 89
         if rand(15)
            if isspelled($n) curse
               if rand(50)
                  cast 'colour spray' $n
                  else
                  cast 'drown' $n
                  endif
               else
               cast 'curse' $n
               endif
            endif
         endif
      if $i.level > 74
         if $i.level < 90
            if rand(10)
               if isspelled($n) curse
                  if rand(50)
                     cast 'colour spray' $n
                     else
                     cast 'drown' $n
                     endif
                  else
                  cast 'curse' $n
                  endif
               endif
            endif
         endif
     if $i.level > 50
         if $i.level < 75
            if rand(5)
               if isspelled($n) curse
                  if rand(50)
                     cast 'colour spray' $n
                     else
                     cast 'drown' $n
                     endif
                  else
                  cast 'curse' $n
                  endif
               endif
            endif
         endif
      endif
   endif
~
>act_prog p deathstroke~
if isfight($i)
   if ispc($n)
      if $i.level > 89
         if rand(15)
            if isspelled($n) curse
               if rand(50)
                  cast 'colour spray' $n
                  else
                  cast 'drown' $n
                  endif
               else
               cast 'curse' $n
               endif
            endif
         endif
      if $i.level > 74
         if $i.level < 90
            if rand(10)
               if isspelled($n) curse
                  if rand(50)
                     cast 'colour spray' $n
                     else
                     cast 'drown' $n
                     endif
                  else
                  cast 'curse' $n
                  endif
               endif
            endif
         endif
     if $i.level > 50
         if $i.level < 75
            if rand(5)
               if isspelled($n) curse
                  if rand(50)
                     cast 'colour spray' $n
                     else
                     cast 'drown' $n
                     endif
                  else
                  cast 'curse' $n
                  endif
               endif
            endif
         endif
      endif
   endif
~
>act_prog p attempts to hit you in your solar plexus!~
if isfight($i)
   if ispc($n)
      if $i.level > 89
         if rand(15)
            if isspelled($n) curse
               if rand(50)
                  cast 'colour spray' $n
                  else
                  cast 'drown' $n
                  endif
               else
               cast 'curse' $n
               endif
            endif
         endif
      if $i.level > 74
         if $i.level < 90
            if rand(10)
               if isspelled($n) curse
                  if rand(50)
                     cast 'colour spray' $n
                     else
                     cast 'drown' $n
                     endif
                  else
                  cast 'curse' $n
                  endif
               endif
            endif
         endif
     if $i.level > 50
         if $i.level < 75
            if rand(5)
               if isspelled($n) curse
                  if rand(50)
                     cast 'colour spray' $n
                     else
                     cast 'drown' $n
                     endif
                  else
                  cast 'curse' $n
                  endif
               endif
            endif
         endif
      endif
   endif
~
>act_prog p lunges forward in an attempt to smash into you but falls flat on~
if isfight($i)
   if ispc($n)
      if $i.level > 89
         if rand(15)
            if isspelled($n) curse
               if rand(50)
                  cast 'colour spray' $n
                  else
                  cast 'drown' $n
                  endif
               else
               cast 'curse' $n
               endif
            endif
         endif
      if $i.level > 74
         if $i.level < 90
            if rand(10)
               if isspelled($n) curse
                  if rand(50)
                     cast 'colour spray' $n
                     else
                     cast 'drown' $n
                     endif
                  else
                  cast 'curse' $n
                  endif
               endif
            endif
         endif
     if $i.level > 50
         if $i.level < 75
            if rand(5)
               if isspelled($n) curse
                  if rand(50)
                     cast 'colour spray' $n
                     else
                     cast 'drown' $n
                     endif
                  else
                  cast 'curse' $n
                  endif
               endif
            endif
         endif
      endif
   endif
~
>act_prog p tries to headbutt you, but fails miserably.~
if isfight($i)
   if ispc($n)
      if $i.level > 89
         if rand(15)
            if isspelled($n) curse
               if rand(50)
                  cast 'colour spray' $n
                  else
                  cast 'drown' $n
                  endif
               else
               cast 'curse' $n
               endif
            endif
         endif
      if $i.level > 74
         if $i.level < 90
            if rand(10)
               if isspelled($n) curse
                  if rand(50)
                     cast 'colour spray' $n
                     else
                     cast 'drown' $n
                     endif
                  else
                  cast 'curse' $n
                  endif
               endif
            endif
         endif
     if $i.level > 50
         if $i.level < 75
            if rand(5)
               if isspelled($n) curse
                  if rand(50)
                     cast 'colour spray' $n
                     else
                     cast 'drown' $n
                     endif
                  else
                  cast 'curse' $n
                  endif
               endif
            endif
         endif
      endif
   endif
~
>act_prog p starts swinging~
if isfight($i)
   if ispc($n)
      if $i.level > 89
         if rand(15)
            if isspelled($n) curse
               if rand(50)
                  cast 'colour spray' $n
                  else
                  cast 'drown' $n
                  endif
               else
               cast 'curse' $n
               endif
            endif
         endif
      if $i.level > 74
         if $i.level < 90
            if rand(10)
               if isspelled($n) curse
                  if rand(50)
                     cast 'colour spray' $n
                     else
                     cast 'drown' $n
                     endif
                  else
                  cast 'curse' $n
                  endif
               endif
            endif
         endif
     if $i.level > 50
         if $i.level < 75
            if rand(5)
               if isspelled($n) curse
                  if rand(50)
                     cast 'colour spray' $n
                     else
                     cast 'drown' $n
                     endif
                  else
                  cast 'curse' $n
                  endif
               endif
            endif
         endif
      endif
   endif
~
>act_prog p starts advancing toward you, but trips over~
if isfight($i)
   if ispc($n)
      if $i.level > 89
         if rand(15)
            if isspelled($n) curse
               if rand(50)
                  cast 'colour spray' $n
                  else
                  cast 'drown' $n
                  endif
               else
               cast 'curse' $n
               endif
            endif
         endif
      if $i.level > 74
         if $i.level < 90
            if rand(10)
               if isspelled($n) curse
                  if rand(50)
                     cast 'colour spray' $n
                     else
                     cast 'drown' $n
                     endif
                  else
                  cast 'curse' $n
                  endif
               endif
            endif
         endif
     if $i.level > 50
         if $i.level < 75
            if rand(5)
               if isspelled($n) curse
                  if rand(50)
                     cast 'colour spray' $n
                     else
                     cast 'drown' $n
                     endif
                  else
                  cast 'curse' $n
                  endif
               endif
            endif
         endif
      endif
   endif
~
>act_prog p advances confidently towards you, and flies into a rage!~
if isfight($i)
   if ispc($n)
      if $i.level > 89
         if rand(15)
            if isspelled($n) curse
               if rand(50)
                  cast 'colour spray' $n
                  else
                  cast 'drown' $n
                  endif
               else
               cast 'curse' $n
               endif
            endif
         endif
      if $i.level > 74
         if $i.level < 90
            if rand(10)
               if isspelled($n) curse
                  if rand(50)
                     cast 'colour spray' $n
                     else
                     cast 'drown' $n
                     endif
                  else
                  cast 'curse' $n
                  endif
               endif
            endif
         endif
     if $i.level > 50
         if $i.level < 75
            if rand(5)
               if isspelled($n) curse
                  if rand(50)
                     cast 'colour spray' $n
                     else
                     cast 'drown' $n
                     endif
                  else
                  cast 'curse' $n
                  endif
               endif
            endif
         endif
      endif
   endif
~
>act_prog p utters the words, 'qfzzsuio oculoahp'~
if isfight($i)
   if ispc($n)
      if $i.level > 89
         if rand(15)
            if isspelled($n) curse
               if rand(50)
                  cast 'colour spray' $n
                  else
                  cast 'drown' $n
                  endif
               else
               cast 'curse' $n
               endif
            endif
         endif
      if $i.level > 74
         if $i.level < 90
            if rand(10)
               if isspelled($n) curse
                  if rand(50)
                     cast 'colour spray' $n
                     else
                     cast 'drown' $n
                     endif
                  else
                  cast 'curse' $n
                  endif
               endif
            endif
         endif
     if $i.level > 50
         if $i.level < 75
            if rand(5)
               if isspelled($n) curse
                  if rand(50)
                     cast 'colour spray' $n
                     else
                     cast 'drown' $n
                     endif
                  else
                  cast 'curse' $n
                  endif
               endif
            endif
         endif
      endif
   endif
~
>act_prog p You dodge a bash from~
if isspelled($i) iron_roots
    else
    if $i.level > 89
        if rand(30)
            cast 'iron roots'
            endif
        endif
    if $i.level > 74
        if $i.level < 90
            if rand(20)
                cast 'iron roots'
                endif
            endif
        endif
    if $i.level > 50
       if $i.level < 75
           if rand(10)
               cast 'iron roots'
               endif
           endif
        endif
    endif
~
>act_prog p sends you sprawling.~
if isspelled($i) iron_roots
    else
    if $i.level > 89
        if rand(30)
            cast 'iron roots'
            endif
        endif
    if $i.level > 74
        if $i.level < 90
            if rand(20)
                cast 'iron roots'
                endif
            endif
        endif
    if $i.level > 50
       if $i.level < 75
           if rand(10)
               cast 'iron roots'
               endif
           endif
        endif
    endif
~
>act_prog p utters the words, 'euzuiz yjfl'~
if isfight($i)
   if ispc($n)
      if $i.level > 89
         if rand(15)
            if isspelled($n) curse
               if rand(50)
                  cast 'colour spray' $n
                  else
                  cast 'drown' $n
                  endif
               else
               cast 'curse' $n
               endif
            endif
         endif
      if $i.level > 74
         if $i.level < 90
            if rand(10)
               if isspelled($n) curse
                  if rand(50)
                     cast 'colour spray' $n
                     else
                     cast 'drown' $n
                     endif
                  else
                  cast 'curse' $n
                  endif
               endif
            endif
         endif
     if $i.level > 50
         if $i.level < 75
            if rand(5)
               if isspelled($n) curse
                  if rand(50)
                     cast 'colour spray' $n
                     else
                     cast 'drown' $n
                     endif
                  else
                  cast 'curse' $n
                  endif
               endif
            endif
         endif
      endif
   endif
~
>act_prog p utters the words, 'garandoa oahz'~
if isfight($i)
   if ispc($n)
      if $i.level > 89
         if rand(15)
            if isspelled($n) curse
               if rand(50)
                  cast 'colour spray' $n
                  else
                  cast 'drown' $n
                  endif
               else
               cast 'curse' $n
               endif
            endif
         endif
      if $i.level > 74
         if $i.level < 90
            if rand(10)
               if isspelled($n) curse
                  if rand(50)
                     cast 'colour spray' $n
                     else
                     cast 'drown' $n
                     endif
                  else
                  cast 'curse' $n
                  endif
               endif
            endif
         endif
     if $i.level > 50
         if $i.level < 75
            if rand(5)
               if isspelled($n) curse
                  if rand(50)
                     cast 'colour spray' $n
                     else
                     cast 'drown' $n
                     endif
                  else
                  cast 'curse' $n
                  endif
               endif
            endif
         endif
      endif
   endif
~
>act_prog p utters the words, 'wzhzaf gxandoaw'~
if isfight($i)
   if ispc($n)
      if $i.level > 89
         if rand(15)
            if isspelled($n) curse
               if rand(50)
                  cast 'colour spray' $n
                  else
                  cast 'drown' $n
                  endif
               else
               cast 'curse' $n
               endif
            endif
         endif
      if $i.level > 74
         if $i.level < 90
            if rand(10)
               if isspelled($n) curse
                  if rand(50)
                     cast 'colour spray' $n
                     else
                     cast 'drown' $n
                     endif
                  else
                  cast 'curse' $n
                  endif
               endif
            endif
         endif
     if $i.level > 50
         if $i.level < 75
            if rand(5)
               if isspelled($n) curse
                  if rand(50)
                     cast 'colour spray' $n
                     else
                     cast 'drown' $n
                     endif
                  else
                  cast 'curse' $n
                  endif
               endif
            endif
         endif
      endif
   endif
~
>act_prog p utters the words, 'yufzghafw'~
if isfight($i)
   if ispc($n)
      if $i.level > 89
         if rand(15)
            if isspelled($n) curse
               if rand(50)
                  cast 'colour spray' $n
                  else
                  cast 'drown' $n
                  endif
               else
               cast 'curse' $n
               endif
            endif
         endif
      if $i.level > 74
         if $i.level < 90
            if rand(10)
               if isspelled($n) curse
                  if rand(50)
                     cast 'colour spray' $n
                     else
                     cast 'drown' $n
                     endif
                  else
                  cast 'curse' $n
                  endif
               endif
            endif
         endif
     if $i.level > 50
         if $i.level < 75
            if rand(5)
               if isspelled($n) curse
                  if rand(50)
                     cast 'colour spray' $n
                     else
                     cast 'drown' $n
                     endif
                  else
                  cast 'curse' $n
                  endif
               endif
            endif
         endif
      endif
   endif
~
>act_prog p utters the words, 'sandoaarlkz'~
if isfight($i)
   if ispc($n)
      if $i.level > 89
         if rand(15)
            if isspelled($n) curse
               if rand(50)
                  cast 'colour spray' $n
                  else
                  cast 'drown' $n
                  endif
               else
               cast 'curse' $n
               endif
            endif
         endif
      if $i.level > 74
         if $i.level < 90
            if rand(10)
               if isspelled($n) curse
                  if rand(50)
                     cast 'colour spray' $n
                     else
                     cast 'drown' $n
                     endif
                  else
                  cast 'curse' $n
                  endif
               endif
            endif
         endif
     if $i.level > 50
         if $i.level < 75
            if rand(5)
               if isspelled($n) curse
                  if rand(50)
                     cast 'colour spray' $n
                     else
                     cast 'drown' $n
                     endif
                  else
                  cast 'curse' $n
                  endif
               endif
            endif
         endif
      endif
   endif
~
>act_prog p utters the words, 'pzrrghxanduaw'~
if isfight($i)
   if ispc($n)
      if $i.level > 89
         if rand(15)
            if isspelled($n) curse
               if rand(50)
                  cast 'colour spray' $n
                  else
                  cast 'drown' $n
                  endif
               else
               cast 'curse' $n
               endif
            endif
         endif
      if $i.level > 74
         if $i.level < 90
            if rand(10)
               if isspelled($n) curse
                  if rand(50)
                     cast 'colour spray' $n
                     else
                     cast 'drown' $n
                     endif
                  else
                  cast 'curse' $n
                  endif
               endif
            endif
         endif
     if $i.level > 50
         if $i.level < 75
            if rand(5)
               if isspelled($n) curse
                  if rand(50)
                     cast 'colour spray' $n
                     else
                     cast 'drown' $n
                     endif
                  else
                  cast 'curse' $n
                  endif
               endif
            endif
         endif
      endif
   endif
~
>act_prog p utters the words, 'eugszr~
if isfight($i)
   if ispc($n)
      if $i.level > 89
         if rand(15)
            if isspelled($n) curse
               if rand(50)
                  cast 'colour spray' $n
                  else
                  cast 'drown' $n
                  endif
               else
               cast 'curse' $n
               endif
            endif
         endif
      if $i.level > 74
         if $i.level < 90
            if rand(10)
               if isspelled($n) curse
                  if rand(50)
                     cast 'colour spray' $n
                     else
                     cast 'drown' $n
                     endif
                  else
                  cast 'curse' $n
                  endif
               endif
            endif
         endif
     if $i.level > 50
         if $i.level < 75
            if rand(5)
               if isspelled($n) curse
                  if rand(50)
                     cast 'colour spray' $n
                     else
                     cast 'drown' $n
                     endif
                  else
                  cast 'curse' $n
                  endif
               endif
            endif
         endif
      endif
   endif
~
>act_prog p utters the words, 'saxzf pandoaw'~
if isfight($i)
   if ispc($n)
      if $i.level > 89
         if rand(15)
            if isspelled($n) curse
               if rand(50)
                  cast 'colour spray' $n
                  else
                  cast 'drown' $n
                  endif
               else
               cast 'curse' $n
               endif
            endif
         endif
      if $i.level > 74
         if $i.level < 90
            if rand(10)
               if isspelled($n) curse
                  if rand(50)
                     cast 'colour spray' $n
                     else
                     cast 'drown' $n
                     endif
                  else
                  cast 'curse' $n
                  endif
               endif
            endif
         endif
     if $i.level > 50
         if $i.level < 75
            if rand(5)
               if isspelled($n) curse
                  if rand(50)
                     cast 'colour spray' $n
                     else
                     cast 'drown' $n
                     endif
                  else
                  cast 'curse' $n
                  endif
               endif
            endif
         endif
      endif
   endif
~
>act_prog p utters the words, 'yrawzghfutz'~
if isfight($i)
   if ispc($n)
      if $i.level > 89
         if rand(15)
            if isspelled($n) curse
               if rand(50)
                  cast 'colour spray' $n
                  else
                  cast 'drown' $n
                  endif
               else
               cast 'curse' $n
               endif
            endif
         endif
      if $i.level > 74
         if $i.level < 90
            if rand(10)
               if isspelled($n) curse
                  if rand(50)
                     cast 'colour spray' $n
                     else
                     cast 'drown' $n
                     endif
                  else
                  cast 'curse' $n
                  endif
               endif
            endif
         endif
     if $i.level > 50
         if $i.level < 75
            if rand(5)
               if isspelled($n) curse
                  if rand(50)
                     cast 'colour spray' $n
                     else
                     cast 'drown' $n
                     endif
                  else
                  cast 'curse' $n
                  endif
               endif
            endif
         endif
      endif
   endif
~
>act_prog p utters the words, 'pzar gsfal'~
if isfight($i)
   if ispc($n)
      if $i.level > 89
         if rand(15)
            if isspelled($n) curse
               if rand(50)
                  cast 'colour spray' $n
                  else
                  cast 'drown' $n
                  endif
               else
               cast 'curse' $n
               endif
            endif
         endif
      if $i.level > 74
         if $i.level < 90
            if rand(10)
               if isspelled($n) curse
                  if rand(50)
                     cast 'colour spray' $n
                     else
                     cast 'drown' $n
                     endif
                  else
                  cast 'curse' $n
                  endif
               endif
            endif
         endif
     if $i.level > 50
         if $i.level < 75
            if rand(5)
               if isspelled($n) curse
                  if rand(50)
                     cast 'colour spray' $n
                     else
                     cast 'drown' $n
                     endif
                  else
                  cast 'curse' $n
                  endif
               endif
            endif
         endif
      endif
   endif
~
>act_prog p trips you and you go down!~
if isspelled($i) iron_roots
    else
    if $i.level > 89
        if rand(30)
            cast 'iron roots'
            endif
        endif
    if $i.level > 74
        if $i.level < 90
            if rand(20)
                cast 'iron roots'
                endif
            endif
        endif
    if $i.level > 50
       if $i.level < 75
           if rand(10)
               cast 'iron roots'
               endif
           endif
        endif
    endif
~
>fight_prog 50~
weather
~
>fight_prog 10~
if tempvar($i[israiny]) == true
    cast 'call lightning'
    else
    if tempvar($i[issunny]) == true
        cast 'sun ray'
        else
        if rand(33)
            cast 'entangle'
            else
            if rand(50)
                cast 'colour spray'
                else
                cast 'drown'
                endif
            endif
        endif
    endif
~
>fight_prog 8~
if rand(33)
    cast 'entangle'
    else
    if rand(50)
        cast 'colour spray'
        else
        cast 'drown'
        endif
    endif
~
>act_prog You have no feeling~
MPSETTEMP $i israiny null
MPSETTEMP $i issunny null
~
>rand_prog 100~
if isfight($i)
    else
    if isspelled($i) iron_roots
        cast 'iron roots'
        else
        if isfight($r)
            if isnpc($r)
                if isfollow($r)
                else
                cast 'power heal' $r
                endif
            endif
        endif
      endif
   endif
~
>load_prog 100~
cast 'eyes of the owl'
cast 'sense life'
cast 'camouflage'
cast 'resist cold'
cast 'resist acid'
cast 'resist energy'
cast 'resist fire'
cast 'barkskin'
cast 'stoneskin'
cast 'staunchblood'
cast 'oaken fortitude'
cast 'rapid mend'
cast 'greater stoneshield'
cast 'lightning shield'
~
>fight_prog 5~
if isspelled($n) debility
    if rand(33)
        cast 'entangle'
        else
        if rand(50)
            cast 'colour spray'
            else
            cast 'drown'
            endif
        endif
   else
   cast 'debility' $n
   endif
~
>act_prog cloudless~
MPSETTEMP $i issunny true
MPSETTEMP $i israiny false
~
>act_prog cloudy~
MPSETTEMP $i issunny true
MPSETTEMP $i israiny false
~
>act_prog flashes~
MPSETTEMP $i israiny true
MPSETTEMP $i issunny false
~
>act_prog rainy~
MPSETTEMP $i israiny true
MPSETTEMP $i issunny false
~
>fight_prog 5~
if isspelled($n) poison
    if rand(33)
        cast 'entangle'
        else
        if rand(50)
            cast 'colour spray'
            else
            cast 'drown'
            endif
        endif
   else
   cast 'poison' $n
   endif
~
>fight_prog 5~
if isspelled($n) weaken
    if rand(33)
        cast 'entangle'
        else
        if rand(50)
            cast 'colour spray'
            else
            cast 'drown'
            endif
        endif
   else
   cast 'weaken' $n
   endif
~
>fight_prog 4~
if isspelled($n) blindness
or isspelled($n) curse
or isspelled($n) attrition
     if rand(33)
        cast 'entangle'
        else
        if rand(50)
            cast 'colour spray'
            else
            cast 'drown'
            endif
        endif
    else
    cast 'attrition' $n
    endif
~
>fight_prog 4~
if isspelled($n) blindness
or isspelled($n) curse
or isspelled($n) attrition
     if rand(33)
         cast 'entangle'
         else
         if rand(50)
             cast 'colour spray'
             else
             cast 'drown'
             endif
         endif
    else
    cast 'curse' $n
    endif
~
>fight_prog 3~
if isspelled($n) blindness
or isspelled($n) curse
or isspelled($n) attrition
     if rand(33)
         cast 'entangle'
         else
         if rand(50)
             cast 'colour spray'
             else
             cast 'drown'
             endif
        endif
    else
    cast 'blindness' $n
    endif
~
>hitprcnt_prog 50~
if rand(25)
  cast 'power heal' $i
endif
~
>arand_prog 1~
if isspelled($i) oaken_fortitude
    else
    cast 'oaken fortitude'
    endif
if isspelled($i) barkskin
    else
    cast 'barkskin'
    endif
if isspelled($i) camouflague
    else
    cast 'camouflage'
    endif
if isspelled($i) stoneshield
   else
   cast 'greater stoneshield'
   endif
if isspelled($i) infravision
    else
    cast 'eyes of the owl'
    endif
if isspelled($i) sense_life
    else
    cast 'sense life'
    endif
if isspelled($i) resist_cold
    else
    cast 'resist cold'
    endif
if isspelled($i) resist_acid
    else
    cast 'resist acid'
    endif
if isspelled($i) resist_energy
    else
    cast 'resist energy'
    endif
if isspelled($i) resist_fire
    else
    cast 'resist fire'
    endif
if isspelled($i) stoneskin
    else
    cast 'stoneskin'
    endif
if isspelled($i) lightning_shield
    else
    cast 'lightning shield'
    endif
if isspelled($i) staunchblood
    else
    cast 'staunchblood'
    endif
if isspelled($i) rapid_mend
    else
    cast 'rapid mend'
    endif
~
|
S
#145
skillset mob~
PROC MOB NECROMANCER LEVEL 1-20~
This is a Skillset Mob for the Necromancer Class, levels 1-20.
~
~
0 0 -1
35651624 0 0 -1
0 0 0
20 10 0d1+0 1d1+0
0 0
8 8 0 0 0 0
>load_prog 100~
cast 'shield' $i
cast 'resist acid' $i
~
>fight_prog 10~
cast 'chill touch' $n
~
>fight_prog 10~
if isspelled($n) weaken
  cast 'chill touch' $n
else
  cast 'weaken' $n
endif
~
>arand_prog 1~
if isspelled($i) shield
else
  cast 'shield' $i
  break
endif
if isspelled($i) resist_acid
else
  cast 'resist acid' $i
  break
endif
~
|
S
#146
skillset mob~
PROC MOB NECROMANCER LEVEL 21-35~
This is a Skillset Mob for the Necromancer Class, levels 21-35.
~
~
16777216 0 -1
35651624 0 0 -1
0 0 0
20 10 0d1+0 1d1+0
0 0
8 8 0 0 0 0
>load_prog 100~
cast 'shield' $i
cast 'resist acid' $i
cast 'resist cold' $i
~
>fight_prog 10~
cast 'souldrain' $n
~
>fight_prog 10~
MPECHO $I flashes an evil grin, and you can almost catch a glimpse of some fangs...
cast 'vampiric touch' $n
~
>fight_prog 10~
cast 'chill touch' $n
~
>arand_prog 1~
if isspelled($i) shield
else
  cast 'shield' $i
  break
endif
if isspelled($i) resist_acid
else
  cast 'resist acid' $i
  break
endif
if isspelled($i) resist_cold
else
  cast 'resist cold' $i
  break
endif
~
|
S
#147
skillset mob~
PROC MOB NECROMANCER LEVEL 36-50~
This is a Skillset Mob for the Necromancer Class, levels 36-50.
~
~
16777216 0 -1
2359338 0 0 -1
0 0 0
20 10 1d1+0 1d1+0
0 0
8 8 0 0 0 0
>load_prog 100~
cast 'shield' $i
cast 'resist acid' $i
cast 'resist cold' $i
~
>fight_prog 5~
MPECHO With a wave of $l hand, $I summons forth a zombie for protection.
MPMLOAD 17390
say Kill! KIIIIILL!
MPFORCE zombie kill $r
cackle
~
>fight_prog 10~
cast 'souldrain' $n
~
>fight_prog 15~
MPECHO $I flashes an evil grin, and you can almost catch a glimpse of some fangs...
cast 'vampiric touch' $n
~
>fight_prog 10~
cast 'chill touch' $n
~
>fight_prog 25~
if level($i) > 39
  if ismobvnuminroom(22394)
  or ismobvnuminroom(22395)
  or ismobvnuminroom(22396)
  or ismobvnuminroom(22397)
  or ismobvnuminroom(22398)
    cackle
    MPECHO With a wave of $l hand, $I sends a zombie follower back to the grave.
    MPPURGE zombie
    break
  endif
endif
~
>fight_prog 15~
if level($i) > 39
  if numpcs() > 2
    cast 'life leech'
    break
  endif
endif
~
>arand_prog 1~
if isspelled($i) shield
else
  cast 'shield' $i
  break
endif
if isspelled($i) resist_acid
else
  cast 'resist acid' $i
  break
endif
if isspelled($i) resist_cold
else
  cast 'resist cold' $i
  break
endif
~
|
S
#148
skillset mob~
PROC MOB NECROMANCER LEVEL 51+~
This is a Skillset Mob for the Necromancer Class, levels 51+.
~
~
16777216 0 -1
35913768 0 0 -1
0 0 0
20 10 500d1+0 1d1+0
0 0
8 8 0 0 0 0
>damage_prog 250~
if cansee($n)
    if $i.level > 89
        if rand(15)
            MPECHOAT $n $I utters a sinister incantation as $j suddenly grabs you.
            MPECHOAT $n Your vision momentarily blurs with the sudden pain.
            MPECHOAROUND $n $I utters a sinister incantation as $j suddenly grabs $n.
            MPDAMAGE $n 1d1% undefined hitpoints
            MPDAMAGE $n 50d5 undefined hitpoints
            endif
        endif
    if $i.level > 74
        if $i.level < 90
            if rand(10)
                MPECHOAT $n $I utters a sinister incantation as $j suddenly grabs you.
                MPECHOAT $n Your vision momentarily blurs with the sudden pain.
                MPECNOAROUND $n $I utters a sinister incantation as $j suddenly grabs $n.
                MPDAMAGE $n 1d1% undefined hitpoints
                MPDAMAGE $n 45d5 undefined hitpoints
                endif
             endif
         endif
     if $i.level > 50
        if $i.level < 75
            if rand(5)
                MPECHOAT $n $I utters a sinister incantation as $j suddenly grabs you.
                MPECHOAT $n Your vision momentarily blurs with the sudden pain.
                MPECHOAROUND $n $I utters a sinister incantation as $j suddenly grabs $n.
                MPDAMAGE $n 40d5 undefined hitpoints
                endif
             endif
          endif
      endif
~
>act_prog p skull explodes, releasing a blast of negative energy.~
MPSETTEMP $i skulltrap false
~
>fight_prog 5~
if ismobvnuminroom(17390)
   if tempvar($i[skulltrap]) == true
      else
      if $i.level > 89
         MPSETTEMP $i skulltrap true
         MPTHROW 17390 1 0
         endif
      if $i.level > 74
         if $i.level < 90
            MPSETTEMP $i skulltrap true
            MPTHROW 17390 2 0
            endif
         endif
     if $i.level > 50
         if $i.level < 75
            MPSETTEMP $i skulltrap true
            MPTHROW 17390 3 0
            endif
         endif
      endif
   endif
~
>fight_prog 10~
if ismobvnuminroom(17385)
   MPECHO With a quick wave of $l hand, $i utters a dark incantation.
   MPTHROW 17385 1 0
   else
   MPECHO $i whispers a strange incantation.
   cackle
   MPMLOAD 17385
   MPTHROW 17385 2 0 $i
   endif
~
>load_prog 100~
cast 'shield' $i
cast 'resist acid' $i
cast 'resist cold' $i
cast 'vampiric aura' $i
~
>fight_prog 10~
MPECHO With a wave of $l hand, $I summons forth a ghast for protection.
MPMLOAD 17390
say Kill! KIIIIILL!
MPTHROW 17390 4 0
cackle
~
>fight_prog 10~
cast 'souldrain'
~
>fight_prog 10~
MPECHO $I flashes an evil grin, and you can almost catch a glimpse of some fangs...
cast 'vampiric touch' $n
~
>fight_prog 10~
if isspelled($n) attrition
    cast 'chill touch'
    else
    cast 'attrition'
    endif
~
>fight_prog 25~
if level($i) > 39
  if ismobvnuminroom(22394)
  or ismobvnuminroom(22395)
  or ismobvnuminroom(22396)
  or ismobvnuminroom(22397)
  or ismobvnuminroom(22398)
    cackle
    MPECHO With a wave of $l hand, $I sends a zombie follower back to the grave.
    MPPURGE zombie
    break
  endif
endif
~
>arand_prog 1~
if isspelled($i) shield
    else
    cast 'shield'
    endif
if isspelled($i) resist_acid
    else
    cast 'resist acid'
    endif
if isspelled($i) resist_cold
    else
    cast 'resist cold'
    endif
if isspelled($i) vampiric_aura
    else
    cast 'vampiric aura'
    endif
~
|
S
#149
skillset mob~
PROC MOB PSIONICIST LEVEL 1-20~
This is a Skillset mob for the Psionicist Class, levels 1-20.
~
~
256 0 -1
2097216 0 0 -1
0 0 0
20 10 0d1+0 1d1+0
0 0
8 8 0 0 0 0
>load_prog 100~
cast 'staunchblood' $i
cast 'insomnia' $i
cast 'armor' $i
~
>fight_prog 10~
if isspelled($n) curse
  cast 'paralyze' $n
else
  cast 'curse' $n
endif
~
>fight_prog 10~
cast 'paralyze' $n
~
>arand_prog 1~
if isspelled($i) staunchblood
else
  cast 'staunchblood' $i
  break
endif
if isspelled($i) insomnia
else
  cast 'insomnia' $i
  break
endif
if isspelled($i) armor
else
  cast 'armor' $i
  break
endif
~
|
S
#150
skillset mob~
PROC MOB PSIONICIST LEVEL 21-35~
This is a Skillset mob for the Psionicist Class, levels 21-35.
~
~
256 0 -1
2097216 0 0 -1
0 0 0
20 10 0d1+0 1d1+0
0 0
8 8 0 0 0 0
>load_prog 100~
cast 'staunchblood' $i
cast 'insomnia' $i
cast 'armor' $i
cast 'resist energy' $i
cast 'resist fire' $i
~
>fight_prog 10~
if isspelled($n) curse
  cast 'paralyze' $n
else
  cast 'curse' $n
endif
~
>fight_prog 10~
if isspelled($n) blindness
  cast 'curse' $n
else
  cast 'blind' $n
endif
~
>fight_prog 10~
cast 'paralyze' $n
~
>arand_prog 1~
if isspelled($i) staunchblood
else
  cast 'staunchblood' $i
  break
endif
if isspelled($i) insomnia
else
  cast 'insomnia' $i
  break
endif
if isspelled($i) armor
else
  cast 'armor' $i
  break
endif
if isspelled($i) resist_energy
else
  cast 'resist energy' $i
  break
endif
if isspelled($i) resist_fire
else
  cast 'resist fire' $i
  break
endif
~
|
S
#151
skillset mob~
PROC MOB PSIONICIST LEVEL 36-50~
This is a Skillset mob for the Psionicist Class, levels 36-50.
~
~
256 0 -1
2097216 0 0 -1
0 0 0
20 10 0d1+0 1d1+0
0 0
8 8 0 0 0 0
>load_prog 100~
cast 'staunchblood' $i
cast 'insomnia' $i
cast 'armor' $i
cast 'resist energy' $i
cast 'resist fire' $i
cast 'shadowslip' $i
~
>fight_prog 10~
cast 'power harm' $n
~
>fight_prog 10~
cast 'paralyze' $n
~
>fight_prog 10~
if isspelled($n) curse
  cast 'paralyze' $n
else
  cast 'curse' $n
endif
~
>fight_prog 10~
if isspelled($n) blindness
  cast 'curse' $n
else
  cast 'blind' $n
endif
~
>fight_prog 10~
if level($i) > 39
  if isspelled($n) sanctuary
      cast 'dispel magic' $n
  else
    if isspelled($n) fireshield
      cast 'dispel magic' $n
  else
    if isspelled($n) stoneshield
      cast 'dispel magic' $n
  else
    if isspelled($n) lightning_shield
      cast 'dispel magic' $n
  else
    if isspelled($n) acid_shield
      cast 'dispel magic' $n
  endif
endif
~
>arand_prog 1 ~
if isspelled($i) staunchblood
else
  cast 'staunchblood' $i
  break
endif
if isspelled($i) insomnia
else
  cast 'insomnia' $i
  break
endif
if isspelled($i) armor
else
  cast 'armor' $i
  break
endif
if isspelled($i) resist_energy
else
  cast 'resist energy' $i
  break
endif
if isspelled($i) resist_fire
else
  cast 'resist fire' $i
  break
endif
if isspelled($i) shadowslip
else
  cast 'shadowslip' $i
  break
endif
~
|
S
#152
skillset mob~
PROC MOB PSIONICIST LEVEL 51+~
This is a Skillset mob for the Psionicist Class, levels 51+.
~
~
256 0 -1
2097216 0 0 -1
0 0 0
20 10 0d1+0 1d1+0
0 0
8 8 0 0 0 0
>load_prog 100~
cast 'staunchblood'
cast 'insomnia'
cast 'armor'
cast 'resist energy'
cast 'resist fire'
cast 'shadowslip'
cast 'lightning shield'
cast 'detect invisibility'
cast 'true sight'
cast 'haste'
cast 'rapid mend'
cast 'feline agility'
cast 'freefloat'
~
>fight_prog 10~
if ispc($r)
   MPECHOAT $r $I's gaze fixes upon you, inducing a sense of extreme vertigo.
   MPECHOAROUND $r $I's gaze fixes upon $r.
   MPBESTOW $r insane 2 2
   else
   MPECHOAT $n $I's gaze fixes upon you, inducing a sense of extreme vertigo.
   MPECHOAROUND $n $I's gaze fixes upon $n.
   MPBESTOW $n insane 2 2
   endif
~
>fight_prog 10~
cast 'power harm' $n
~
>fight_prog 10~
cast 'paralyze' $n
~
>fight_prog 10~
MPECHOAT $n A $$Bray of light$$R shoots from $I's outstretched limb to strike you in the face.
MPECHOAROUND $n A $$Bray of light$$r shoots from $I's outstretched limb to strike $n in the face.
MPADDLAG $n 8
MPDAMAGE $n 25d10 undefined hitpoints
~
>fight_prog 10~
MPECHOAT $n With a wave of $l hand, $I sends you flying across the room.
MPECHOAROUND $n With a wave of $l hand, $I sends $n flying across the room.
if isspelled($n) fly
or isspelled($n) freefloat
   MPDAMAGE $n 10d5 undefined hitpoints
   else
   MPDAMAGE $n 20d5 undefined hitpoints
   endif
~
>hitprcnt_prog 40~
if isfight($i)
   if ispc($n)
      if $i.level > 89
         if rand(15)
            if isspelled($n) solidity
               MPECHOAT $n $I's gaze fixes upon you, inducing a sudden fit of panic.
               MPECHOAROUND $n $I's gaze fixes upon $n.
               MPFORCE $n flee
               else
               MPECHO A look of either intense concentration or constipation briefly flashes across $I's face.
               say I wish you'd get lost!
               MPTELEPORT $n area
               endif
            endif
         endif
      if $i.level > 74
         if $i.level < 90
            if rand(10)
               if isspelled($n) solidity
                  MPECHOAT $n $I's gaze fixes upon you, inducing a sudden fit of panic.
                  MPECHOAROUND $n $I's gaze fixes upon $n.
                  MPFORCE $n flee
                  else
                  MPECHO A look of either intense concentration or constipation briefly flashes across $I's face.
                  say I wish you'd get lost!
                  MPTELEPORT $n area
                  endif
               endif
            endif
         endif
     if $i.level > 50
         if $i.level < 75
            if rand(5)
               if isspelled($n) solidity
                  MPECHOAT $n $I's gaze fixes upon you, inducing a sudden fit of panic.
                  MPECHOAROUND $n $I's gaze fixes upon $n.
                  MPFORCE $n flee
                  else
                  MPECHO A look of either intense concentration or constipation briefly flashes across $I's face.
                  say I wish you'd get lost!
                  MPTELEPORT $n area
                  endif
               endif
            endif
         endif
      endif
   endif
~
>fight_prog 10~
if isspelled($n) sanctuary
    cast 'dispel magic' $n
else
  if isspelled($n) fireshield
    cast 'dispel magic' $n
else
  if isspelled($n) stoneshield
    cast 'dispel magic' $n
else
  if isspelled($n) lightning_shield
    cast 'dispel magic' $n
else
  if isspelled($n) acid_shield
    cast 'dispel magic' $n
endif
endif
endif
endif
endif
~
>arand_prog 1~
if isspelled($i) staunchblood
    else
   cast 'staunchblood'
   endif
if isspelled($i) insomnia
   else
   cast 'insomnia'
   endif
if isspelled($i) armor
    else
    cast 'armor'
    endif
if isspelled($i) resist_energy
    else
    cast 'resist energy'
    endif
if isspelled($i) resist_fire
    else
    cast 'resist fire'
    endif
if isspelled($i) shadowslip
    else
    cast 'shadowslip'
    endif
if isspelled($i) lightning_shield
    else
    cast 'lightning shield'
   endif
if isspelled($i) detect_invisibility
    else
    cast 'detect invisibility'
    endif
if isspelled($i) true_sight
    else
    cast 'true sight'
    endif
if isspelled($i) haste
    else
    cast 'haste'
    endif
if isspelled($i) rapid_mend
    else
    cast 'rapid mend'
    endif
if isspelled($i) feline_agility
    else
    cast 'feline agility'
    endif
if isspelled($i) freefloat
    else
    cast 'freefloat'
    endif
~
|
S
$~
